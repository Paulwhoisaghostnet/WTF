# Token Product Stories

Token-producing applications must prove create, modify, transfer, burn, bundle, import, and export behavior.

## Macaroni

### Macaroni: User creates token

As a Creator
I want to user creates token using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User modifies token

As a Creator
I want to user modifies token using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User transfers token

As a Creator
I want to user transfers token using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User burns token

As a Creator
I want to user burns token using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User bundles token

As a Creator
I want to user bundles token using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Spaghetti

### Spaghetti: User creates token

As a Creator
I want to user creates token using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User modifies token

As a Creator
I want to user modifies token using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User transfers token

As a Creator
I want to user transfers token using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User burns token

As a Creator
I want to user burns token using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User bundles token

As a Creator
I want to user bundles token using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Gnocchi

### Gnocchi: User creates token

As a Creator
I want to user creates token using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User modifies token

As a Creator
I want to user modifies token using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User transfers token

As a Creator
I want to user transfers token using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User burns token

As a Creator
I want to user burns token using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User bundles token

As a Creator
I want to user bundles token using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Ravioli

### Ravioli: User creates token

As a Creator
I want to user creates token using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User modifies token

As a Creator
I want to user modifies token using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User transfers token

As a Creator
I want to user transfers token using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User burns token

As a Creator
I want to user burns token using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User bundles token

As a Creator
I want to user bundles token using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Rotini

### Rotini: User creates token

As a Creator
I want to user creates token using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User modifies token

As a Creator
I want to user modifies token using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User transfers token

As a Creator
I want to user transfers token using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User burns token

As a Creator
I want to user burns token using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User bundles token

As a Creator
I want to user bundles token using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Penne

### Penne: User creates token

As a Creator
I want to user creates token using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user creates token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User modifies token

As a Creator
I want to user modifies token using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user modifies token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User transfers token

As a Creator
I want to user transfers token using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user transfers token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User burns token

As a Creator
I want to user burns token using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user burns token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User bundles token

As a Creator
I want to user bundles token using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user bundles token
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User imports token elsewhere

As a Creator
I want to user imports token elsewhere using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user imports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User exports token elsewhere

As a Creator
I want to user exports token elsewhere using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user exports token elsewhere
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.
