# Contract Product Stories

Contract-producing applications must prove create, configure, deploy, upgrade, ownership transfer, and token-product integration behavior.

## Macaroni

### Macaroni: User creates contract

As a Creator
I want to user creates contract using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User configures contract

As a Creator
I want to user configures contract using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User deploys contract

As a Creator
I want to user deploys contract using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User upgrades contract

As a Creator
I want to user upgrades contract using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User transfers ownership

As a Creator
I want to user transfers ownership using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Macaroni: User integrates token products

As a Creator
I want to user integrates token products using Macaroni
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Macaroni has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Spaghetti

### Spaghetti: User creates contract

As a Creator
I want to user creates contract using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User configures contract

As a Creator
I want to user configures contract using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User deploys contract

As a Creator
I want to user deploys contract using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User upgrades contract

As a Creator
I want to user upgrades contract using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User transfers ownership

As a Creator
I want to user transfers ownership using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Spaghetti: User integrates token products

As a Creator
I want to user integrates token products using Spaghetti
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Spaghetti has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Gnocchi

### Gnocchi: User creates contract

As a Creator
I want to user creates contract using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User configures contract

As a Creator
I want to user configures contract using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User deploys contract

As a Creator
I want to user deploys contract using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User upgrades contract

As a Creator
I want to user upgrades contract using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User transfers ownership

As a Creator
I want to user transfers ownership using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Gnocchi: User integrates token products

As a Creator
I want to user integrates token products using Gnocchi
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Gnocchi has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Ravioli

### Ravioli: User creates contract

As a Creator
I want to user creates contract using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User configures contract

As a Creator
I want to user configures contract using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User deploys contract

As a Creator
I want to user deploys contract using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User upgrades contract

As a Creator
I want to user upgrades contract using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User transfers ownership

As a Creator
I want to user transfers ownership using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Ravioli: User integrates token products

As a Creator
I want to user integrates token products using Ravioli
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Ravioli has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Rotini

### Rotini: User creates contract

As a Creator
I want to user creates contract using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User configures contract

As a Creator
I want to user configures contract using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User deploys contract

As a Creator
I want to user deploys contract using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User upgrades contract

As a Creator
I want to user upgrades contract using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User transfers ownership

As a Creator
I want to user transfers ownership using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Rotini: User integrates token products

As a Creator
I want to user integrates token products using Rotini
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Rotini has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Penne

### Penne: User creates contract

As a Creator
I want to user creates contract using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User configures contract

As a Creator
I want to user configures contract using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User deploys contract

As a Creator
I want to user deploys contract using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User upgrades contract

As a Creator
I want to user upgrades contract using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User transfers ownership

As a Creator
I want to user transfers ownership using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Penne: User integrates token products

As a Creator
I want to user integrates token products using Penne
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Penne has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

## Lasagna

### Lasagna: User creates contract

As a Creator
I want to user creates contract using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user creates contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Lasagna: User configures contract

As a Creator
I want to user configures contract using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user configures contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Lasagna: User deploys contract

As a Creator
I want to user deploys contract using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user deploys contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Lasagna: User upgrades contract

As a Creator
I want to user upgrades contract using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user upgrades contract
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Lasagna: User transfers ownership

As a Creator
I want to user transfers ownership using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user transfers ownership
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.

### Lasagna: User integrates token products

As a Creator
I want to user integrates token products using Lasagna
So that the product lifecycle is proven beyond a route smoke test.

Acceptance Criteria:

Given Lasagna has the required package, wallet, network, and pinning inputs
When the creator executes user integrates token products
Then the app validates preconditions, creates only confirmed durable state, and emits enough evidence for Colander, WTF.ME, WTFOS pinning, or a collector to verify the result.

Failure Criteria:

- Wallet rejection, wrong chain, missing permission, unavailable pinner/indexer, duplicate action, or stale live state blocks completion and gives recovery instructions.
