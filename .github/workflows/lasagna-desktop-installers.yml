name: Lasagna Desktop Installers

on:
  workflow_dispatch:
    inputs:
      publish_release:
        description: "Attach installers to a GitHub release"
        type: boolean
        default: false
      release_tag:
        description: "Release tag to publish, for example lasagna-desktop-v1.0.1-alpha.1"
        required: false
        type: string
  push:
    tags:
      - "lasagna-desktop-v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS universal
            os: macos-latest
            command: npm run dist:mac --prefix apps/lasagna-desktop
            artifact: lasagna-desktop-macos
          - label: Windows x64
            os: windows-latest
            command: npm run dist:windows --prefix apps/lasagna-desktop
            artifact: lasagna-desktop-windows
          - label: Raspberry Pi arm64
            os: ubuntu-24.04-arm
            command: npm run dist:raspberry-pi --prefix apps/lasagna-desktop
            artifact: lasagna-desktop-raspberry-pi

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            apps/lasagna-desktop/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install desktop dependencies
        run: npm ci --prefix apps/lasagna-desktop

      - name: Verify desktop packaging policy
        run: npm run lasagna:desktop:check

      - name: Build installer
        run: ${{ matrix.command }}

      - name: Install and smoke packaged macOS artifacts
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          VERSION=$(node -p "require('./apps/lasagna-desktop/package.json').version")
          bash scripts/pasta-desktop-macos-artifact-smoke.sh \
            "lasagna" \
            "apps/lasagna-desktop/release/Lasagna-Studio-${VERSION}-mac-universal.dmg" \
            "apps/lasagna-desktop/release/Lasagna-Studio-${VERSION}-mac-universal.zip" \
            "lasagna-studio.app" \
            "lasagna-studio"

      - name: Install and smoke packaged Windows application
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $version = node -p "require('./apps/lasagna-desktop/package.json').version"
          $installer = Join-Path "${{ github.workspace }}" "apps\lasagna-desktop\release\Lasagna-Studio-$version-win-x64.exe"
          & ".\scripts\pasta-desktop-windows-installer-smoke.ps1" `
            -AppKey "lasagna" `
            -InstallerPath $installer `
            -ExecutableName "lasagna-studio.exe" `
            -ShortcutName "Lasagna Studio"

      - name: Install and smoke packaged Raspberry Pi application
        if: matrix.os == 'ubuntu-24.04-arm'
        shell: bash
        run: |
          VERSION=$(node -p "require('./apps/lasagna-desktop/package.json').version")
          bash scripts/pasta-desktop-linux-installer-smoke.sh \
            "lasagna" \
            "apps/lasagna-desktop/release/Lasagna-Studio-${VERSION}-linux-arm64.deb" \
            "lasagna-studio" \
            "lasagna-studio"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            apps/lasagna-desktop/release/*.dmg
            apps/lasagna-desktop/release/*.zip
            apps/lasagna-desktop/release/*.exe
            apps/lasagna-desktop/release/*.deb
            apps/lasagna-desktop/release/*.blockmap
            apps/lasagna-desktop/release/latest*.yml
          if-no-files-found: error

  release:
    name: Publish installer release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/lasagna-desktop-v') || (github.event_name == 'workflow_dispatch' && inputs.publish_release)
    steps:
      - name: Checkout release source
        uses: actions/checkout@v5

      - name: Validate release tag
        shell: bash
        run: |
          VERSION=$(node -p "require('./apps/lasagna-desktop/package.json').version")
          EXPECTED_TAG="lasagna-desktop-v${VERSION}"
          ACTUAL_TAG="${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}"
          if [ "$ACTUAL_TAG" != "$EXPECTED_TAG" ]; then
            echo "release tag must match package version: expected $EXPECTED_TAG, got ${ACTUAL_TAG:-empty}"
            exit 1
          fi
          REMOTE_SHA="$(git ls-remote --tags origin "refs/tags/${ACTUAL_TAG}^{}" | awk 'NR == 1 { print $1 }')"
          if [ -z "$REMOTE_SHA" ]; then
            REMOTE_SHA="$(git ls-remote --tags origin "refs/tags/${ACTUAL_TAG}" | awk 'NR == 1 { print $1 }')"
          fi
          if [ -n "$REMOTE_SHA" ] && [ "$REMOTE_SHA" != "$GITHUB_SHA" ]; then
            echo "existing release tag must identify the workflow source: expected $GITHUB_SHA, got $REMOTE_SHA"
            exit 1
          fi

      - name: Download installer artifacts
        uses: actions/download-artifact@v5
        with:
          path: release-artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find release-artifacts -maxdepth 3 -type f -print -exec cp {} release/ \;
          ls -lah release

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
          target_commitish: ${{ github.sha }}
          name: Lasagna Desktop ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
          files: release/*
          fail_on_unmatched_files: true
          prerelease: ${{ contains(github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name, '-alpha.') || contains(github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name, '-beta.') || contains(github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name, '-rc.') }}
