# Colander Individual User Stories

Each feature is expressed for Creator, Collector, Curator, Marketplace User, and Administrator, even when a role mostly observes or audits the feature.

## Wallet and network context

### Creator

As a Creator
I want to perform wallet and network context with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for wallet and network context are satisfied
When the creator attempts wallet and network context
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of wallet and network context before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for wallet and network context are satisfied
When the collector attempts wallet and network context
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how wallet and network context affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for wallet and network context are satisfied
When the curator attempts wallet and network context
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from wallet and network context without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for wallet and network context are satisfied
When the marketplace user attempts wallet and network context
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover wallet and network context without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for wallet and network context are satisfied
When the administrator attempts wallet and network context
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Manual contract open

### Creator

As a Creator
I want to perform manual contract open with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for manual contract open are satisfied
When the creator attempts manual contract open
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of manual contract open before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for manual contract open are satisfied
When the collector attempts manual contract open
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how manual contract open affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for manual contract open are satisfied
When the curator attempts manual contract open
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from manual contract open without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for manual contract open are satisfied
When the marketplace user attempts manual contract open
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover manual contract open without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for manual contract open are satisfied
When the administrator attempts manual contract open
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Pasta adapter detection

### Creator

As a Creator
I want to perform pasta adapter detection with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for pasta adapter detection are satisfied
When the creator attempts pasta adapter detection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of pasta adapter detection before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for pasta adapter detection are satisfied
When the collector attempts pasta adapter detection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how pasta adapter detection affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for pasta adapter detection are satisfied
When the curator attempts pasta adapter detection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from pasta adapter detection without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for pasta adapter detection are satisfied
When the marketplace user attempts pasta adapter detection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover pasta adapter detection without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for pasta adapter detection are satisfied
When the administrator attempts pasta adapter detection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Available action filtering

### Creator

As a Creator
I want to perform available action filtering with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for available action filtering are satisfied
When the creator attempts available action filtering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of available action filtering before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for available action filtering are satisfied
When the collector attempts available action filtering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how available action filtering affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for available action filtering are satisfied
When the curator attempts available action filtering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from available action filtering without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for available action filtering are satisfied
When the marketplace user attempts available action filtering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover available action filtering without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for available action filtering are satisfied
When the administrator attempts available action filtering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Relationship graph rendering

### Creator

As a Creator
I want to perform relationship graph rendering with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for relationship graph rendering are satisfied
When the creator attempts relationship graph rendering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of relationship graph rendering before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for relationship graph rendering are satisfied
When the collector attempts relationship graph rendering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how relationship graph rendering affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for relationship graph rendering are satisfied
When the curator attempts relationship graph rendering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from relationship graph rendering without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for relationship graph rendering are satisfied
When the marketplace user attempts relationship graph rendering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover relationship graph rendering without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for relationship graph rendering are satisfied
When the administrator attempts relationship graph rendering
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Generic FA2 transfer fallback

### Creator

As a Creator
I want to perform generic fa2 transfer fallback with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for generic fa2 transfer fallback are satisfied
When the creator attempts generic fa2 transfer fallback
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of generic fa2 transfer fallback before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for generic fa2 transfer fallback are satisfied
When the collector attempts generic fa2 transfer fallback
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how generic fa2 transfer fallback affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for generic fa2 transfer fallback are satisfied
When the curator attempts generic fa2 transfer fallback
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from generic fa2 transfer fallback without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for generic fa2 transfer fallback are satisfied
When the marketplace user attempts generic fa2 transfer fallback
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover generic fa2 transfer fallback without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for generic fa2 transfer fallback are satisfied
When the administrator attempts generic fa2 transfer fallback
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Token transfer form

### Creator

As a Creator
I want to perform token transfer form with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for token transfer form are satisfied
When the creator attempts token transfer form
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of token transfer form before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for token transfer form are satisfied
When the collector attempts token transfer form
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how token transfer form affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for token transfer form are satisfied
When the curator attempts token transfer form
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from token transfer form without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for token transfer form are satisfied
When the marketplace user attempts token transfer form
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover token transfer form without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for token transfer form are satisfied
When the administrator attempts token transfer form
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Role/admin update forms

### Creator

As a Creator
I want to perform role/admin update forms with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for role/admin update forms are satisfied
When the creator attempts role/admin update forms
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of role/admin update forms before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for role/admin update forms are satisfied
When the collector attempts role/admin update forms
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how role/admin update forms affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for role/admin update forms are satisfied
When the curator attempts role/admin update forms
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from role/admin update forms without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for role/admin update forms are satisfied
When the marketplace user attempts role/admin update forms
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover role/admin update forms without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for role/admin update forms are satisfied
When the administrator attempts role/admin update forms
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## External publisher handoff

### Creator

As a Creator
I want to perform external publisher handoff with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for external publisher handoff are satisfied
When the creator attempts external publisher handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of external publisher handoff before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for external publisher handoff are satisfied
When the collector attempts external publisher handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how external publisher handoff affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for external publisher handoff are satisfied
When the curator attempts external publisher handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from external publisher handoff without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for external publisher handoff are satisfied
When the marketplace user attempts external publisher handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover external publisher handoff without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for external publisher handoff are satisfied
When the administrator attempts external publisher handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Post-operation state refresh

### Creator

As a Creator
I want to perform post-operation state refresh with clear progress, validation, and resumable output
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for post-operation state refresh are satisfied
When the creator attempts post-operation state refresh
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of post-operation state refresh before signing or purchasing
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for post-operation state refresh are satisfied
When the collector attempts post-operation state refresh
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how post-operation state refresh affects provenance, curation, and relationship metadata
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for post-operation state refresh are satisfied
When the curator attempts post-operation state refresh
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from post-operation state refresh without hidden quantity, price, or contract ambiguity
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for post-operation state refresh are satisfied
When the marketplace user attempts post-operation state refresh
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover post-operation state refresh without bypassing app roles or chain safety
So that Colander can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for post-operation state refresh are satisfied
When the administrator attempts post-operation state refresh
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.
