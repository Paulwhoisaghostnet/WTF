# WTF Platform User Interaction Inventory

Last reviewed: 2026-07-23

This inventory is the interaction contract for WTF end-to-end coverage,
EXP/reward triggers, activity monitoring, abuse review, and cheat detection. It
lists every current user-facing, staff-facing, bot-facing, and paired-agent
interaction surface found in the app route map, server route registration,
schemas, public-access docs, and feature modules.

Use this as the seed for:

- E2E test suites by issue of concern, domain, and sub-domain.
- Canonical event names for the EXP/reward/challenge/side-quest/arcade systems.
- Monitoring hooks for 100% interaction observability, warnings, alarms,
  punishments, and manual review queues.

## Source Basis

- Browser route map: `client/src/routes/page-defs.ts`.
- Server route registration: `server/routes.ts`.
- Public/API/MCP contract: `docs/public-access.md`.
- Reward and monitoring schemas: `shared/schema-admin.ts`,
  `shared/schema-gameshow.ts`, `shared/schema-liveops.ts`,
  `shared/schema-console.ts`, `shared/schema-desktop.ts`,
  `shared/schema-market.ts`, `shared/schema-wallet.ts`,
  `shared/schema-casino.ts`, `shared/schema-club-dues.ts`, and
  `shared/schema-game-studio.ts`, `shared/schema-telegram.ts`, and
  `shared/schema-ipfs-pinning.ts`, and `shared/schema-green-room.ts`.
- Challenge automation foundation: `shared/schema-challenge-automation.ts`,
  `server/challenges`, and `client/src/features/admin/challenges`.
- WTF OS admin surface coverage: `client/src/features/admin-os/admin-surface-registry.ts`
  and the native `AppWindow` admin panel.
- Inventory-driven E2E coverage: `tests/e2e/inventory`, `tests/playwright/inventory`,
  and `npm run test:e2e:inventory:coverage`.
- Comms mesh schemas: `shared/schema-comms.ts` and `shared/schema-mail.ts`.
- Current feature routes/modules under `server/routes`, `server/features`,
  `client/src/pages`, and `client/src/features`.

## E2E Coverage Contract

The inventory is executable through the modular E2E scheme:

- Subdomain ownership tests parse this file and create one Playwright coverage
  test for each inventory row and every canonical handle it owns.
- Route surface tests cover every concrete `PAGE_DEFS` route through
  `tests/e2e/inventory/route-fixtures.mjs`.
- Domain interoperability tests live in
  `tests/e2e/inventory/domain-workflows.mjs` and cover cross-subdomain
  workflows such as gameshow rewards, social-to-XP automation, commerce,
  media-to-Arcade publishing, TV programming, and admin operations.
- System integration tests verify strict-admin visibility, native app admin
  tooling, central challenge automation access, and normalized event-shape
  coverage for every handle in this inventory.
- Feature-depth accounting lives in
  `tests/e2e/inventory/coverage-layers.mjs` and
  `tests/playwright/inventory/feature-depth.spec.mjs`. It intentionally
  separates complete E2E skeleton coverage from full feature-behavior coverage.
  Named behavior assertions live in `tests/e2e/inventory/behavior-assertions.mjs`
  and app-owned behavior coverage is registered on the owning admin/app surface
  via `behaviorAssertionIds` in
  `client/src/features/admin-os/admin-surface-registry.ts`.
- Actor-backed live orchestration lives in `tests/e2e/puppets`,
  `playwright.live.config.mjs`, and
  `tests/playwright/live/puppet-orchestration.spec.mjs`. It seeds 12 local
  puppet users with strong ignored credentials and platform-keyring-backed
  wallets, then password-logs them in, verifies wallet challenges, route-smokes
  the registered route fixtures, and runs domain workflows against a real local
  server/database. External OAuth providers remain intentionally excluded from
  this live harness.

When a feature adds or changes a route, interaction, API handle, admin surface,
reward trigger, challenge trigger, side quest verifier, bot/agent tool,
telemetry event, or normalized `SystemEvent`, the same change must update this
inventory and the appropriate fixture under `tests/e2e/inventory/`. The coverage
gate is `npm run test:e2e:inventory:coverage`; UI/interaction changes should
also run `npm run test:e2e:inventory`. Changes that touch auth, wallet binding,
roles, rewards, admin controls, durable persistence, or cross-domain workflows
should also update or run `npm run test:e2e:live:puppets` when practical.

Do not claim full feature behavior coverage from the skeleton alone. A feature
counts as behavior-covered only when an app-owned test asserts both the
user-visible result and the durable side effect, such as a saved record, emitted
event, reward grant, permission change, wallet preflight, queue mutation, or
chain-backed verification. Mutating an app behavior assertion requires updating
both the assertion's `ownerSurfaceIds` and the owning surface's
`behaviorAssertionIds`; the coverage gate fails if either side drifts.

## Access Legend

| Access | Meaning |
| --- | --- |
| Public | No signed-in session required. |
| Session | Signed-in browser session required. |
| Time out | Signed-in account can authenticate and log out, but all app/window launch surfaces are blocked while the `time_out` role is assigned. |
| Role access | Signed-in account has one or more assigned roles from the canonical role-membership table; access is the union of role permissions and registered WTF OS surface grants. |
| Cobwebsaints full user | Non-assignable custom role reserved for cobwebsaints; grants full non-admin user, creator, social, market, pinning, and default non-admin WTF OS surface access while denying moderation/admin authority and admin panel visibility. |
| Owner/creator | Signed-in user must own the wallet, media item, channel, project, grant, listing, or record. |
| Trusted creator | Signed-in user has a trusted creator role or one of `trusted_arcade_creator`, `trusted_console_creator`, `trusted_tv_creator`, or `trusted_market_creator`. |
| Admin | Strict `admin` role only. Admin screens and native OS admin panels should not be visible to host/cohost unless a separate non-admin staff screen is explicitly created. |
| Staff | Admin, host, cohost, resident wizard, or a role with the required permission. |
| Bot | Discord bot, webhook, HMAC, or server-to-server credential. |
| Agent | Paired MCP token acting only for the approved user. |
| System | Worker, scheduled import, indexer, telemetry sink, or health monitor. |

## Route Surface Matrix

| Domain | Routes and entry points |
| --- | --- |
| Public entry | `/`, `/gamma`, `/beta`, `/login`, `/register`, `/leaderboard`, `/gallery`, `/gallery/token/:contract/:tokenId`, `/token/:contract/:tokenId`, `/links`, `/faq`, `/user/:username`, `/messageboard`, `/wtf-recapture`, `/calendar`, `/arcade`, `/dues`, `/discord/terms`, `/discord/privacy`, `/discord/linked-roles`, `/embed/tv/:ref`, `/oembed`. |
| Gameshow | `/dashboard`, `/dedrooms`, `/rounds`, `/rounds/:id`, `/challenges`, `/side-quests`, `/mint-portal`, `/calendar`, `/wtf-recapture`, `/control-board`. |
| Social | `/messages`, `/messages/dms/:id`, `/mail`, `/admin-inbox`, `/digest`, `/skywire`, `/live`, `/live/r/:roomId`, `/tz2at`, `/crp-nominate`, `/wim`, `/dear-diary`, `/messageboard`, `/w`, `/w/post/:id`, `/w/chat`, `/w/groupchat/:id`, `/chat`, `/chat/:id`, `/dicksword`, `/i-hate-telegram`, `/profile`, `/wtf-subdomains`, `/wtf-subdomains/setup`, `/user/:username`. |
| Desktop OS | Desktop shell, controlled default desktop app set, Start Menu, taskbar, desktop icons, user-created app shortcuts, command palette, `/mission-control`, `/command-palette`, `/recovery-mode`, `/file-manager`, `/settings`, `/browser`, `/browser-boundaries`, `/applications`, `/applications/:appId/play`, `/agent`, `/terminal`, `/cli`, `/theme-builder`, `/notification-center`, `/desktop-settings`, `/task-manager`, `/map-lab`, desktop pet tray, desktop artifact layer, screen saver, custom cursor, MCP pairing. |
| Wallet/portfolio | `/dashboard`, `/profile`, `/my-gallery`, `/collekt`, `/tezos-intel`, `/wtf-subdomains`, `/wtf-subdomains/setup`, `/swap`, `/marketplace`, `/rat-race`, `/trade-boards`, `/dues`, `/operator-wallet`, `/payroll`, `/contract-factory`. |
| Media/creation | `/music`, `/my-videos`, `/my-photos`, `/my-music`, `/tezamp`, `/tezamp/winamp-bootloader`, `/ipfs-pinning`, `/apps/porcupin-setup`, `/apps/porcupin-dashboard`, `/studio`, `/studio/:id`, `/game-studio`, `/tools/broot`, `/tools/pixalerce`, `/tools/particle-painter`, `/tools/industrializer`, `/tools/pauls-particles-v1`, `/tools/nikshumika-paint`, `/tools/kandinsky-composer`, `/tools/pixel-patterns`, `/tools/penrose-backgrounds`, `/tools/ch-ease`, `/tools/macaroni-packager`, `/tools/colander`, `/tools/spaghetti`, `/tools/gnocchi`, `/tools/ravioli`, `/tools/rotini`, `/tools/penne`, `/tools/lasagna`, `/tools/macaroni`. |
| TV | `/tv`, `/embed/tv/:ref`, `/oembed`, public TV channel/current/stream/media/cache routes, session creator channel routes, staff canonical TV config routes. |
| Commerce | `/wtfiam` including the ranked Apps app-store category, `/marketplace`, `/rat-race`, `/trade-boards`, `/dashboard`, `/swap`, `/wtf-recapture`, `/dues`, `/operator-wallet`, `/payroll`, `/contract-factory`. |
| Arcade/console | `/arcade`, `/console`, `/game-studio`, public Arcade/Console catalogs, SDK, score/session routes, Game Studio project/build/submit routes, staff Arcade moderation/audit routes. |
| Casino | `/casino`, `/casino/wtf-button`, `/casino/rug-pull`, `/casino/guinea-pig-raceway`, WTF Casino desktop icon, Start Menu Gaming category, casino in-app-market category, Casino membership contract, membership verification APIs, built-in table registry, creator practice-table builder/status desk, operator practice moderation, durable no-value play results, mocked WTF Button jackpot tables, mocked Rug Pull pressure table, mocked Guinea Pig Raceway 3D race table, and fail-closed future wager-session routes. |
| Admin/ops | Strict-admin `/admin` control suite, `/control-board`, `/backup-manager`, `/contract-factory`, `/operator-wallet`, `/payroll`, `/dev/ux-lab`, OS Admin surface registry, native window admin/settings panels, public liveness `GET /api/health`, public readiness `GET /api/health/ready`, authenticated diagnostics `GET /api/health/diagnostics`, authenticated disk diagnostics `GET /api/health/disk`, runtime metrics endpoint (`GET /api/metrics`, admin- or `WTF_METRICS_TOKEN`-gated: event-loop lag, CPU, DB pool, per-route latency, WebSocket counts), system logs, desktop app gates, desktop app doc registry/install keys, role permissions, role surface visibility, reward ledger, challenge automation, user management, Studio storage, media storage, backup restore proof. |
| Agents/automation | `/mcp`, `/api/access`, `/api/mcp/tokens`, Discord bot routes, Telegram digest bot routes, attendance bot routes, board webhooks, client logs, scheduled Arcade source import, wallet/indexing workers. |

## Concern: Entry, Authentication, and Account Identity

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Public entry | Public | View landing/login/register; open public leaderboards, gallery, links, FAQ, public profiles, message board, Arcade, TV embeds, calendar, Discord legal pages. | `public.route.viewed`, `public.link.opened` |
| FAQ registration video guides | Public | Open `/faq`; preview the short sign-in, tool navigation, Tezos wallet, Etherlink wallet, X, Discord, Skywire/Bluesky, and Google Drive walkthroughs recorded with the TommyTezos account; play or complete a guide; enable English captions; read the matching transcript and steps. | `faq.tutorial.selected`, `faq.tutorial.played`, `faq.tutorial.completed` |
| FAQ wtfOS promos | Public | Open `/faq`; preview the TommyTezos promo collection for Play, Create, Shop, Events, Talk, and the connected wtfOS overview; select, play, or complete a promo; enable English captions; read its exact first-person transcript. | `faq.promo.selected`, `faq.promo.played`, `faq.promo.completed` |
| Gamma auth return handoff | Public/session by linked route | Open a protected `/gamma/...` route or `gamma.wtfos.app/...` route as a guest; see a Gamma-owned auth-required gate with a focused Enter and return action; press Enter to open the shared Login screen hosted inside Gamma with the Username field focused, or choose registration/wallet login through the same hosted auth surface; preserve a sanitized local return target after password, wallet-login, or wallet-registration success; land back on the attempted Gamma route instead of Classic or the Gamma home page; when Gamma Login/Register opens without a protected-route return, use `/dashboard` as the default post-auth route so direct Gamma auth behaves like logging into an OS session; keep Classic auth landing behavior unchanged. | `auth.login.succeeded`, `auth.register.succeeded`, `auth.wallet.verify.succeeded`, `public.route.viewed` |
| Gamma command search launcher | Public/session/admin by linked route | Type a known app, creation tool, system surface, communication route, Count surface, or static WTFOS route into the Gamma start-desk search or route-toolbar search; inspect command results built from existing Gamma stations, app catalog entries, and registered static route definitions; launch the selected command inside `/gamma/...` while shared auth, role, app-pass, and permission gates still apply; when no route matches, use the explicit Gallery search fallback instead of creating a new search API or assistant dependency; keep Classic and Beta search behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma command keyboard shortcut | Public/session/admin by linked route | Press Ctrl/Cmd+K while the Gamma home desk or a Gamma app route is focused; move focus to the mounted Gamma command input without opening a modal, assistant, or Classic surface; type a known app, tool, or route and launch it through the same Gamma command search path while shared auth, role, app-pass, and route gates still apply; keep Classic and Beta keyboard behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma home enter-to-continue | Public/session by linked route | Land on `/gamma` with the visible primary Continue/Login boot action focused; press Enter or Space on that focused action, or press Enter while the idle Gamma home desk owns focus and no input, link, button, or menu item owns the event; launch the same visible primary Continue/Login action as the start menu; send guests to shared Login hosted at `/gamma/login?return=/dashboard` and then into `/gamma/dashboard` after shared authentication succeeds; send signed-in users to their latest sanitized Continue route or `/gamma/dashboard`; keep Classic and Beta keyboard behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma command result keyboard navigation | Public/session/admin by linked route | Type a Gamma command query, press ArrowDown to move focus from the search input into the visible command results, press ArrowUp or ArrowDown to cycle result buttons, press Escape from a focused result to return to the command input with the query intact, and press Enter on the focused known-route or Gallery fallback result; keep focus movement inside the Gamma command surface, preserve `/gamma/...` routing, and leave Classic and Beta command behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma command escape dismissal | Public/session/admin by linked route | Press Escape while a Gamma start-desk or app-route command input is focused after typing a query; clear the query, dismiss input focus, and leave the current `/gamma` or `/gamma/...` route unchanged without submitting search, opening an assistant/modal, loading Classic, or changing Beta keyboard behavior. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma app-route keyboard desk shortcut | Public/session/admin by linked route | Press Alt+Home while focused on a Gamma app route outside editable fields; return to the Gamma desk at `/gamma` without using pointer navigation, loading Classic, or bypassing shared route/auth/app gates; keep input, textarea, select, and contenteditable targets under the app's normal keyboard ownership; keep Classic and Beta keyboard behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma route history recovery | Public/session/admin by linked route | Navigate between registered routes inside Gamma; use the shell-owned Back, Forward, and Desk controls or Alt+ArrowLeft / Alt+ArrowRight outside editable fields to move through a bounded sanitized Gamma route stack; keep unavailable directions disabled; reject API, unregistered, and external paths; preserve `/gamma/...` routing and shared auth, role, app-pass, and permission gates without depending on browser chrome or loading Classic; keep Classic and Beta history behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma recent route restore | Public/session/admin by linked route | Open registered routes inside Gamma; let the Gamma shell remember only sanitized browser-local route strings for recently opened non-auth/non-home routes; return to `/gamma` and see the newest route promoted into the Continue action plus open-app dock buttons in the session console; click a recent route or use the home dock keyboard app-switch shortcut outside editable fields to resume the front recent route inside `/gamma/...`; keep the normal auth, role, app-pass, and route permission gates intact; fall back to Daily, People, and Gallery pinned starts for a fresh browser session; keep Classic and Beta session behavior unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma recent app task switcher | Public/session/admin by linked route | Open multiple registered routes inside Gamma; while on a Gamma app route, use the app taskbar's Switch region or keyboard app-switch shortcut to jump directly to other recent Gamma routes without returning to `/gamma`; exclude the currently active route from switch buttons; ignore keyboard switching while text inputs or editable fields have focus; fall back to route-backed Daily, People, and Gallery switches when no browser-local recents exist; keep all shared auth, role, app-pass, and route permission gates intact; keep Classic and Beta taskbars unchanged. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma session lock control | Session by linked route | Sign in to Gamma; choose Lock from the persistent session controls or press Ctrl+Alt+L outside editable fields; return to the Gamma desk at `/gamma` while the current account remains signed in, mounted, and able to resume routes; do not call logout, clear auth, bypass route gates, or affect Classic/Beta session controls. | `public.route.viewed`, `app.interaction.tracked` |
| Gamma system clock calendar handoff | Public/session by linked route | Open `/gamma` or any Gamma app route; read the browser-local time and date from the persistent Gamma system tray; activate the clock control to open the existing Calendar app at `/gamma/calendar` while preserving shared Calendar APIs, local event storage, auth gates, route gates, and Gamma shell chrome; do not add a Gamma time API, mutate calendar data, or affect Classic/Beta presentation behavior. | `public.route.viewed`, `calendar.viewed`, `app.interaction.tracked` |
| Gamma Inbox and Notification Center shell | Session by linked route | Open `/gamma/messages`, `/gamma/notifications`, `/gamma/notification-center`, or `gamma.wtfos.app/messages`; render direct-message conversation lists, message threads, notification preference rows, notification filters, and notification rows inside Gamma-owned communication chrome; preserve shared DM and notification APIs, event logging, auth, roles, and read/preference mutations; click supported Studio notifications or Studio-room buttons and remain on the Gamma route prefix for `/studio/:projectId` rather than opening the Classic desktop window manager; keep Classic and Beta presentation behavior unchanged. | `dm.conversation.created`, `dm.message.sent`, `notification_center.viewed`, `notification_center.notification_opened`, `notification_center.mark_read`, `notification_center.mark_all_read`, `notification_center.preferences_saved`, `notification.viewed`, `notification.opened`, `notification.read`, `notification.read_all`, `notification.preference.updated` |
| Gamma tray notification signals | Session by linked route | Sign in to Gamma; let the persistent system tray read the existing notification unread count; see the Signals tray action report guest, checking, clear, unread, or degraded notification state with an unread count when available; open `/gamma/notifications` from the same control while preserving shared notification APIs, read/preference behavior, auth, and route gates; keep Classic and Beta tray behavior unchanged. | `notification_center.viewed`, `notification.viewed`, `app.interaction.tracked` |
| Gamma Casino lobby shell | Session by linked route | Open `/gamma/casino` or `gamma.wtfos.app/casino`; render app-pass status, membership-card status, configured contract, installed table cards, and entry controls inside Gamma-owned casino lobby chrome; preserve shared Casino status/game/membership/entry APIs, wallet membership purchase flow, app-pass dependency, and fail-closed wagering policy; click supported table routes or the app-pass market route and remain on the Gamma route prefix for `/casino/:table` or `/wtfiam` rather than opening the Classic desktop window manager; keep Classic and Beta presentation behavior unchanged. | `casino.access.viewed`, `casino.entry.granted`, `casino.entry.rejected`, `casino.games.viewed`, `casino.membership.intent_created`, `casino.membership.verified`, `casino.wager_session.rejected`, `wtfiam.viewed` |
| Gamma Casino table shell | Session by linked route | Open `/gamma/casino/wtf-button`, `/gamma/casino/rug-pull`, `/gamma/casino/guinea-pig-raceway`, or the matching `gamma.wtfos.app/casino/...` route; render WTF Button table controls, Rug Pull pressure controls, Raceway 3D scene, tote/race cards, wallet summaries, scoreboards, and timelines inside Gamma-owned table chrome; preserve shared Casino table state, quote, press, join, delay, witness, vote, bet, and effect APIs plus mocked-wager fail-closed behavior; use table return controls to stay on the Gamma route prefix for `/casino` rather than opening the Classic desktop window manager; keep Classic and Beta presentation behavior unchanged. | `wtf_button.lobby.viewed`, `wtf_button.table.viewed`, `wtf_button.quote.created`, `wtf_button.press.succeeded`, `wtf_button.press.rejected`, `rug_pull.rules.viewed`, `rug_pull.round.join_intent_created`, `rug_pull.button.press_intent_created`, `rug_pull.witness.vote_cast`, `rug_pull.wager.rejected`, `guinea_pig_raceway.rules.viewed`, `guinea_pig_raceway.race_card.viewed`, `guinea_pig_raceway.tote.viewed`, `guinea_pig_raceway.bet_intent_created`, `guinea_pig_raceway.effect_intent_created`, `guinea_pig_raceway.wager.rejected` |
| Gamma arcade OS shell | Public/session/admin by linked route | Open `/gamma` or `gamma.wtfos.app/`; land on a Tezos-dark arcade OS floor that uses existing public/session/admin routes as launch targets; use the Gamma boot desk for a route-backed account tile, boot search, a compact checklist for App access, Daily loop, and People visible route-backed checks, plus Continue/Login, Gallery, Apps, Inbox, and Settings start controls before reading deeper route clusters; after login, use the Gamma session console as the OS home row for profile identity, a mounted-workspace table for account/profile, restore target, app-pass state, and active Gamma shell, Home `/dashboard`, Inbox `/messages`, Apps `/wtfiam?category=apps`, Settings `/settings`, Daily proof `/side-quests`, People `/w`, and Objects `/gallery` without leaving `/gamma/...`; use the Gamma wake queue to see the next launch order from existing session state: Resume/Login, Inbox, Daily, People, and Apps, with Resume pointing to the newest sanitized recent route or Dashboard and every row preserving Gamma routing; use the persistent Gamma system tray on home and app routes to see session, shared API health (checking/online/degraded), Signals, Daily, Apps, and People status, then use session controls for Desk, Settings, and Sign out/Login, jump to Profile/Login, Settings, Notifications, Side Quests, WTFIAM Apps, or W while preserving `/gamma/...`, including degraded shared API recovery through Settings; use the Gamma daily return strip to open Side Quests, Challenges, W people discovery, or Notifications as the obvious reason to return after login, then follow a daily Side Quest task into Message Board, claim a verified daily proof, and continue to WTFIAM market unlocks while staying on `/gamma/...`; inspect the Gamma Access Passport for current role, EXP tier, app-store unlock count, Side Quests, Challenges, Apps, and Leaderboard route-backed next actions; when signed in as an admin, inspect the Count admin lane for user triage, loop audit, and market/app-pass route handoffs while EXP remains review evidence rather than permission authority; open any linked application route inside Gamma-owned chrome through the hostname-preserving Gamma workspace instead of falling back to the default desktop; when an app-store route is not installed, use Gamma's locked-app gate to open WTFIAM Apps inside Gamma instead of dead-ending; use local `/gamma/:route` as the inventory harness mirror for production `gamma.wtfos.app/:route`; keep the Gamma presentation shell for same-session canonical route changes such as `/gallery` after a Gamma entry; use Gamma breadcrumbs, search, Enter-to-continue on the idle home desk, Ctrl/Cmd+K command focus, Arrow-key command result focus, Escape command result-to-input recovery, Escape command dismissal, Alt+Home return-to-desk from app routes, persistent system tray, Start side rail, route app taskbar, profile, Apps, notifications, settings, permission gates, shared app content region, and Classic/Beta/Gamma UX switcher while preserving the selected route; use the app taskbar on Gamma app routes to see and focus the active app strip when real hosted app content opens, close the current app route back to the Gamma desk, jump to Desk/Login, Inbox, Daily, Apps, or Settings, and switch directly to recent registered Gamma routes by keyboard or pointer with mobile-sized controls; render shared app primitives such as windows, group boxes, buttons, text inputs, tabs, and tables through Gamma presentation adapters while Classic/Beta continue to delegate to the React95 implementation; host Dashboard cockpit/first-screen next-action rail/passport/portfolio/activity/discovery chrome, creation-tool iframe shell/header/provenance chrome for Broot and sibling vendored tools while leaving iframe apps raw, WIM's app-owned custom windows and popups plus marketplace listing/titlebar chrome, WTFIAM economy tabs/item-card/cart/tip-ledger/app-store chrome, Profile identity/avatar/wallet/social chrome, Skywire social bridge/feed/live-status chrome, tz2at identity/market-analytics chrome, Rat Race urgency card/diagnostic chrome, My Videos/Photos/Music library/upload/channel/player chrome, Side Quests/Challenges progression cards/reward-account/submission chrome, Rounds/Round Detail season launch board/round info/challenge handoff chrome, The Count Admin Control Suite nav/overview/role/market/automation chrome, shared token detail modals, media delete confirmation dialogs, WTF LIVE creation dialogs, WTF LIVE public-room popout windows, Message Board custom channel chrome/settings dialogs, Dicksword custom Discord identity/avatar/admin chrome, W digest shell/timeline card chrome, WTF TV playback cabinet/menu/metadata chrome, and WTF LIVE room/show-kit/tip route handoffs with Gamma chrome when launched from Gamma; click registered internal links and app-owned route buttons emitted by shared app content, including Profile/Skywire/WTF LIVE/Recovery/Dicksword/Skywire-pipeline/Side Quest task/Rounds launch-board handoffs, and stay inside Gamma routing while API/OAuth starts and explicit interface-switch links remain allowed exits; inspect visible peer signals from existing EXP leaderboard reads or quiet fallback people labels; open creation/publishing stations for Studio, Broot, Macaroni, and IPFS through their current gates; open Gallery, Arcade, Market, Leaderboard, and Apps discovery stations; open W, WIM, WTF LIVE, Message Board, Dicksword, TV, and Skywire communication stations as first-class parts of the floor; inspect buried XP/role/sidequest/challenge signal lights without creating a new progression engine; open The Count booth links for Sidequests, Challenges, and Admin through existing permissions only. | `public.route.viewed`, `leaderboard.viewed`, `app.interaction.tracked` |
| Beta discovery hub | Public/session/admin by linked route | Open `/beta` or `beta.wtfos.app/`; answer the first-minute WTFOS questions; use the First-Minute Wayfinder to jump from safe-first-win, people-now, object-hunt, creator-runway, builder-output, choose-path, find-tool, or Count-review questions into existing beta sections, selected puppet paths, route-owned destinations, and atlas filters without mutating app state; inspect read-only now signals from existing public routes including leaderboard, rewards, market listings, public trade-board cache, public XP activity, WTF LIVE room presence, upcoming calendar events, WTF TV channels, arcade play, and console discovery; inspect the public proof board that turns existing object, creator-channel, play, and builder-output reads into live-or-quiet snippets before routing to Gallery, TV, Arcade, or Console; inspect the Attention Triage Board that turns public, protected, and admin-gated signals into seven route-owned next actions for first safe action, people moving now, collector heat, creator recovery, play/builder output, tomorrow catch-up, and Count hot queues without creating notifications or bypassing gates; inspect the Daily Return Board that turns changes, quests, people, objects, projects, and Count/admin queues into today/tomorrow actions while routing only to existing WTFOS surfaces with their current permissions; inspect the Unlock Passport board that gives each user puppet plus The Count a compact visible-now, next-safe-action, proof-needed, unlocks-next, stays-locked, Count-review, and return-tomorrow card backed only by existing routes; inspect the Unlock Questline Board that explains how new-user, collector, creator, builder, curator, community, and Count/admin paths use existing side quests, challenges, EXP, rewards, role/permission gates, admin review, and abuse guards before routing stage buttons to existing WTFOS surfaces; inspect the Unlock Governance Matrix that maps each puppet path to existing evidence, EXP signal, reward or market sink, role boundary, Count decision, anti-farm guard, and user/admin routes without creating a new progression engine; inspect the App Relationship Navigator that maps first-win, collector, creator, builder, curator, community, economy, and Count liveops chains with comes-before, consumes, feeds-into, comes-after, Count-watch, and existing route-step buttons without changing app behavior; choose a role-based discovery trail for collector, creator, builder, community, or The Count admin workflows; inspect per-trail live proof snippets that connect current signals to the path where they matter; inspect trail-level state readouts that distinguish quiet data, protected data, unavailable providers, and admin-only data before the user opens the next route; read protected-step microcopy before non-public trail routes explain what sign-in, role, or admin gates protect; inspect the communication map for W Feed, WIM, WTF LIVE, Digest, Mail, and Skywire before/after context; inspect Social Pulse notification groups for social attention, progress and unlocks, live/scheduled moments, creator recovery, collector/market motion, and Count admin attention; inspect the notification control map that points each loop to its owning action route, System Settings `/settings`, Digest `/digest`, and `/api/notifications/preferences` without changing notification logic; inspect puppet retest snapshots that compare production-baseline timings to beta timings for all six persistent agents and all six required discovery checkpoints; choose a puppet path; inspect the Journey Command Center that stitches the selected puppet's proof route, first action, proving route, return route, and strict-admin Count review into five existing route-owned steps; inspect the persistent agent loop, production scan summary, visibility radar, route bridges, EXP/level/role unlock ladder; filter the App Visibility Atlas by search, visibility tier, user stage, and puppet path before opening existing side quests, challenges, leaderboards, live, calendar, TV, creator, collector, community, market, public profile, communication, and admin routes without bypassing their auth, role, app-gate, wallet, reward, or admin permissions; inspect the Count Admin Workbench that maps user triage, sidequest creation, challenge creation, reward configuration, role/app-gate review, market sink management, automation audit, and visibility/communication review to existing admin routes, player routes, evidence, checklists, decision gates, risk controls, and success signals without writing admin state; use The Count admin puppet stories, strict-admin summary counts, and Count liveops command deck to manage user needs, role gates, quest/challenge load, reward settlement, market operations, automation definitions, first quest triage, challenge arcs, role and permission review, reward economy tuning, in-app market availability, and abuse-loop audits through existing gated admin and owner surfaces. | `public.route.viewed`, `side_quest.viewed`, `challenge.viewed`, `leaderboard.viewed`, `app.interaction.tracked` |
| Beta product home | Public/session/admin by linked route | Open `/beta` or `beta.wtfos.app/` and land on the live OS cockpit instead of a report page; inspect the sticky shell bar for selected path, actual authority, live signal count, Gallery command on wider chrome, and signed-in/guest Quest command; choose one of six persona paths from the dark active-window mission deck without granting roles; use the compact OS directive, session HUD, active job window, You/Can/Next command readout, people-moving/public-object/check-changed command pulse, human social-reason line, dominant signed-in Launch quest or guest concrete-object inspection command, and visually marked quest stage before reading deeper evidence; on mobile, the compressed active job appears before the system-monitor rail and keeps current ability, people proof, and the public-object launch action readable without requiring a long report scroll; inspect the selected EXP level/passport/questline card on wider layouts and open quest-stage route buttons where visible; continue into the full-width live desktop band with a compact launcher dock for Quest, People, Gallery, Studio, Changes, and Admin, one selected quest panel, proof/return state, EXP progress, people-present pulse, and quest/people/discovery/tomorrow/Count lanes; read the compact OS boot readout for OS/run/first/next/return state instead of a FAQ-style answer strip; explicitly open the collapsible Admin Lab/Research Deck only when route maps, puppet evidence, app atlas, Count runbooks, or critic-loop status are needed; every button opens an existing route through its current public, session, role, or admin gate and the lab toggle changes only beta-local view state. | `public.route.viewed`, `app.interaction.tracked` |
| Beta route console shell | Public/session/admin by linked route | Open `/beta/:route`, same-session canonical routes such as `/gallery` after entering Beta, or direct production `beta.wtfos.app/:route`; keep every registered shared WTFOS route inside the Beta dark console shell instead of the Classic desktop; preserve auth, role, app-gate, API, wallet, reward, admin, and route-owner behavior; use breadcrumbs, route search, grouped rail navigation, signed-in/profile/notification/settings tools, and Classic/Beta/Gamma switcher links that preserve the selected route while explicit API, download, OAuth, and interface-switch exits stay raw; render shared React95 app primitives and route-owner `data-*-presentation-host="beta"` chrome through the Beta presentation adapter with near-black surfaces, white headings, gray body text, mint active/status states, amber progress, and magenta current-route emphasis while route errors remain contained inside the Beta shell instead of escaping to Classic UI. | `public.route.viewed`, `app.interaction.tracked` |
| Beta section compass | Public/session/admin by linked section | Inspect the `/beta` Section Compass after the First-Minute Wayfinder; review fourteen major beta boards by question, audience, stage, access boundary, use-when guidance, proof rationale, and next move; jump to existing beta anchors for now signals, public proof, people discovery, attention triage, daily return, passports, questlines, governance, relationships, route groups, trails, puppet paths, Count runbook, or App Visibility Atlas using beta-local hash and scroll state only. | `public.route.viewed`, `app.interaction.tracked` |
| Beta creator project proof ladder | Public/session/role by linked route | Inspect the `/beta` Creator Project Proof Ladder inside the Public Proof section; review Studio draft, Broot asset-prep, Macaroni package, IPFS durable media, WTF TV media channel, Console project output, and Skywire broadcast rows by visible/inspect/gated status, owner surface, current limit, next dependency, gate boundary, and no-write boundary before opening the owner route through its current public, session, or role gate. | `public.route.viewed`, `app.interaction.tracked` |
| Beta people discovery board | Public/session by linked route | Inspect the `/beta` People Discovery Board that groups existing now signals into active users, new users, creators, collectors, builders, curators, collaborators, and interesting wallets; review each role's proof signals, why-care copy, next action, quiet fallback, and related existing routes before opening the route through its current access gate. | `public.route.viewed`, `app.interaction.tracked` |
| Beta people proof gap matrix | Public/session by linked route | Inspect the `/beta` People Proof Gap Matrix inside the People Discovery section; review active-user, newcomer, creator, collector, builder, curator, collaborator, and interesting-wallet proof rows by direct/routed/weak status, existing proof signals, current weakness, next beta UI move, quiet fallback, and no-write boundary before opening the proof route through its current gate. | `public.route.viewed`, `app.interaction.tracked` |
| Beta route group guide | Public/session/admin by linked route | Inspect the `/beta` Route Group Guide that explains overlapping route clusters for first wins, collector economy, creator pipeline, builder output, curator signal, community communications, and Count liveops; review use-first, use-next, proof, quiet-rule, and Count-watch copy; open each existing route through its current gate or apply the group's atlas filters without mutating app state. | `public.route.viewed`, `app.interaction.tracked` |
| Beta Count liveops recipe board | Public/session/admin by linked route | Inspect the `/beta` Count Liveops Recipe Board that turns starter witness, creator publish, collector market, builder surface, curator signal, and community return needs into non-writing admin blueprints across existing EXP, side quest, challenge, reward, role/permission, market, notification, and app-gate surfaces; open each recipe stage through the route's current gate without creating quests, granting roles, settling rewards, changing market state, or sending notifications from beta. | `public.route.viewed`, `app.interaction.tracked` |
| Beta puppet memory ledger | Public/session/admin by linked route | Inspect the `/beta` Puppet Memory Ledger that renders six persistent puppet cards and 36 checkpoint pills for first task, success condition, understand/find people/find activity/find useful tool/perform task/discover next step checks, confusion, hesitation, dead-end note, abandonment risk, success, delight, unexpected discovery, remaining friction, next route, and saved-time evidence without writing user state or replacing existing navigation. | `public.route.viewed`, `app.interaction.tracked` |
| Beta friction queue | Public/session/admin by linked route | Inspect the `/beta` Beta Friction Queue after puppet retests; review seven evidence-backed next-iteration items for people proof, creator project proof, Count authority boundaries, route-name clusters, return-loop clarity, advanced app value, and assistant thresholds; jump to the owning beta board or open the existing owner route while preserving current auth, role, admin, reward, notification, wallet, and app-gate boundaries without writing beta state. | `public.route.viewed`, `app.interaction.tracked` |
| Local auth | Public/session/time out | Register; log in; land on the desktop instead of auto-opening Dashboard; allow `time_out` accounts to authenticate but block all app/window launch surfaces until their role changes; run the welcome event after verified credentials; show the one-time WTF OS welcome message for unwelcomed accounts; choose Play, Create, Shop, Events, or Talk from the shared first-run task map with unclipped icon/label controls; recover through Help & Start Here or Profile; keep the mobile Help task map in one readable column with each description contained by its button; show the UTC-day GM NFT welcome for accounts without today's GM flag; clear stale cached users when protected APIs return unauthenticated; log out; fetch current user; change or set password; handle failed or expired sessions. | `auth.register.succeeded`, `auth.register.failed`, `auth.login.succeeded`, `auth.login.failed`, `auth.welcome.event`, `auth.welcome.completed`, `auth.gm_welcome.event`, `auth.gm_welcome.completed`, `auth.session.invalidated`, `auth.logout`, `auth.password.changed` |
| Social auth | Public/session callback | Start and complete Google, GitHub, X/Twitter, and Discord OAuth; derive provider callback URLs from the canonical `wtfos.app` origin even when legacy platform aliases remain in env; view provider config; run X OAuth diagnostics/self-test. | `auth.oauth.started`, `auth.oauth.completed`, `auth.oauth.failed`, `auth.oauth.diagnostics.viewed` |
| Wallet auth | Public/session/wallet | Request a Tezos mainnet wallet challenge whose readable signed payload binds the exact wallet address, WTF OS origin, login/register/link action, expiry, and one-time nonce; clear stale legacy Beacon/WalletConnect auth state before explicit wallet login retries; subscribe to Octez Connect active-account events before requesting permission; use Octez Connect as the wallet transport with Taquito only as the Tezos operation/signing SDK layer; ignore Shadownet app-local preferences for primary account identity; require the submitted public key to derive the challenged wallet before atomically consuming the scoped nonce; verify the mainnet wallet signature; create wallet-backed accounts; sign in through wallet proof; show a bounded retryable wallet error instead of staying on Connecting forever. | `auth.wallet.challenge.created`, `auth.wallet.verify.succeeded`, `auth.wallet.verify.failed`, `auth.wallet.registered` |
| Profile identity | Session | Read the signed-in account summary through `/api/profile/account`; edit display name/bio/account fields; toggle email/social visibility; link/unlink verified X and Discord identities; when linking X, bind the intended handle from the Profile field into the OAuth session and block callbacks where X returns a different logged-in account; view linked Skywire/AT Protocol handle, open Skywire to connect or manage it, and manually disconnect the linked Skywire identity; view XP profile data. | `profile.updated`, `profile.social.linked`, `profile.social.unlinked`, `profile.social.link_blocked`, `profile.public_visibility.updated` |
| Profile picture | Session/owner | Browse positive-balance owned-token PFP candidates; search/page candidates; edit a held token on canvas and save the result through owner-scoped media; upload image media as the profile/game avatar; save or remove PFP. Arbitrary remote/data URLs and unowned token references are rejected at the server write boundary. | `profile.pfp.candidate_viewed`, `profile.pfp.edited`, `profile.pfp.saved`, `profile.avatar_media.saved`, `profile.pfp.removed` |
| Public profile | Public/session | View public profile, public XP activity, trade board, listings, public about/social fields, and linked Skywire/AT Protocol profile handle; start DM lookup when signed in; on Gamma, inspect the public-profile shell regions for About, social, wallets, trade board, listings, activity, and messages without falling back to the classic desktop. | `profile.public.viewed`, `profile.dm_lookup.opened` |
| Notifications | Session | View preferences; update toggles; list/filter inbox; open notification target; mark one/all read. | `notification.preference.updated`, `notification.viewed`, `notification.read`, `notification.read_all` |

## Concern: Desktop OS, Navigation, and Personal Environment

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Window shell | Public/session by route/time out blocked/admin app disabled | Open windows from routes/icons/registry-driven Start Menu sections; block every windowed app route for signed-in `time_out` accounts while still allowing logout; fail closed with an admin-disabled message when a disabled app route is reached directly or from a stale shortcut; persist and restore local window sessions; focus/cycle existing windows; use full-screen auth overlays; contain crashed apps inside their own window; close/minimize/focus windows; show desktop and restore the previous visible working set from the taskbar or keyboard. | `desktop.window.opened`, `desktop.window.focused`, `desktop.window.closed`, `desktop.window.minimized`, `desktop.window.restored`, `desktop.show_desktop.toggled`, `desktop.start_menu.opened`, `desktop.app.disabled_by_admin` |
| Mission Control | Session | View user-first status for current account, active wallet, claimable rewards, open challenges, unread notifications, failed jobs, system health, chain/RPC state, and next work; jump to the owning app without entering admin surfaces. When launched from Gamma, render status metrics, side-quest/reward/job/notification rows, and route-hub actions inside Gamma-owned Mission Control chrome so every action handoff stays on `/gamma/...` instead of falling into the Classic desktop. | `mission_control.viewed`, `mission_control.action_opened`, `wallet.balance.viewed`, `challenge.viewed`, `notification.viewed`, `cockpit.sync.status_viewed` |
| Command palette | Session/public route gates/admin app gates | Open `/command-palette`; search live route registry and sacred workflow aliases; open apps, rounds, rewards, wallet activity, media libraries, IPFS/archive preparation, project bundles, Studio projects, system checks, local-service status, recovery reports, backup/restore proof, and admin routes only when the signed-in role can access them and the owning app is enabled by admin. When launched from Gamma, render the status grid, search input, command rows, shortcut rows, and route-open controls inside Gamma-owned Command Palette chrome so command execution stays on `/gamma/...` instead of opening the Classic desktop route. | `command_palette.opened`, `command_palette.executed`, `desktop.window.opened` |
| File Manager | Session; wallet + user-owned Pinata account for media mint | Open `/file-manager` as the WTF dwelling map for Desktop, Projects, Media, Documents, Downloads, Vault, Apps, Chain, Archives, and Shared; view existing media/project counts and jump to each owning app without host filesystem access; browse the complete owned Media folder in newest-first order; and open Mint Manager for any ready uploaded file without downloading and re-uploading it. Choose the HEN/Teia shared contract, an Objkt-ready Pasta standard collection, a compatible wallet/Colander-associated contract, or a new Pasta standard/open-edition contract; complete destination-owned user steps; and verify the resulting linked-wallet operation plus indexed token transfer through `/api/mint-manager/receipt`. When launched from Gamma, render dwelling rows, bundle rows, media-service rows, IPFS rows, and route-open controls inside Gamma-owned utility chrome so every dwelling/bundle/service handoff stays on `/gamma/...`. | `file_manager.viewed`, `file_manager.opened`, `media.mint_manager.opened`, `media.mint_manager.destination_selected`, `media.mint_manager.media_pinned`, `media.mint_manager.publisher_handoff_opened`, `media.mint_manager.receipt_verified`, `desktop.window.opened`, `blockchain.tezos.token_mint` |
| System Settings | Session/admin route gates | Open `/settings` as the central OS settings hub; choose a Language & Region display locale that persists through the desktop settings API using its optimistic concurrency token (including null for compare-and-insert), updates document `lang`/`dir`, and changes system-owned OS shell text without translating user-authored posts, chats, names, token metadata, media titles, or generated secrets; jump to account, appearance, notifications, files, wallet, WTF Domains, W, recovery, and admin-only settings without bypassing each owner app's permission, CSRF, wallet, or admin gates; when launched from Gamma, render the status grid, settings cards, interface selector, language panel, boundary panel, and owner-route handoffs inside Gamma-owned settings chrome so `/gamma/settings` routes to `/gamma/...` owner surfaces instead of the Classic desktop; open the focused `/wtf-subdomains/setup` Subdomain Setup window to claim the signed-in user's `username.wtfos.me` publishing host, inspect Macaroni readiness, connect a Tezos wallet, view/build `label.wtf.tez` registrar commit/register plans, and hand off deeper editing to the WTF Domains owner surface. | `system_settings.viewed`, `system_settings.opened`, `system_settings.language_changed`, `desktop.window.opened`, `wtf_site.claimed`, `wtf_domain.registration.prepared` |
| Browser Boundaries | Session | Open `/browser-boundaries`; inspect the standard access manifest, public/session/role route split, public API count, MCP bearer-token boundary, CSRF write boundary, CSP frame boundary, and explicit browser modes for normal browsing, wallet-safe mode, local development, media capture, archive/save-to-project, and admin surfaces without exposing private payloads or secrets. When launched from Gamma, render status cells, boundary rows, browser-mode rows, route tables, MCP boundary, and action buttons inside Gamma-owned utility chrome so boundary checks and mode shortcuts stay on `/gamma/...`. | `browser_boundaries.viewed`, `browser_boundaries.action_opened`, `desktop.window.opened` |
| Controlled Browser | Session | Open `/browser`; enter an allowlisted URL, inspect the current allowlist, load approved links in the sandboxed controlled iframe, or explicitly open eligible blocked links outside wtfOS without changing the browser allowlist API, sandbox policy, referrer policy, or external-open rules. When launched from Gamma, render the link chamber, allowlist, viewport, blocked/ready states, and open controls inside Gamma-owned utility chrome. | `desktop.window.opened`, `browser.navigation.blocked` |
| Remote Applications | Session | Open `/applications` and `/applications/:appId/play` through the `applications` DesktopAppKey gate; list remote application manifests from the isolated host service through `/api/apphost/apps`; launch, refresh status for, inspect health/process diagnostics for, and stop a running application through authenticated wtfOS `/api/apphost/apps/:id/*` proxy calls without exposing the upstream apphost directly; open a managed wtfOS play window that creates a WebRTC receive-only connection through `/api/apphost/apps/:id/stream/offer` using the measured lowest-latency encode profile (native capture size up to a 1280x720/30fps software-encode cap, single-buffer leaky encode queue), renders the remote desktop video/audio inside a game-first play surface with overlay controls and a bottom status dock so the stream occupies the window when negotiation succeeds, shows a live fps/RTT/jitter monitor from `RTCPeerConnection.getStats()` in the session status dock backed by server-side encoder stats (encode fps/kbps in `stream/status`), falls back to apphost snapshots while WebRTC is connecting or unavailable, stops the game while keeping the provider launcher warm for swift ~3s relaunches with a boot-grace status hold so warm starts never flap to exited, requires the client to join the matching `/ws/apphost` app room before accepting input, and forwards pointer, wheel, and keyboard events through fire-and-forget `/api/apphost/apps/:id/input` posts with client-side move coalescing under a dedicated per-user `apphost-session` rate limiter (6000/min, exempt from the generic 200/min `/api/*` quota so gameplay input can never 429 auth or desktop reads); starts remote video muted so frames render before any user gesture and unmutes audio on first interaction, self-heals a black stream by renegotiating when zero frames decode while connected, streams the application's native cursor (`show-pointer=true`), hides the local and wtfOS custom cursors over the play surface, exposes a manual Capture cursor control (Esc releases pointer lock) while defaulting to absolute coordinates for menu navigation, focuses the titled game X11 window before every injected event so keyboard/pointer reach the client instead of Openbox/Steam overlays, and delays WebRTC attach until `progress.phase === "ready"` so boot-time black frames are shown via snapshots instead of a dead stream; keep provider/runtime details invisible to users and preserve the single-active-external-app guard across all wtfOS users; when launched from Gamma, render the toolbar, app list, status panel, diagnostics panel, launch/stop/status controls, and app play frame inside Gamma-owned Applications chrome while preserving the shared apphost API boundary. | `applications.viewed`, `applications.launch_requested`, `applications.status_viewed`, `applications.stop_requested`, `desktop.window.opened` |
| Agent | Session/agent | Open `/agent` as the native AI workspace; when launched from Gamma, render provider setup, chat, workbench, permissions, MCP preview, project memory, extension manifests, local file panels, git panels, and companion knowledge inside Gamma-owned Agent chrome so every tab remains on `/gamma/agent` without entering the Classic desktop; configure OpenAI, Anthropic, Google, OpenRouter, Ollama, LM Studio, or OpenAI-compatible provider adapters with user-owned credentials that are not proxied through wtfOS; review detected provider capability profiles and override available reasoning, multimodal, artifact, tool, embedding, local inference, and custom-endpoint features per provider without changing Agent apps; send chat through browser-direct provider APIs or local endpoints from the current browser session; maintain a persistent project plan; parse provider replies or pasted drafts into reviewable proposed actions; apply or dismiss generated file and command actions behind visible grants; use code editing, diagnostics, content search, code outline navigation, repository summary, diff, terminal transcript, git summary, markdown preview, image preview, native file create/rename/delete, project memory, companion knowledge backed by a machine-readable route/provider/MCP/filesystem/extension knowledge base, and wtfOS filesystem project paths inside one AppWindow; jump from code outline symbols, diagnostics, and content search results to exact editor lines; install, enable/disable, and remove user-owned Agent extension manifests for providers, MCP servers, tools, personalities, themes, and knowledge packs without granting runtime authority outside visible permissions; save, restore, import, and export Agent project snapshots under `WTF/Projects/Agent` without serializing provider credentials; create or revoke scoped MCP paired tokens; preview the MCP resources, scopes, allowed tools, and blocked tool families produced by the current visible grants before pairing an external agent; review and toggle read, write, execute, terminal, filesystem, wallet, network, application, temporary, project, and persistent grants before connected agents can act. | `agent.viewed`, `agent.provider.updated`, `agent.chat.sent`, `agent.plan.updated`, `agent.action.proposed`, `agent.action.applied`, `agent.action.dismissed`, `agent.file.edited`, `agent.file.created`, `agent.file.renamed`, `agent.file.deleted`, `agent.diagnostics.ran`, `agent.code_navigation.opened`, `agent.filesystem.saved`, `agent.filesystem.restored`, `agent.filesystem.imported`, `agent.filesystem.exported`, `agent.terminal.command`, `agent.permission.updated`, `agent.memory.updated`, `agent.extension.installed`, `agent.extension.updated`, `agent.extension.removed`, `mcp.token.created`, `mcp.token.revoked`, `desktop.window.opened` |
| Agent native git | Session/agent | Stage and unstage changed files in the current wtfOS project worktree; commit staged files into Agent's snapshot-backed local history; create and switch local Agent branches only when write and project grants are visible and enabled; surface the same status through the terminal `git status` command without executing a host git binary. | `agent.git.staged`, `agent.git.unstaged`, `agent.git.committed`, `agent.git.branch.created`, `agent.git.branch.switched` |
| Terminal | Session | Open `/terminal`; run allowlisted WTF OS commands for health, jobs, access manifest, routes, MCP status, Recovery Mode, Settings, Wallet, and Mission Control without executing arbitrary server shell commands. Uses the shared `wtfos-cli` kernel (embedded panel). `open` and route shortcuts enforce the same auth/role/app gates as the browser UI. When launched from Gamma, render the status grid, safe-command rows, CLI panel boundary, and access shortcut inside Gamma-owned utility chrome; CLI-driven navigation and the Browser Boundaries shortcut preserve `/gamma/...`. | `terminal.viewed`, `terminal.command_executed`, `desktop.window.opened` |
| CLI | Session | Open `/cli` or choose CLI interface mode in Settings for a full-screen safe CLI/TUI powered by the same shared `wtfos-cli` kernel as Terminal; run allowlisted commands, themes, banner/motd, and gated route launches with no server shell execution. When launched from Gamma, render the full-screen CLI frame, output, prompt, and run control inside Gamma-owned utility chrome, and route CLI-driven `open /path` commands through `/gamma/...` instead of falling into the Classic desktop. | `cli.viewed`, `cli.command_executed` |
| wtfOS Native CLI | Public/session | Install `@wtfos/cli` from npm (`wtfos` / `wtf` bin); run allowlisted commands against any deployment via `WTFOS_URL`; `wtfos login` stores a browser session cookie; `open`/`routes` call `/api/cli/can-open` and `/api/cli/routes` so native clients cannot bypass browser login, role, or app gates. | _(native client — no browser SystemEvent)_ |
| Task Manager | Session | Open `/task-manager`; inspect open windows, focused/minimized/running state, session uptime, signed-in user/role, and keyboard shortcut reference; switch to a selected window, close a selected non-Task-Manager window, or minimize all windows through the shared window manager. When launched from Gamma, render tabs, process table, action buttons, resource panel, shortcut panel, and status bar inside Gamma-owned utility chrome without leaving the Gamma shell. | `desktop.window.opened`, `desktop.window.focused`, `desktop.window.closed`, `desktop.window.minimized` |
| WTF Map Lab | Public demo/session edit/role ingest/agent create-only | Open `/map-lab` directly as an anonymous visitor even without the paid Map Lab app entitlement, while keeping launcher/store entitlement gates intact for the full app; when launched from Gamma, render the node palette, manual route controls, graph toolbar, scrollable board, nodes, ports, route list, run metrics, selected inspectors, minimap, AT import controls, and read-only demo state inside Gamma-owned Map Lab chrome so canvas work remains on `/gamma/map-lab` without entering the Classic desktop; open the generated read-only wtfOS topology demo for any user to inspect and run without editing nodes/routes, including route registry, domain/subdomain lanes, native manager surfaces, interaction inventory, SystemEvent spine, domain workflows, behavior assertions, public/API/agent paths, persistence, and deploy-health nodes; add keyed/indexed workflow nodes from typed templates for prompt inputs, HF model calls, Gradio Spaces, functions, routers, agents, memory, outputs, roadmaps, repos, policies, data paths, and milestones; connect output ports to compatible input ports with labeled colored routes/pipelines and visible compatible/incompatible port feedback; cancel pending routes with Escape; inspect, edit, keyboard-delete, and color selected routes; run the graph to pulse active routes and show connected-node activity; lock important nodes; drag unlocked nodes, nudge selected nodes with optional snap-to-grid, and optimize unlocked node positions for flow or fit; scroll, pan, zoom, fit, reset, and overview-jump the map viewport inside resizable/maximized app windows; preview AT Protocol repo/firehose imports as read-only map material; save/restore the user's map document to the WTFOS repo boundary; allow MCP users to create map objects without granting use of ingested repo or firehose data paths. | `map_lab.viewed`, `map_lab.demo.opened`, `map_lab.node.created`, `map_lab.node.moved`, `map_lab.wire.created`, `map_lab.route.created`, `map_lab.pipeline.ran`, `map_lab.ingest.previewed`, `map_lab.repo.saved`, `map_lab.repo.restored`, `map_lab.viewport.changed` |
| Notification Center | Session | Open `/notification-center` or legacy `/notifications` as a first-class OS surface; view all/unread changes, adjust notification preferences, open supported linked targets, and mark one/all notifications read without entering direct-message threads first. When launched from Gamma through either alias, render notification rows, preference rows, filters, and supported target handoffs inside Gamma-owned communication chrome. | `notification_center.viewed`, `notification_center.filter_changed`, `notification_center.notification_opened`, `notification_center.mark_read`, `notification_center.mark_all_read`, `notification_center.preferences_saved`, `notification.viewed`, `notification.opened`, `notification.read`, `notification.read_all`, `notification.preference.updated` |
| Recovery Mode | Session | View user-safe incident state for health, media cache, wallet sessions, chain network overrides, and saved shell layout; disconnect local Tezos/Etherlink wallet sessions; reset local chain network overrides; clear saved window session; refresh filesystem probes; export a local recovery report; open emergency Terminal; route operator-only permissions reset, app rollback, driver quarantine, and restore-proof work to admin-gated surfaces. When launched from Gamma, render severity, local state cells, incidents, local repairs, operator repairs, messages, and route buttons inside Gamma-owned utility chrome so reloads and repair handoffs preserve `/gamma/...`. | `recovery_mode.viewed`, `recovery_mode.wallets_disconnected`, `recovery_mode.network_reset`, `recovery_mode.window_session_cleared`, `recovery_mode.filesystem_checked`, `recovery_mode.report_exported`, `recovery_mode.action_opened` |
| Desktop icons, shortcuts, and Start Menu app gates | Session/public gates/time out blocked/admin app gates | Open enabled app icons and registry-derived Start Menu entries organized as Apps, `CREATE!`, domain categories, account/system items, and Browse; keep the core Contact Admin icon visible on every ordinary signed-in default desktop while keeping the strict Admin Panel absent for non-admin roles; hide app icons, Start Menu entries, shortcuts, and command-palette launch commands from signed-in `time_out` accounts and from disabled app gates, including independently gateable owner surfaces such as WTF Domains (`wtf-subdomains`); show yellow outlines on experimental app icons; use right-click or Shift-click Windows 95 context menus; drag enabled Start Menu items to the desktop to create route shortcuts; open/move/delete shortcuts; drag native icons and shortcuts with smooth live movement while committing the final persisted position per signed-in user on release; reset layout; stale shortcut/direct-route launches for disabled apps render the admin-disabled failure state instead of running the app. | `desktop.icon.opened`, `desktop.icon.moved`, `desktop.icon_layout.reset`, `desktop.context_menu.opened`, `desktop.shortcut.created`, `desktop.shortcut.opened`, `desktop.shortcut.moved`, `desktop.shortcut.deleted`, `desktop.app.disabled_by_admin` |
| Native app admin affordance | Admin | Open the native `ADM` panel inside each registered WTF OS app window; inspect domain/subdomain, adjust native settings, open central admin tabs, and review challenge automation handles for that app. | `admin.os_surface.viewed`, `admin.os_surface.setting_opened`, `admin.app_gate.updated` |
| Native app bug reporting affordance | Session | Open the shared ant/bug icon in any WTF OS app window titlebar or inline app header; review the current window's domain, subdomain, surface label, and route context; submit a bounded bug report that creates a `bug reports` Message Board post and delivers targeted system inbox cards/notifications to admin-role users. | `desktop.bug_report.opened`, `bug_report.created` |
| Theme Builder | Session | Open `/theme-builder` or legacy `/desktop-settings`; choose left-column categories for Desktop background, Appearance, Font, Cursor, Physics/icons, Pet, and Agent pairing so each main panel only shows the selected setting type; use the tab-local Save button for the active category or the static bottom-right `SAVE` button outside all tab panels; see the global `SAVE` button green when the current draft matches profile-recorded settings and red after any unsaved appearance change; choose OS appearance grammar (`classic-95`, `wtf-xp`, `wtf-aqua`, `wtf-zine`) separately from user colors; use the enforced `wtfos-soft-system` font pack with instant OS-wide typography preview; set WIM and WTF LIVE default chat typography presets within constrained color, emphasis, and size windows while font fields stay locked to Soft System; choose color scheme; edit window/desktop/title/text/highlight/button colors; set/clear/upload wallpaper; set token wallpaper; choose cursor style; enable physics and gravity mode; set WX weather from the system tray lightning control; manage desktop pet visibility and MCP pairing without leaving the user OS. When launched from Gamma through either `/theme-builder` or `/desktop-settings`, render appearance, typography, chat defaults, wallpaper/token art, cursor/physics, pet care, and MCP pairing inside Gamma-owned settings chrome while preserving the shared desktop/media/profile/pet/MCP APIs. | `desktop.settings.viewed`, `desktop.appearance.updated`, `desktop.font_pack.updated`, `desktop.chat_typography.updated`, `desktop.wallpaper.uploaded`, `desktop.wallpaper.token_set`, `desktop.physics.updated`, `desktop.weather.updated` |
| Desktop pet | Session/agent | Enable pet; adopt/generate/revive; rename/customize; view state/events; feed, water, play, pet, clean, scoop, poop, medicine, nap, revive. Water care hydrates thirsty pets before using water as bath/clean care. Each care action can award once-daily XP. | `desktop.pet.generated`, `desktop.pet.customized`, `desktop.pet.action`, `desktop.pet.death`, `xp.awarded` |
| Desktop ambient world | Session | Post heartbeat; submit pet escape; submit toy escape; place food/water/pillow/mess/remains; observe anonymous neighboring pets/toys without topology disclosure. | `desktop.world.heartbeat`, `desktop.world.pet_escape`, `desktop.world.toy_escape`, `desktop.drop.placed`, `desktop.drop.cleaned` |
| Reggie onboarding assistant | Session | See Reggie, the golden pixel-hamster wtfOS assistant, on the classic desktop for every signed-in user who has not completed Reggie's Challenge; click Reggie for greetings, next-step recommendations, and a quest log with locked/available/completed side quests read from `GET /api/reggie/quest`; right-click the desktop and choose Summon Reggie to pop him back near the pointer after hiding or snoozing him; hide Reggie until summoned, snooze him for ten minutes with an actual wake timer, or let passive nag bubbles appear temporarily and disappear again; ask free-form questions answered from the deterministic (non-AI) wtfOS knowledge base plus live account/quest state, with smart-ass fallback replies for out-of-scope questions; persist Reggie's assistant-authored speech through `POST /api/reggie/messages` so temporary speech bubbles are also recoverable as Reggie-tagged WIM/Inbox instant messages; follow "Take me there" navigation while Reggie walks beside `data-reggie-anchor`/desktop-icon anchors, keeping the bubble in view and on the side away from the indicated control so it remains clickable; keep the custom cursor visibly above the speech bubble and its buttons without capturing input; receive periodic nag reminders; emit tour checkpoints when visiting quest destinations; force a progress re-verification pass; watch Reggie celebrate and retire when the finale completes. | `reggie.assistant.opened`, `reggie.assistant.summoned`, `reggie.assistant.dismissed`, `reggie.hint.shown`, `reggie.question.asked`, `reggie.message.sent`, `reggie.checkpoint.reached`, `reggie.progress.checked`, `reggie.quest.completed` |
| Desktop artifacts/items | Session/owner | Render inventory-backed artifacts; use cursor tool tray, scale tool, portal gun, jukebox/Tezamp, fan, lights, sticky note, mop, vacuum, train kit, paper shredder, catapult, ant farm, and other granted artifacts. | `desktop.artifact.spawned`, `desktop.artifact.used`, `desktop.tool.selected`, `desktop.item.effect_triggered` |
| Screen saver/cursor | Session | Trigger/exit screen saver; use custom animated cursors; hide system cursor during custom cursor/care tool use. | `desktop.screensaver.started`, `desktop.screensaver.exited`, `desktop.cursor.updated` |
| MCP pairing | Session | View MCP endpoint; create named token whose scopes are capped to the signed-in user's role; copy raw token once; list active prefixes/scopes/last-use; revoke token. | `mcp.token.created`, `mcp.token.revoked`, `mcp.token.listed` |

## Concern: Wallets, Tokens, Portfolio, and On-Chain State

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Tezos wallets | Session | List linked wallets; request challenge; link only through a connected wallet ownership signature; reject typed address-only new wallet links; unlink; set primary; sync profile tokens; sync one wallet; fetch wallet WTF balance/tokens/profile dossier. | `wallet.challenge.created`, `wallet.linked`, `wallet.unlinked`, `wallet.primary_set`, `wallet.sync_requested`, `wallet.balance.viewed` |
| Wallet signer preflight | Session/wallet | Rehydrate/request active account through Octez Connect v5; attach a custom Octez-backed wallet provider to Taquito for operation construction; enable mobile/QR WalletConnect only with the deployment's explicit `VITE_WALLETCONNECT_PROJECT_ID`; keep extension and supported web-wallet transports usable when that optional ID is absent; use Octez-hosted RPC defaults for mainnet/Shadownet; validate chain id; reject mismatched account before writes. | `wallet.provider.preflight.succeeded`, `wallet.provider.preflight.failed`, `wallet.account_mismatch.blocked` |
| Etherlink wallets | Session/staff | View supported networks; request challenge; link EVM/Etherlink wallet; set primary; unlink; sync one wallet; sync all; list Etherlink assets. | `etherlink.wallet.linked`, `etherlink.wallet.unlinked`, `etherlink.wallet.primary_set`, `etherlink.wallet.sync_requested`, `etherlink.assets.viewed` |
| Dashboard/cockpit | Session | View passport, active season, WTF/XP summary, holdings, activity, collections, sync/backfill state, P&L, positions, realized sales, and rollups; when hosted by Gamma after login, use the first-screen next-action rail to open existing Daily proof `/side-quests`, Challenges `/challenges`, People `/w`, Apps `/wtfiam?category=apps`, Inbox `/messages`, or Profile `/profile` routes without leaving the Gamma presentation host. | `dashboard.viewed`, `cockpit.holdings.viewed`, `cockpit.activity.viewed`, `cockpit.collection.viewed`, `cockpit.sync.run_requested`, `app.interaction.tracked` |
| Wallet event ingestion | System | Ingest token transfers, token mint/burn, XTZ transfers, contract calls, delegations, originations; track cursors, sync runs, and indexing queue. | `wallet_event.ingested`, `wallet_sync.started`, `wallet_sync.completed`, `wallet_sync.failed`, `indexing_queue.enqueued` |
| Tezos Intel | Session | View sources/import commands; load creator score; compare creators; load market pulse over a selected window; on Gamma, use the market-intel shell with score, compare, market pulse, and source panels rendered in Gamma chrome while keeping `/api/tezos-intel/*` shared. | `tezos_intel.sources.viewed`, `tezos_intel.creator.viewed`, `tezos_intel.compare.viewed`, `tezos_intel.market_pulse.viewed` |
| WTF Domains | Session/staff/admin app gate | Enable the independent `wtf-subdomains` / `desktop:wtf-subdomains` owner surface in the app registry; view own wtf.tez grants; view registrar config/status/storage; prepare commit/register operations from `/wtf-subdomains` or the focused `/wtf-subdomains/setup` Subdomain Setup window opened from Settings; view pin registry summaries and the canonical PDS/subdomain target used by IPFS Pinning Manager; view domain chat config; staff grant label to user; staff update grant status/op hash/notes; admin-seed an account-specific bypass registration when OAuth/social-login breakage blocks a trusted user's verified social/domain access, as with cobwebsaints receiving `unitedsaints` X, `cobwebsaints.bsky.social`, `cobwebsaints.wtfos.me`, and `cobwebsaints.wtf.tez`. | `wtf_domain.grants.viewed`, `wtf_domain.registration.prepared`, `ipfs_pinning.subdomain_registry.linked`, `wtf_domain.chat_config.viewed`, `wtf_domain.granted`, `wtf_domain.status_updated`, `admin.app_gate.updated` |
| wtfOS user sites | Public/session/staff/system | Claim the signed-in user's original username as a `username.wtfos.me` host from `/wtf-subdomains` or the focused `/wtf-subdomains/setup` Subdomain Setup window after wallet, OAuth social, and Bluesky/WTF DID eligibility checks; keep the label stable across username changes unless staff migrates it; edit the root page plus up to 25 named pages in the WTF Domains page editor or through trusted creator tools such as Macaroni; attach only owned media-library assets within the 500 MB site cap while IPFS pin bytes remain outside the user-site asset cap; save page HTML up to the shared 5 MB cap; sandbox-preview draft HTML; immediately publish an immutable version with digest, page list, attached media ids, DID target snapshot, actor, and audit metadata; enqueue `app.wtfos.identity.site` provenance, `app.wtfos.identity.siteSnapshot` render payload, and primary `app.wtfos.identity.siteIndex` pointer records so the `.me` renderer can serve without the app database; publish Macaroni drop pages to `https://username.wtfos.me/drop-title` only after Macaroni has a deployed or resumed `KT1...` contract; roll back to a previous published version; serve published pages and attached media from `https://username.wtfos.me/`, redirect unpublished claimed hosts to `/user/:username`, serve `https://username.wtfos.me/.well-known/atproto-did` and public pin pointers at `https://username.wtfos.me/.well-known/wtfos-pins` for non-suspended hosts with active identity proof, block privileged app/API/service-worker paths on user hosts, never overwrite a user-authored `/pins` page, allow Caddy on-demand TLS only for registered non-suspended hosts, warn on identity proof loss, and let staff suspend or restore serving without deleting data or DID history. | `wtf_site.claimed`, `wtf_site.page_saved`, `wtf_site.page_deleted`, `wtf_site.assets_updated`, `wtf_site.published`, `wtf_site.rolled_back`, `wtf_site.suspended`, `wtf_site.restored`, `wtf_site.public.viewed`, `ipfs_pinning.subdomain_registry.linked`, `macaroni.drop_published`, `atproto.handle.verified`, `wtfos.atproto_outbox.enqueued` |
| Macaroni exported drop pages | Public/wallet | Load a Macaroni Studio-exported static drop page from wtfOS or a copied third-party host; read live contract stage/supply state; show the actual per-wallet/allowlist limits from chain storage; connect a Tezos wallet through one reusable Octez-primary client with active-account subscription and legacy Taquito compatibility scoped to generated static wallet bundles; preflight chain id, RPC packing, balance, stage, and wallet allowance before mint; send mint/reveal wallet operations; recover owned mints; share the drop with service-aware bounded X/Bluesky compose text that preserves mint/media URLs where possible; and add sale stages to a calendar through prefilled ICS or Google Calendar links. | `macaroni.drop.viewed`, `macaroni.drop_wallet.connected`, `macaroni.drop_mint.preflighted`, `macaroni.drop_minted`, `macaroni.drop_revealed`, `macaroni.drop_wallet.preflight_failed`, `macaroni.drop_shared`, `macaroni.drop_calendar_added` |
| Token archive | Session/owner | Request archive data; queue owned-token artifact preservation; view archive status. | `token_archive.requested`, `token_archive.status_viewed` |
| Contract activity | Session/staff | Submit contract activity evidence; browse/filter contract activity log by status/search. | `contract_activity.submitted`, `contract_activity.viewed`, `contract_activity.status_updated` |

## Concern: Gameshow Participation, Progression, and Rewards

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Seasons | Public/session/staff | List seasons; view details; staff create/update/delete/status-manage seasons. | `season.viewed`, `season.created`, `season.updated`, `season.deleted` |
| Rounds | Public/session/staff | List rounds; view round detail; inspect attached challenges/status/dates/rules; staff create/update/delete/attach/status-manage rounds. | `round.viewed`, `round.created`, `round.updated`, `round.deleted` |
| Challenges | Public/session/staff | List/filter/view one-time bigger missions; inspect reward/subdomain/mint data; submit text/link/evidence; view own submission; staff create/update/grade pass/fail/bonus, set reward flags, mark reward paid. Challenges may span several side quests or show events rather than a single recurring task. | `challenge.viewed`, `challenge.submitted`, `challenge.graded`, `challenge.reward_flag.created`, `challenge.reward_claimed`, `challenge.reward_marked_paid` |
| Challenge rewards | System/staff | Award submission XP; award grade XP; insert reward-ledger row; grant wtf.tez subdomain; claim reward flag. | `xp.awarded`, `reward.ledger.created`, `wtf_domain.granted`, `challenge.reward_claimed` |
| Side quests | Public/session/staff | Public catalog list; signed-in detail view redacts other users' proof URLs/text unless staff; rate-limited completion submit; require a matching confirmed fee of at least the configured WTF amount before a paid quest can be submitted, auto-approved, manually approved, or rewarded; auto-track daily side quest progress per signed-in user through normalized OS events; reset daily quest readiness at 00:00 UTC; show ready-to-claim, claimed, and player-completion counts without marking other users complete. Staff create/update special quests and approve/reject manual completions, while fee confirmation binds the fee record to its owning quest. | `side_quest.viewed`, `side_quest.progress_verified`, `side_quest.completion_submitted`, `side_quest.auto_verified`, `side_quest.approved`, `side_quest.rejected` |
| DedRooms | Session/admin | Open `/dedrooms` as a signed-in AppWindow MUD; when opened through Gamma, keep the transcript, status rail, prompt bar, command input, presence, and admin editor inside Gamma-owned presentation chrome while preserving shared `/api/dedrooms/*` and `/ws/dedrooms`; restore the player's stasis location after closing/reopening; read transcript-first terminal output, compact status rail, character sheet, coordinate, placed room, map reveal summary, known authoritative passages, inspectable room objects, inventory, room resources, NPCs, presence, and command deck; submit authoritative MUD commands through `POST /api/dedrooms/command`; use commands including `look`, `inspect <thing>`, `sheet`, `roll <skill>`, `map`, `doors`, `listen`, `overhear`, `campaign`, and door travel only through listed passages such as `go north`, `go hatch`, `enter mirror`, or `go ceiling door`; explore a persistent finite authored-room map seeded by five anchor rooms before first player spawn: THNG, Herb's Ivory Tower on High Horse Hill, The Governance Chambers, The Trilla-tek Wishworks, and The Tyranny Force Listening Bakery, while player spawn targets avoid those anchor rooms until discovered through play; reveal unused authored rooms through unresolved listed passages until the deck is exhausted, then loop to existing rooms; keep the Green Room absent until intro campaign/world events trigger it; inspect stateful details including Lily's shoe, taxi-like tire tracks, yellow paint scrapings, ant bureaucracy, aubergine chapel rites, uranium laundry warnings, sheep constellations, blanket-cult minutes, cat-wall routes, the Yellow Knight, blinking splendor, and Tezos/bread-art display-case mirror rooms for baker-bankers, crumb custody rewards, delegation loaves, generative bread-art pinning, case placards/indexers, front-lawn display cases, Here And Now/Teia bread kiosk ghosts, crumb resale splitters, governance, Michelson contracts, rollup bridges, and wallet-safety lore; gather contested daily resource drops or lower-yield farmable resources discovered through `look` and room state; run 40 command minigames such as `count ants`, `polish aubergine`, `wash glow`, `count sheep`, `pet cat`, `salute knight`, `decode splendor`, `bake block`, `bank bread`, `pin art`, `polish case`, `pin bread`, `dust case`, `list bread`, `split crumbs`, `vote quorum`, `verify contract`, `bridge rumor`, and `check wallet`; combine three matching tier items into one upgraded tier; talk to scheduled/reactive NPCs with d20-style character sheets; overhear passive room conversations such as bread-art haggling; offer shared milestone proofs; attune personal proofs; mark other players `friend`, `ally`, `neutral`, or `sus-af`; create/list alliances; join `/ws/dedrooms` for room presence and room/world event fan-out without client-authoritative mutations; let admins edit content records, campaign mode, target departure count, anchor config, room deck entries, resource/event/dialogue definitions, and validate room/NPC/item/resource/campaign/anchor references before publishing; active Search for the Green Room intro campaign departure grants Season 3 Contestant/contestant roles, creates Season 3 contestant rows, marks the MUD player departed, and reopening departed players shows exactly `You have departed from this world.` while myth mode keeps the lore without granting status. | `ded_rooms.command.look`, `ded_rooms.command.inspect`, `ded_rooms.command.go`, `ded_rooms.command.sheet`, `ded_rooms.command.roll`, `ded_rooms.command.map`, `ded_rooms.command.doors`, `ded_rooms.command.listen`, `ded_rooms.command.campaign`, `ded_rooms.command.talk`, `ded_rooms.command.combine`, `ded_rooms.command.minigame`, `ded_rooms.resource.gathered`, `ded_rooms.pond.offering`, `ded_rooms.shared_milestone`, `ded_rooms.green_room.placed`, `ded_rooms.player.departed`, `ded_rooms.content.upsert`, `ded_rooms.campaign.update` |
| Side quest rewards | Session/system/staff | Award quest XP and insert reward ledger rows after verified side quest rewards are claimed, or immediately for side quests explicitly marked automatic such as Mint Art Monday; show the reward account; warn and skip new WTF ledger grants while the user has the No WTF Rewards curse; let earned WTF be used in WTFIAM or cashed out when not cursed; keep EXP as an in-app-only score. | `xp.awarded`, `reward.ledger.created`, `reward.account.viewed`, `reward.wtf_embargo.warned`, `side_quest.reward_claimed`, `side_quest.reward_distributed`, `wtf.awarded` |
| Auto-verification | Session/system | Current route implementation verifies: `manual`, `profile_avatar`, `profile_bio`, `wallet_connected`, `social_twitter`, `social_discord`, `post_message`, `holds_positive_balance`, `holds_art_nft`, `has_mint_event`, `listed_on_trade_board`, `wtf_swapped_in_buyback`, `wtf_paid_to_operator_at_least`. Schema also declares future/latent handles: `x_space_attendance`, `x_hashtag_post`, `console_hiscore`, `mint_with_tag`, `mint_in_curation`, `discord_voice_presence`. | `side_quest.auto_verify.checked`, `side_quest.auto_verify.failed`, `side_quest.auto_verify.schema_gap` |
| Challenge automation | Staff/system | Admin builds event/predicate rules from registered trigger handles, combines ALL/ANY conditions, attaches reward actions, activates/pauses/archives definitions, inspects per-user progress, recent events, action logs, and audit logs. Canonical daily side quests are automation definitions with daily completion keys, mostly user claim gating, optional automatic reward execution, weekday predicates for UTC-day loops such as Mint Art Monday, and per-day global completion counts. Live ingestion currently covers messageboard posts/channel posts/reactions, XP awards, wallet links/relinks, linked-wallet Tezos mints/activity, desktop pet interactions, and generic `app.interaction.tracked`; Tezos ownership predicates verify against linked wallet holdings/TzKT. | `messageboard.post.created`, `messageboard.channel.post.created`, `messageboard.reaction.added`, `messageboard.reaction.removed`, `xp.awarded`, `wtf.awarded`, `user.wallet.connected`, `blockchain.tezos.token_mint`, `desktop.pet.interacted`, `app.interaction.tracked`, `nft.ownership.verified`, `token.contract.owned`, `token.id.owned`, `gameshow.challenge.completed`, `challenge_automation.reward_claim_required` |
| Reggie's Quest onboarding questline | Session/system | Complete Reggie's 29-step onboarding questline plus finale, seeded idempotently at boot as `reggieQuest` challenge automation definitions (`reggie_*_v1` seed keys) with prerequisite gating enforced inside each condition tree via the `reggie.step_completed` predicate; steps verify through normalized events (`user.wallet.connected`, `desktop.pet.interacted`, `wim.message.sent`, `dm.message.sent`, `macaroni.package_finalized`, `atproto.room.message_sent`, `desktop.appearance.updated`, `desktop.icon.opened`, `xp.awarded`, `wtf.awarded`, `leaderboard.xp.viewed`, `reggie.checkpoint.reached`) and DB-backed `reggie.*` predicates for profile readiness, PFP, X/Bluesky links, Tezos/multi/Etherlink wallets, wtfos.me and wtf.tez claims, Studio projects, WTF LIVE rooms/stages, calendar tickets, and market purchases; rewards (EXP, WTF, notifications, finale inventory item) execute automatically on verification without a claim step; the user reads progress and locked/available/completed status from `GET /api/reggie/quest`. | `reggie.checkpoint.reached`, `reggie.quest.completed`, `xp.awarded`, `wtf.awarded`, `app.interaction.tracked`, `challenge_automation.reward_claim_required` |
| Mint portal | Session | View mint-bound challenges; copy submission tags; open mint links; check recent mints; auto-create submissions from indexed mints; view token/op hash data; when opened through Gamma, keep challenge tabs, direct mint controls, submission rows, and generative-art entry inside Gamma-owned presentation chrome while preserving shared mint-portal APIs, wallet connect, and `mintOpenEditionFromWtf`. | `mint_portal.viewed`, `mint_submission.created`, `mint_submission.matched` |
| Leaderboards | Public | View WTF holder leaderboard; view earned-WTF reward leaderboard with total earned/current owed/paid/market-spent columns; view EXP earned/spent leaderboard; view other in-app rewards such as wtf.tez subdomains; view recent WTF transfers with Tezos enrichment. | `leaderboard.viewed`, `leaderboard.rewards_wtf.viewed`, `leaderboard.rewards_exp.viewed`, `leaderboard.rewards_other.viewed`, `leaderboard.xp.viewed`, `leaderboard.transfers.viewed` |
| WTF Recapture | Public/session/staff | View recapture leaderboard, buyback windows, auctions, own recapture events; understand that buyback, ante, entry-fee, and auction-settlement payments are manual external-wallet steps whose exact linked sender, destination, token, amount, and applied operation are verified through TzKT before credit; receive a versioned WtfBuybackV1 directional allowlist proof whose left/right steps match the checked-in contract; verify a completed buyback swap by operation hash; record an explicitly off-chain auction bid that sends and reserves no WTF; staff manage windows and operator flows, while obsolete or root-incompatible allowlists are rejected before funding/opening; when opened through Gamma, keep payment-boundary guidance, leaderboard rows, buyback windows, auction cards, tabs, metrics, and status pills inside Gamma-owned presentation chrome while preserving shared recapture, buyback, and auction APIs. | `recapture.viewed`, `buyback.intent_submitted`, `wtf_auction.bid_submitted`, `buyback.window_updated` |
| Calendar/events | Public/session/staff/bot | Open `/calendar` from its desktop icon or permanent task-tray icon; browse local-time events in navigable Day, Week, Month, or Agenda views; move to the previous or next temporal window and return to today through one shared date-navigation model; activate any event card in every view to read its full description, creator attribution, participation links, and original TTC source link when applicable; mark an event Interested or Going, explicitly turn its task-tray reminder on or off, clear the plan, and review durable account-backed choices in My plans; read TTC recurring occurrences restored around the rolling next-occurrence iCal anchor; create a personal browser-only event; submit a shared event ticket; view own tickets; staff review/publish/cancel/manual-create/sync events; receive one replaceable tray popup per chosen followed event or personal-entry threshold (today/login, one day, six hours, one hour, and start time) with per-user viewed history; open prefilled Calendar creation context from WTF LIVE rooms, WIM, or Message Board; bot mirrors Discord events; on Gamma, keep Calendar inside the Gamma application shell while preserving shared APIs, local personal storage, auth gates, and TTC links. | `calendar.viewed`, `calendar.personal_entry_created`, `calendar.ticket_submitted`, `calendar.ticket_reviewed`, `calendar.event_published`, `calendar.event_cancelled`, `calendar.participation.updated`, `calendar.participation.cleared`, `discord.event_mirrored`, `app.interaction.tracked` |
| Attendance | Session/bot/staff | Claim in-app attendance; ingest Discord voice/stage state; view own rollup; staff view event attendance trail. | `attendance.claimed`, `attendance.voice_state_ingested`, `attendance.heartbeat`, `attendance.viewed` |
| Reward ledger/operator | Session/wallet/staff | Users view reward account balances, spend earned WTF in WTFIAM, and request cashout once available earned WTF is at least 20 WTF. The reward-disburser issuer creates a claimant-bound redemption in mainnet reward escrow V2 `KT1PiBUGpind9xUxGR4EzpeXiySHcgUBDQZo`; only the primary linked Tezos wallet can sign `claim_redemption`; wtfOS verifies the exact applied mainnet operation before marking the reward ledger paid. Interrupted claims remain claimable/finalizable without a second reward allocation. The separate contract-admin wallet owns pause, recovery, and role rotation while the issuer cannot exercise those controls. Staff view/filter/select rewards and reconcile operator runs. EXP is never wallet-disbursed. | `reward.account.viewed`, `reward.cashout_requested`, `reward.cashout_paid`, `reward.market_spent`, `reward.ledger.viewed`, `reward.marked_paid`, `operator.disbursement.previewed`, `operator.disbursement.run`, `operator.disbursement.reconciled` |

## Concern: Community, Social, Messaging, and Discord

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Message board | Public/session/staff/webhook | List categories/channels; read channel messages/pins; post where allowed with visible send failure feedback; react where channel permissions allow; edit/delete own; staff moderate, pin/unpin, manage categories/channels/permissions/webhooks. | `board.channel.viewed`, `board.message.created`, `messageboard.post.created`, `messageboard.channel.post.created`, `messageboard.reaction.added`, `messageboard.reaction.removed`, `board.message.edited`, `board.message.deleted`, `board.message.pinned`, `board.webhook_received`, `xp.awarded` |
| Legacy threads/channels | Public/session | Read/create legacy threads/replies where supported; receive deprecation headers toward board surface; list/read only legacy channels whose audience includes the user's effective roles; post only with the matching audience and `post_message_board` permission; reject missing channels and cross-channel reply parents so legacy numeric IDs cannot bypass canonical board authorization. | `legacy_thread.viewed`, `legacy_thread.created`, `legacy_thread.replied` |
| Direct messages | Session/operator | Search users; create/list/read DMs and Studio room conversations; send messages; mark read from Messages, WIM, Studio, or Inbox; receive Studio-specific notifications while the shared DM route remains the canonical write backend; privately report a specific received message with a written reason; prevent self-reporting and duplicate reports; let an authorized operator review the original message and reason, record a required disposition note, and close the open safety item without including private message content in normalized event metadata. | `dm.conversation.created`, `dm.message.sent`, `dm.message.read`, `dm.message.reported`, `dm.message.report_reviewed`, `xp.awarded` |
| Comms kernel | Session/system | Index normalized cards from Inbox Mail, WIM/DMs, Studio, admin/system notices, invites, notification subscriptions, Board, W, Telegram, and future read-only external adapters; isolate read states and unread counts per user; resolve each item back to its owning WTFOS route or approved external link without becoming the canonical write backend. | `comms.item.indexed`, `comms.item.read`, `comms.route.opened` |
| Inbox | Session/system/webhook | Open `/mail` as the permanent central messaging hub with a non-deletable but repositionable desktop icon labeled Inbox and a role-correct unread badge from `GET /api/comms/unread-count`; list all normalized messages with search, filter, sort, and three-row cards trimmed by category (`system`, `admin`, `user_mail`, `wim`, `invite`, `notification_subscription`, `studio`); open category tabs, chronological WIM/Mail/Studio conversation history, the `Admin contact` tab backed by `/api/admin-inbox/threads` or the admin list, and Drafts for WIM users, sys admins, or external mail; read and reply inside user-to-admin threads while preserving separate user/admin unread state; use first-class New message and New mail controls; mark messages read manually or when viewed from Inbox/WIM/Messages/Studio; flag/star/bookmark/WTF-react, reply, forward, save WIP drafts, and save reusable templates; reply to selected external mail from the reader panel; reply to WIM direct and Studio conversations through an inline instant-message composer in the conversation tab; provision an eligible user's `username@mail.wtfgameshow.app` mailbox, return 403 with the existing identity/remediation code when mail prerequisites are unmet, send through the configured provider or existing DM routes, ingest inbound and delivery webhooks, persist delivery failures, and assign system urgency tiers for account, wallet, social-proof, safety, and immediate system needs; when opened through Gamma, keep mailbox status, navigation, message list, selected-message reader, compose controls, conversation composer, and send feedback inside Gamma-owned presentation chrome while preserving `/api/mail/*`, `/api/messages/*`, `/api/notifications/*`, and comms indexing behavior. | `inbox.viewed`, `inbox.message.read`, `inbox.message.flagged`, `inbox.message.starred`, `inbox.message.bookmarked`, `inbox.message.reacted`, `inbox.message.replied`, `inbox.message.forwarded`, `inbox.draft.saved`, `admin_inbox.message.read`, `admin_inbox.reply.created`, `dm.message.sent`, `mail.message.received`, `mail.message.sent`, `mail.delivery.failed`, `comms.item.indexed` |
| Contact Admin | Session submit/reply; admin global read/reply | Open the separate always-visible `/admin-inbox` core desktop app. Non-admin users draft a private issue, idea, question, feedback item, or other message; provide message, impact, evidence notes, reproduction steps, and expected outcome; optionally attach up to five server-validated image uploads using the prompt “Would you like to attach some screenshots?”; and send the authenticated submission to all admin-role readers without trusting client sender identity. Admin-role users open the same app into a global inbox, mark messages read, inspect raw form-table data, an RFC-like email rendering, agent-ready Markdown, and protected screenshot evidence, then reply in the durable thread. The reporting user can recover and continue the same thread from Inbox's `Admin contact` tab; admin and user reply states contribute independently to the Inbox unread badge. | `admin_inbox.message.created`, `admin_inbox.message.read`, `admin_inbox.reply.created`, `desktop.window.opened` |
| Digest | Session | View the unified timeline of normalized cards; filter by source; mark items read; open source-owned routes or approved external targets while keeping origin labels visible. When opened through Gamma, Digest renders inside the Gamma application shell and preserves the Gamma presentation layer for source-owned route handoffs such as W post aliases and Browser source previews without changing shared `/api/comms/*` reads or read-state writes. | `digest.viewed`, `digest.source_filtered`, `comms.item.read`, `comms.route.opened` |
| WIM | Session | Open a classic instant messenger roster of WTF users as a desktop-level buddy-list widget without a containing WIM AppWindow; relocate, resize, minimize, maximize, close, and restore the buddy widget through system appearance-owned window controls and the shared OS taskbar instead of app-drawn traffic lights or a WIM-local dock; see users bucketed as active, inactive/away, or offline from the browser-session presence contract; manage a browser-local friends list with collapsible friends/active/inactive/offline/all-user/recent-chat groups; open the settings icon as an in-place popup, not a route or replacement screen, to create browser-local custom buddy lists and place users into those lists; accept WTF LIVE attendance add-buddy shortcuts into the same browser-local friend list; keep conversation windows closed until the user double-clicks a WTF user or recent chat, chooses the visible open-chat button, or presses Enter/Space on a WTF user, then open or create that user's direct DM-backed chat history as a desktop-level movable/resizable/minimizable/maximizable/closable conversation widget with the same system-owned control styling; show Reggie assistant conversations as source-tagged Reggie WIM messages when `/api/reggie/messages` writes assistant-authored DM rows; show Studio project conversations in Recent Chats and open them in the same lightweight WIM conversation widget rather than forcing the full Studio interface, while keeping Studio rooms out of the buddy roster; combine multiple conversations as tabs in one conversation widget, drag tabs between WIM conversation widgets, or drop a tab onto the desktop WIM surface to isolate it as its own conversation widget; compose conversation messages with classic IM-style font family, font size, text color, bold, italic, and underline controls that persist in DM message metadata so recipients see the chosen styling; insert GIPHY/Tenor GIF URLs through the GIF picker/search handoff, attach wtfOS My Media items from `/api/media/mine`, and attach owned/created token links from `/api/profile/tokens` without creating a parallel message backend; show unread direct, Reggie assistant, and Studio project messages as dismissible WIM desktop popups when WIM loads, and open the canonical DM conversation from the popup; send through the existing DM route; keep the buddy list, chat widgets, tab controls, picker controls, unread popups, and composer usable in a 320px mobile Chrome-style viewport without horizontal document overflow while preserving visible focus, browser-locale sorting/time formatting, ARIA name/role/value, live message log announcements, and at least the WCAG 2.2 AA target-size floor for active controls. | `wim.chat.opened`, `wim.friend.added`, `wim.message.sent`, `wim.offline_popup.opened`, `wim.offline_popup.dismissed`, `reggie.message.sent`, `dm.message.sent` |
| Controlled browser | Session | Open approved WTF/social/Tezos/market links in the lightweight link chamber; block arbitrary domains and route them to the user's external browser instead of proxying random web usage. | `browser.link.opened`, `browser.navigation.blocked`, `comms.route.opened` |
| Dear Diary | Session/owner | Create, edit, delete, classify, tag, date, search, sort, cross-reference, and index private diary entries; open a preloaded welcome-rebellion entry from onboarding. When opened through Gamma, the private diary renders inside Gamma-owned search, stats, entry list, editor, cross-reference, and footer regions while preserving existing `/api/diary/*` CRUD and index behavior. | `diary.index.viewed`, `diary.entry.created`, `diary.entry.updated`, `diary.entry.deleted` |
| Notifications | Session | View Notification Center tab; toggle settings; filter all/unread; open linked item; mark one/all read. | `notification.viewed`, `notification.opened`, `notification.read`, `notification.read_all`, `notification.preference.updated` |
| W Tezos digest | Session/staff | View chronological read-only timeline from admin-curated X handles (profile URL scrape, no X API); refresh; open embedded post cards; open X for view/repost/reply via intent URLs (no native WTF posting); staff edit digest handle list in W admin and WTF OS admin. Gamma hosts `/w`, `/w/post/:id`, `/w/chat`, `/w/groupchat/:id`, `/chat`, and `/chat/:id` inside the same W digest presentation shell while preserving digest-mode capabilities and legacy disabled groupchat behavior. | `w.timeline.viewed`, `w.digest.post.scraped`, `w.admin.digest_handles.updated`, `w.capabilities.viewed` |
| WTF LIVE standalone app | Public/session | Open the signed-in WTF LIVE host dashboard at `/live` inside AppWindow; create public rooms, create private WTF-user rooms with owner-managed access lists, copy guest URLs for public rooms, browse open rooms, browse accessible private rooms, see whether public rooms are quiet or active with current live user counts derived from room WebSocket presence, see Close/Reopen/Delete controls on every owned room card, see private access-list controls on every owned private room card, and use Join to open `/live/r/:roomId` in a separate browser tab/window while the wtfOS app stays on `/live`; open room-only guest links at `/live/r/:roomId` without a WTF account for public rooms; private rooms may only read the authenticated join envelope and realtime socket when the signed-in WTF user owns the room or is on `wtf_live_room_access_members`; guest links may read only `GET /api/wtf-live/public/rooms/:roomId`, `GET /api/wtf-live/public/rooms/:roomId/messages`, and the room-scoped `/ws/wtf-live` signaling lane; open the Sharing tray testing icon and run a pre-join microphone compatibility test from a minimal drawer with a collapsible Details drawer that checks secure browser context, MediaDevices support, browser permission state when available, visible audio inputs, and a short stopped `getUserMedia({ audio: true })` probe with actionable browser/site/operating-system privacy guidance; joined users exchange mic, simultaneous camera, simultaneous screen video/audio, soundboard audio, and dedicated local media-file audio/video deck tracks through a room WebRTC mesh, choose the preferred camera/screen stage focus when both local previews are running, update a room-scoped avatar image for attendance identity, use optional push-to-talk without tearing down mic permission, see all participants in a compact single-line collapsible attendance registry above chat with lit mic indicators, peer transport-health badges, signed-in wtfOS usernames bound to the authenticated socket session, and signed-in-only WIM add-buddy controls for account-backed peers, reserve the bulk stage only for active screen/camera shares, keep mic-only and idle participants out of the stage, keep Connection, Sharing, Screens, Attendance, and Room chat reachable as transparent bento tiles as participant count grows, keep mobile display-name/join plus sharing controls as a stacked/scaled rail above the shared-screens surface without horizontal overflow or tap-target overlap, keep the mobile room vertically scrollable through attendance and chat, keep a visible mic level meter inside Sharing settings, open shared screens/cameras and local previews as movable/maximizable pop-out frames with close buttons, rearrange Connection/Sharing/Screens/Attendance/Room chat inside a transparent bento workspace with drag handles, open Testing and Settings drawers under the Sharing tray, use the enforced wtfOS Soft System receiver default room-chat font for messages whose sender did not assign a font, drag shared screen/camera/media sources together to form a screen grid, drag additional sources into that grid, reveal each grid item pop-out button only on hover/focus, pop out any core panel as a movable/maximizable frame, pin popped-out panels always on top, and pop them back into the bento so desktop stage space can scale independently, open chat images/video in a lightbox-style pop-out, preserve chat scroll position when reviewing earlier messages while showing a new-message jump control, leave the room through a visible Leave Room control that closes socket/peer/media state, request tab close through a visible Close Window control, use a compact stage-level room reaction dock to send transient applause/fire/laugh/wow/love/watching signals, open a compact chat emoji popover to insert emoji into room chat drafts, open a single chat text-style icon to reveal/collapse a temporary room-chat style panel with wtfOS Soft System font locked, color, readable 8-14 size, bold/italic, reset, and Done controls, open the signed-in Show Kit tab to save per-user soundboard clips from bounded audio files, assign safe Ctrl/Alt/Meta shortcuts with duplicate-binding feedback, preview/delete configured clips, and trigger owner-only live-room soundboard buttons/shortcuts that relay bounded audio clips to the audience with visible received status while ignoring shortcuts from chat/form focus, send styled room chat text with Enter while Shift+Enter composes a multiline message, open a signed-in-only WTF LIVE tip tray from attendance or `/tip`, select an account-backed room user and owned tip item, transfer that tip item through the in-app inventory ledger, announce the tip in room chat, and share PNG/JPG/GIF/MP4 chat attachments without gaining access to the rest of WTF OS; navigate Lobby, Room Host, Stages, Show Kit, and Skywire Link sidebar tabs with deep links `?tab=overview|rooms|stages|show-kit|skywire` plus `?room=` and `?stage=` slugs; read rollout/status from `GET /api/wtf-live/status`; list/create/close/reopen/delete rooms, list owned rooms, list accessible private rooms, manage private room access lists, list/create/close/reopen/delete owned stages, and list public stages through `/api/wtf-live/*`; read public room messages and stage broadcasts from connected Skywire users' public AT repos; private room dashboard messages are realtime-only and are not published as public Skywire room records; send public room messages and stage broadcasts through the linked Skywire identity with `rooms` / `stages` capability gates sourced from `GET /api/atproto/me`; consume a pending quoted-post snapshot from `sessionStorage` key `wtf-live:pending-quote` on send; open Skywire Settings at `/skywire?tab=account` for OAuth tier upgrades. | `wtf_live.app.viewed`, `wtf_live.lobby_presence_viewed`, `wtf_live.public_room.viewed`, `wtf_live.public_room.opened_external`, `wtf_live.public_room.joined`, `wtf_live.public_room.left`, `wtf_live.public_room.close_window_requested`, `wtf_live.public_room.media_toggled`, `wtf_live.public_room.mic_test_completed`, `wtf_live.public_room.active_video_selected`, `wtf_live.public_room.avatar_changed`, `wtf_live.public_room.push_to_talk_used`, `wtf_live.public_room.attendance_toggled`, `wtf_live.public_room.share_popout_opened`, `wtf_live.public_room.chat_media_lightbox_opened`, `wtf_live.public_room.chat_autoscroll_reviewed`, `wtf_live.public_room.chat_emoji_inserted`, `wtf_live.public_room.room_reaction_sent`, `wtf_live.public_room.peer_diagnostics_viewed`, `wtf_live.public_room.bento_reordered`, `wtf_live.public_room.sharing_testing_opened`, `wtf_live.public_room.sharing_settings_opened`, `wtf_live.public_room.default_font_changed`, `wtf_live.public_room.screen_grid_created`, `wtf_live.public_room.screen_grid_popout_opened`, `wtf_live.public_room.core_panel_popout_opened`, `wtf_live.public_room.popout_pinned`, `wtf_live.public_room.popout_popped_in`, `wtf_live.public_room.chat_popout_opened`, `wtf_live.public_room.attendance_popout_opened`, `wtf_live.public_room.mobile_controls_viewed`, `wim.friend.added`, `wtf_live.private_room.created`, `wtf_live.private_room.access_updated`, `wtf_live.private_room.joined`, `wtf_live.room.realtime_joined`, `wtf_live.room.media_state_changed`, `wtf_live.room.chat_style_changed`, `wtf_live.room.chat_message_sent`, `wtf_live.room.chat_media_sent`, `wtf_live.soundboard.configured`, `wtf_live.soundboard.clip_triggered`, `wtf_live.tip.sent`, `wtf_live.tab.selected`, `wtf_live.room.created`, `wtf_live.room.closed`, `wtf_live.room.reopened`, `wtf_live.room.deleted`, `wtf_live.stage.created`, `wtf_live.stage.closed`, `wtf_live.stage.reopened`, `wtf_live.stage.deleted`, `atproto.room.message_sent`, `atproto.stage.broadcast_sent` |
| WTF LIVE room presence sounds | Public/session | After joining `/live/r/:roomId`, hear a short ascending local chime when another participant enters and a descending local chime when another participant leaves; hear a spoken “Goodbye” plus a distinct three-note local cue when intentionally leaving or when the room socket closes unexpectedly, so kicks and connection drops are audible; do not replay sounds for the initial room snapshot or current participant; toggle Presence sounds on/off from the room connection controls and persist that browser preference in local storage. | `wtf_live.public_room.presence_sounds_toggled`, `wtf_live.public_room.disconnect_alerted` |
| WTF LIVE stage broadcasts | Session | On the Stages tab, compose a visible stage broadcast with bounded text, text/voice/video/link mode, an optional live or replay URL, and an optional pending Skywire quote; send only while the selected stage is open and the linked AT Protocol session grants the `stages` capability. | `atproto.stage.broadcast_sent` |
| WTF LIVE stage rooms | Public/session | Create a stage with owner-managed host and speaker username lists; save stage host/speaker role lists from the Stages dashboard; open a stage room at `/live/r/:stageId`; join as the owner, host, speaker, or audience; let the owner or a host edit stage role lists from inside the room; keep the room transport, chat, reactions, and watching experience shared by everyone; allow only the stage owner, hosts, and speakers to publish mic, camera, screen, or local media; disable audience share controls and sanitize audience media state at the WebSocket relay so the stage behaves like a role-gated room rather than a broadcast setup form. | `wtf_live.stage.created`, `wtf_live.stage.roles_updated`, `wtf_live.stage_room.joined`, `wtf_live.stage_room.share_blocked` |
| WTF LIVE game rooms | Session/owner/host/member | Create a WTF LIVE room with `roomKind=game`; see it represented as a game room in owned, public, and selected room cards plus the `/live/r/:roomId` public room frame; let the owner or host start Jackbox Party Pack 10 or 11 inside that game room, attach to the hosted apphost WebRTC stream, publish the hosted game video/audio as the room's shared screen, and control the game from the room frame while players stay in the same WTF LIVE room with mic/camera/chat/reactions; keep room mic/camera/screen/media permissions in the game-scoped settings bucket and allow players to watch the host's shared game screen through the normal WTF LIVE WebRTC room transport. | `wtf_live.game_room.created`, `wtf_live.game_room.host_app_opened`, `wtf_live.public_room.joined`, `wtf_live.room.media_state_changed` |
| WTF LIVE Show Kit soundboard | Session/owner | Open `/live?tab=show-kit`; save bounded audio clips as server-backed per-user presets through `GET/PUT /api/wtf-live/soundboard`; set button label, category, safe Ctrl/Alt/Meta shortcut, volume, cooldown, and durable order; preview/delete clips locally; open an owned room and trigger clips from owner-only runtime buttons or shortcuts; send clip audio through the owner's Web Audio `MediaStreamDestination` into the room WebRTC mesh while `/ws/wtf-live` relays bounded cue metadata/status; suppress shortcuts while chat or form controls have focus; enforce cooldown feedback; keep anonymous audience users able to hear/see status without exposing trigger controls. | `wtf_live.soundboard.configured`, `wtf_live.soundboard.clip_triggered`, `wtf_live.room.media_state_changed` |
| WTF LIVE smart room ownership | Session/owner/host | Search existing wtfOS users from WTF LIVE; add selected users as public/private room hosts or guests; add selected users as stage hosts or speakers; send room or stage role invites that create notification-backed invite rows; open owned public/private/stage room settings from dashboard icon buttons and from the live-room Sharing settings drawer; change public/private room guest mic, camera, screen, and media permissions; associate a saved Show Kit to a room or stage; enforce those publish permissions in the join capability envelope and WebSocket media-state relay. | `wtf_live.users.search`, `wtf_live.room.roles_updated`, `wtf_live.room.invite.sent`, `wtf_live.room.settings.updated`, `wtf_live.room.show_kit.selected`, `wtf_live.stage.roles_updated`, `wtf_live.room.media_state_changed` |
| WTF LIVE room scheduling | Session/owner/host | Open the calendar icon on an owned public room, private room, or stage; schedule a room event to the WTF calendar, open a prepared TTC event submission, or do both; persist the WTF LIVE room-to-calendar mapping with the room URL attached so hosts can promote rooms without leaving the owner workflow. | `wtf_live.room.event.scheduled`, `calendar.event.created` |
| Skywire AT Protocol bridge | Public/session/system | Open `skywire.wtfos.app`, `/skywire`, or `/skywire?standalone=1` as a public Skywire entry that renders an OVOID-inspired dark AT Protocol login screen with Handle-or-DID input, Skywire signal examples, and an AT OAuth Continue action without requiring the WTF OS login page first; the standalone OAuth lane accepts a handle or DID, recovers durable app-owned state at callback, creates or resumes a deterministic Skywire session user only after the returned DID/profile is known, shares the production session cookie across `wtfos.app` and `skywire.wtfos.app`, then returns the user to Skywire where all feeds/actions still use the normal session-bound Skywire API gates; open the native Skywire app in the default dark-mode Skywire shell; hand off new-account creation to the official Bluesky signup flow; connect or upgrade an existing Bluesky/AT Protocol account through same-window OAuth that first asks the user to choose Be Safe, Be Social, Be Heard, or Be Bold permissions plus an explicit Bluesky chat/DM add-on, reports unresolved Bluesky handles such as missing `bsky.social` accounts instead of leaving the permissions request looking stalled, returns to `/skywire?tab=account` on canonical `wtfos.app` (legacy platform hosts redirect there before OAuth starts), broadcasts completion to any already-open same-origin Skywire window, and keeps the original Skywire account/settings view as the authoritative user window instead of creating a second upgraded Skywire instance; complete OAuth by recovering app-owned metadata from the OAuth `state` token, refusing reserved shared platform actor handles such as `wtfgameshow.bsky.social`, refusing callbacks whose returned handle/DID do not match the requested or already-linked user account for chat upgrades, treating any popup/storage completion metadata as a legacy hint only until canonical `/api/atproto/me` shows durable account permission, and ignoring stale completion/storage events after durable permission is confirmed so later manual tab changes do not bounce back to Settings; provide an account-panel shortcut and chat-tab failure-state CTA that reconnects with Be Bold plus the explicit Bluesky DM add-on; persist requested tier, requested scope, granted scope, chat add-on state, DID/handle/profile/PDS, and encrypted OAuth/credential material to the exact signed-in user plus DID account row server-side so normal page refreshes, closing all windows, and deploys restore the AT session; gate Skywire actions by granted scope and surface reconnect/upgrade-required state instead of leaking raw SDK restore or upstream forbidden errors; default connected users into a Bluesky-style home timeline backed by `app.bsky.feed.getTimeline`; click a home-feed actor handle/avatar to open that actor's dedicated author-only feed with cursor pagination and a Be Social-gated Follow action on the author card; read the official WTFgameshow account tab through `app.bsky.feed.getAuthorFeed`; read a curated Tezos feed composed only from official Tezos platform/community author feeds; read normalized Bluesky feed cards with authors, timestamps, embeds, GIF/video/image media, metrics, viewer state, source links, clickable quoted-post preview cards, media-first card rendering, and cursor pagination; open any feed post into a normalized in-app thread reader backed by `app.bsky.feed.getPostThread`; dispatch any feed or thread post into one or all WTFOS SystemEvent pipelines for rewards, WTF TV, Studio/Game Studio intake, Rat Race signals, and WTF LIVE rooms/stages without writing hidden canonical PDS state; read recent Skywire pipeline history from `challenge_system_events`; recommend other WTF users who connected AT identities while excluding self-follow; list actors the connected account follows through `app.bsky.graph.getFollows`, compare the user's graph with other Skywire users' follow graphs to suggest unfollowed actors, select any Discover actor into the dedicated Actor Feed tab, and follow actors; search actors through public AppView, inspect author feeds, and follow actors; distinguish AT-compliant handles from Tezos aliases; bridge the user's preferred Tezos domain identity into handle claims; claim or verify WTF-hosted/DNS/HTTPS handles; serve public `/.well-known/atproto-did`; read search/notification feeds; compose, like, repost, open an inline reply composer from the Reply action, reply, quote posts through `app.bsky.embed.record`, receive notifications, and claim existing posts for challenge automation; resolve supported Objkt asset/token/collection/open-edition hrefs and Teia numeric or contractful token page hrefs into token, listing, and direct-buy intent previews before falling back to generic external link cards; before direct wallet buys, re-fetch the signed-in session's linked wallets and require the active signer address to be linked to that current WTF OS user; read the signed-in user's Tezos vault from indexed `wallet_holdings` rows with limit/offset pagination and total counts, defer creator-side Objkt lookup until requested, split owned tokens from created tokens, group/sort created tokens by collection contract, show token cards as catalog facts (title, creator, collection, mint date, token URL), and prefill token-specific Bluesky share posts with the same catalog facts plus token-media-backed Objkt embeds; list Bluesky chat conversations through the explicit DM add-on, open a feed post as Reply In Chat with the post author prefilled as the first recipient, resolve direct or multi-member chats with `chat.bsky.convo.getConvoForMembers`, send chat messages with optional quoted-post record embeds rendered above replies as supertext in quotation marks, attach up to four uploaded media files via signed Skywire chat-media file links, render GIF/image/video/audio attachments in the selected chat thread, and read selected chat messages through the Bluesky chat service; aggregate public `app.wtfgameshow.skywire.room.message` records from connected Skywire users' PDS repos; send public room messages into the signed-in user's PDS repo with optional quoted-post preview snapshots that link back to the quoted post; aggregate public `app.wtfgameshow.skywire.stage.broadcast` records from connected Skywire users' PDS repos; send one-way public stage broadcasts with optional WTF LIVE/replay URLs and quoted-post preview snapshots; publish WTF-native Skywire Signal records into the user's AT repo for portable quest/drop/proof/broadcast state beyond Bluesky's app surface; use Bluesky compose intent fallback without auto-posting. | `atproto.permission_tier.selected`, `atproto.chat_permission.toggled`, `atproto.chat.message_sent`, `atproto.external_signup.opened`, `atproto.account.linked`, `atproto.account.unlinked`, `atproto.profile.updated`, `atproto.actor.searched`, `atproto.actor.followed`, `atproto.handle.claimed`, `atproto.handle.verified`, `atproto.signal.published`, `atproto.room.message_sent`, `atproto.stage.broadcast_sent`, `atproto.post.created`, `atproto.post.claimed`, `atproto.post.replied`, `atproto.post.quoted`, `atproto.thread.viewed`, `skywire.pipeline.reward_queued`, `skywire.pipeline.tv_queued`, `skywire.pipeline.studio_queued`, `skywire.pipeline.rat_race_queued`, `skywire.pipeline.live_queued`, `atproto.post.liked`, `atproto.post.reposted`, `atproto.notification.received`, `skywire.token_link.resolved`, `skywire.token_listing.buy_requested`, `skywire.tezos_vault.viewed`, `w.groupchat.viewed` |
| Skywire live status and group chat | Session/system | Grant Be Heard or Be Bold the `app.bsky.actor.status` repo scope; read, set, and clear the signed-in user's `app.bsky.actor.status/self` live record for WTF LIVE URLs while noting that bsky.app beta rendering remains platform-controlled; show an owned Skywire `WTF LIVE` header badge plus live banner whenever the active record exists so the status remains visible even when Bluesky or Ovoid suppress beta live badges; open the live URL or jump back to the Signals live-status controls from that banner; create Bluesky group conversations when more than one resolved member is selected, preserve optional group names, and keep one-member replies on the direct `chat.bsky.convo.getConvoForMembers` path. | `atproto.live_status.updated`, `atproto.live_status.cleared`, `atproto.chat.group_created`, `atproto.chat.message_sent` |
| Skywire Signal starters | Session/system | Open the Skywire Signals tab after granting Be Heard or Be Bold; select standard starter presets for recent sales, live broadcasts, open drops, collector calls, and proof of work; prefill the Skywire Signal text, signal type, tags, and optional related URI before publishing to the user's `app.wtfgameshow.skywire.signal` AT repo collection; publish recent-sale signals with `market.sale` type, sale/collector/tezos tags, and an optional Objkt/Teia token URL. | `atproto.signal.published` |
| wtfOS user sites | Public/session/staff/system | Claim the signed-in user's original username as a `username.wtfos.me` host from `/wtf-subdomains` or the focused `/wtf-subdomains/setup` Subdomain Setup window after wallet, OAuth social, and Bluesky/WTF DID eligibility checks; keep the label stable across username changes unless staff migrates it; edit the root page plus up to five named pages in the WTF Domains page editor; attach only owned media-library assets within the 100 MB site cap; sandbox-preview draft HTML; immediately publish an immutable version with digest, page list, attached media ids, DID target snapshot, actor, and audit metadata; enqueue `app.wtfos.identity.site` provenance, `app.wtfos.identity.siteSnapshot` render payload, and primary `app.wtfos.identity.siteIndex` pointer records so the `.me` renderer can serve without the app database; roll back to a previous published version; serve published pages and attached media from `https://username.wtfos.me/`, redirect unpublished claimed hosts to `/user/:username`, serve `https://username.wtfos.me/.well-known/atproto-did`, block privileged app/API/service-worker paths on user hosts, allow Caddy on-demand TLS only for registered non-suspended hosts, warn on identity proof loss, and let staff suspend or restore serving without deleting data or DID history. | `wtf_site.claimed`, `wtf_site.page_saved`, `wtf_site.page_deleted`, `wtf_site.assets_updated`, `wtf_site.published`, `wtf_site.rolled_back`, `wtf_site.suspended`, `wtf_site.restored`, `wtf_site.public.viewed`, `atproto.handle.verified`, `wtfos.atproto_outbox.enqueued` |
| Skywire Market Feed channel | Session/system | Select Skywire's `Market Feed` tab or open `/skywire?tab=market`; read public Bluesky posts from all indexed authors that match Objkt or Teia href domains through `app.bsky.feed.searchPosts` on a search-capable Bluesky AppView (`api.bsky.app` by default); locally filter normalized post text, external embeds, and rich-text facet hrefs so the channel only shows Objkt asset/token/collection/open-edition URLs or Teia objkt/token/asset URLs that Skywire can hydrate into Tezos token previews and buy overlays; return an upstream-unavailable state instead of a false quiet lane when every marketplace domain search fails. | `skywire.token_link.resolved`, `skywire.token_listing.buy_requested` |
| Skywire Hot topics lane | Session/system | Select Skywire's `Hot` tab or open `/skywire?tab=hot`; read current Bluesky trending topics from `app.bsky.unspecced.getTrendingTopics` through `/api/skywire/trending-topics`; personalize the topic ranking with the connected viewer DID when the Skywire OAuth session can be restored; render topics as selectable read-only cards; open the selected topic through Skywire's existing search-backed post-card lane with normal actor, thread, pipeline, reply, quote, token-preview, and buy-overlay affordances governed by the user's existing Skywire permission tier. | `skywire.trending_topics.viewed` |
| Skywire Chat OAuth origin binding | Session/system | Enable or change the Skywire Chat Add-on permissions through header/status or Skywire account settings; `wtfgameshow.app`, `www.wtfgameshow.app`, and `new.wtfgameshow.app` redirect to canonical `wtfos.app` before the app session or OAuth start can run; OAuth client metadata, callback URI, and hard public URLs canonicalize legacy WTF Gameshow env values to `https://wtfos.app`; OAuth state records the signed-in user, requested handle, return path, and browser origin that started the flow; OAuth start reports unresolved Bluesky handles before provider handoff; callback writes the granted scopes, encrypted access/refresh tokens, encrypted DPoP key, and chat capability to the initiating user+DID account row from the SDK saved session object, never from the live callback wrapper; callback returns to `/skywire?tab=account` on canonical `wtfos.app`, broadcasts the completion to any already-open original Skywire window, and `/api/atproto/me` returns that durable row with `session.reconnectRequired=false` instead of masking it to null or forcing an immediate reconnect. | `atproto.permission_tier.selected`, `atproto.chat_permission.toggled`, `atproto.account.linked` |
| tz2at identity proof, WTFOS PDS spine, and ecosystem analytics | Session/system | Open the native tz2at app; connect an AT Protocol DID with only the base `atproto` scope; import public tzbsky wallet proofs by reading `com.tzbsky.cryptoAddress/self` from the user's PDS without asking for new write approval; list locally verified WTF Tezos and Etherlink wallets; request the separate `repo:xyz.tz2at.identity.walletLink` scope only when the user chooses to publish a portable wallet-link proof; show the WTFOS PDS spine as a separate service for linked WTFOS DID/repo storage under `app.wtfos.*`; request a WTFOS repo only when the PDS is configured, deriving the requested handle from the signed-in wtfOS username so it matches `username.wtfos.me` user-site publishing before falling back to the canonical AT handle; provision the linked WTFOS repo when PDS health, invite policy, and provisioning flags allow it; write `app.wtfos.identity.link` to the WTFOS repo; keep canonical repo writes limited to allowlisted proofs; persist imported, published, requested, and provisioned state; normalize linked-wallet Tezos chain activity into `blockchain.tezos.*` SystemEvents so challenge, side quest, and reward automation can react to on-chain actions; enqueue `app.wtfos.activity.event` records for both the primary WTFOS repo and the user's linked WTF DID repo; preview wallet activity and search read-only tz2at replay/firehose events by type, chain, address, wallet, contract, marketplace, token, operation hash, and block range without writing chain activity into the user's canonical repo; aggregate live tz2at AT Protocol PDS repo records into a read-only ecosystem analytics suite with separate **Tezos Market** and **Etherlink Bridge** tabs: Tezos tab covers CEX ↔ user ↔ marketplace liquidity on mainnet/shadownet with market-health over selectable windows (24/48/72/96/168 hours) from tz2at replay (L1), category PDS repos, and per-CEX entity-repo reads (deterministic noun handles on `wallets.tz2at.store`), optional async CEX-wallet hydration, capital-in/out-of-CEX metrics, user-flow leaderboards (receivers/senders/retail), market-flow leaderboards (buyers/sellers/venues/routes), source-labeled records (`main-relay`, `category-pds`, `replay-mainnet`, `replay-etherlink`, `cex-entity-repo`), executive readout, PDS inventory, operator brief, entity drilldown, and analytics-to-firehose handoff; Etherlink tab isolates mainnet↔Etherlink corridor volume from replay-etherlink plus bridge-tagged Tezos rows (rollup `credit`/`debit` entrypoints, Tezos bridge/rollup/etherlink text), with L1→L2, L2→L1, internal Etherlink, and Tezos bridge-corridor totals kept in separate base units. | `tz2at.identity.viewed`, `tz2at.tzbsky.imported`, `tz2at.wallet_link.published`, `tz2at.wtfos_pds.requested`, `tz2at.wtfos_pds.provisioned`, `blockchain.tezos.token_transfer_in`, `blockchain.tezos.token_transfer_out`, `blockchain.tezos.token_mint`, `blockchain.tezos.contract_call`, `wtfos.atproto_outbox.enqueued`, `wtfos.atproto_outbox.published`, `tz2at.firehose.previewed`, `tz2at.firehose.searched`, `tz2at.ecosystem.analytics_viewed`, `tz2at.ecosystem.etherlink_bridge_viewed`, `tz2at.ecosystem.analytics_refreshed`, `tz2at.ecosystem.scope_changed`, `tz2at.ecosystem.entity_selected`, `tz2at.ecosystem.entity_scoped`, `tz2at.firehose.entity_opened`, `app.interaction.tracked` |
| CRP nomination AppView | Session | Desktop app key `crp-nominations` at `/crp-nominate` (desktop icon, Start Menu Social, admin gate). Open the CRP Nominations app; resolve a nominee from Tezos wallet, `.tez` domain, X handle, or Bluesky handle by merging Objkt, TzKT, Tezos Domains, tzprofiles, tz2at identity links, tzbsky wallet proofs, and linked wtfOS user data; refine the exact wallet/X/Bluesky combination; choose an official Tezos Commons CRP category; optionally attach justification links and summary; optionally submit anonymously so the CRP repo record omits nominator identity, skips the nominator's wtfOS repo echo, and does not appear in My nominations while still recording a privacy-preserving nomination credit for rewards (`crp_appview_nomination_credits`: user id only, no nominee/category/timestamp); publish canonical `app.wtfos.liveops.crpNomination` facts plus Bluesky-native `app.bsky.feed.post` share records into the dedicated CRP nominations repo; echo `app.wtfos.index.ref` pointers into the nominator's wtfOS user repo and master index when not anonymous; list attributed nominations from the AppView read model; read anonymous nomination credit counts; open Objkt-style X/Bluesky compose intents (Bluesky intent includes the published bsky.app post URL because intent links only support `text`, not embed params). Gamma hosts the resolver, nominee refinement cards, category/justification form, submission result, and My nominations list inside Gamma chrome without changing AppView APIs, event logging, MCP scope behavior, or external share intents. Paired MCP agents may list categories, resolve nominees, read repo status, list attributed nominations, read anonymous credit counts, and submit nominations through scoped tools (`crp-nominations:read`, `crp-nominations:write`) with per-token rate limits; all MCP activity is attributed to the token owner's user account. Docs: `docs/crp-nominations-user-manual.md`, `docs/crp-nominations-builder.md`. | `crp.nomination.viewed`, `crp.nomination.resolve`, `crp.nomination.submitted`, `crp.nomination.submitted.anonymous`, `crp.nomination.share_x`, `crp.nomination.share_bsky`, `crp.nomination.user_echo`, `crp.nomination.bsky_post`, `mcp.crp.read`, `mcp.crp.nomination_submitted`, `wtfos.atproto_outbox.enqueued`, `app.interaction.tracked` |
| W groupchat (legacy) | Session/staff | Disabled in digest mode (410). Legacy stream/DM/groupchat surfaces only when `W_TIMELINE_INGEST_MODE` is not digest. In legacy mode, staff-selected gameshow conversation settings require a bounded JSON-array schema, reject malformed or oversized selections with a clear 400 response before any X lookup, and retain optimistic-lock conflict protection. | `w.groupchat.viewed`, `w.admin.groupchat.updated` |
| W social/settings (legacy) | Session/staff | Digest mode exposes read-only capabilities only. Legacy follow/spaces/stream diagnostics when not in digest mode. | `w.capabilities.viewed`, `w.follow.created`, `w.diagnostics.viewed` |
| Dicksword/Discord | Public/session/bot/staff | View config/commands; view signed-in Discord identity/activity/avatar; create claim; bot proves claim; select avatar layers; staff manage avatar layers/conflicts/role mappings; bot ingests activity, serves role sync/profile. | `discord.claim.created`, `discord.claim.proven`, `discord.avatar.selected`, `discord.activity.ingested`, `discord.role_sync.pulled`, `xp.awarded` |
| I Hate Telegram | Session/bot/staff | Read approved Telegram digest sources; filter messages and FART NOISES alerts; save wallet tracks for FART readiness; signed Telegram bridge ingests updates; staff curates sources and queues WTF announcements for approved Telegram lanes. When opened through Gamma, the reader digest, status strip, source/kind filters, FART wallet tracking, staff source form, announcement form, and queued announcement list render inside Gamma-owned regions while preserving existing `/api/telegram-digest/*` reads, writes, bot ingestion, and external Telegram links. | `telegram.digest.message_ingested`, `telegram.announcement.queued`, `telegram.fart.mirrored`, `fart_noises.track.requested`, `notification.viewed` |
| WebSocket realtime | Public/session | Receive board and Studio live events over the authenticated `/ws` channel; send Studio presence/cursor messages over authenticated realtime; use public `/ws/wtf-live` only for room-scoped WTF LIVE presence, WebRTC signaling, media-state events, game-room media-state relay keyed by `roomKind=game`, bounded chat-style payloads, unstyled room-chat payloads that preserve receiver default font choice, chat/media relay, allowlisted room reaction relay, and owner-gated bounded soundboard clip relay; use authenticated `/ws/apphost` for remote-application room presence, apphost signaling relay, and low-latency pointer/wheel/keyboard input forwarding. | `websocket.connected`, `websocket.message.sent`, `studio.presence.updated`, `board.realtime.received`, `wtf_live.room.realtime_joined`, `wtf_live.room.media_state_changed`, `wtf_live.room.chat_style_changed`, `wtf_live.room.chat_message_sent`, `wtf_live.room.chat_media_sent`, `wtf_live.public_room.room_reaction_sent`, `wtf_live.soundboard.clip_triggered` |

## Concern: Media, Creation, Gallery, and Preservation

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Public gallery | Public | Browse public gallery; open token detail paths; inspect metadata and media preview; when opened through Gamma, keep `/gallery`, `/gallery/token/:contract/:tokenId`, and `/token/:contract/:tokenId` inside Gamma-owned gallery chrome with survival-token grid, slideshow, preview, and notice regions while preserving the existing public gallery behavior. | `gallery.viewed`, `gallery.token.viewed` |
| My Gallery | Session | Browse owned linked-wallet tokens; search/filter/sort; open token detail; import playable media to My Videos/Photos/Games/Music; preserve artifact. | `my_gallery.viewed`, `token.detail.viewed`, `media.token_imported`, `token_archive.requested` |
| Media library | Session/owner; linked Tezos wallet and user-owned Pinata account for minting | List media by category; view metadata; import token media with provenance; upload files to object storage; automatically back new uploads up to the owner's connected Google Drive while retaining object storage as playback authority; retry a failed or missing Drive backup; stream own file; edit credits/title; inspect usage; delete with cascade awareness; and start/resume a destination-aware Mint Manager workflow for owned upload media. Mint Manager keeps Pinata credentials session-only, persists non-secret draft state, delegates specialized Pasta origination/mint forms through expiring CH-EASE handoffs, and verifies completion from exact TzKT operation and token-transfer evidence. Once an applied linked-wallet operation is known, its explicit network, signer, contract, token, amount, artifact URI, and pending/applied indexer status persist against the owned media item; reopening it in another signed-in session recovers the authoritative receipt without trusting browser storage, and an indexer delay instructs the artist to check again without signing twice. | `media.listed`, `media.viewed`, `media.imported`, `media.uploaded`, `media.drive_backup.completed`, `media.drive_backup.failed`, `media.file_served`, `media.updated`, `media.usage_viewed`, `media.deleted`, `media.mint_manager.opened`, `media.mint_manager.destination_selected`, `media.mint_manager.media_pinned`, `media.mint_manager.publisher_handoff_opened`, `media.mint_manager.receipt_verified` |
| My Videos/Photos/Music | Session/owner | Browse libraries; import owned tokens; connect the same personal Google Drive used by Studio; upload files with automatic account-owned backup; retry a Drive backup; edit upload-only credits; play audio/video; open Mint Manager for ready owned photos/videos; add/detach video to TV channel; toggle bumper assignment; delete. | `media.video.played`, `media.audio.played`, `media.drive_backup.completed`, `media.drive_backup.failed`, `media.mint_manager.opened`, `media.tv_added`, `media.tv_detached`, `media.bumper_toggled` |
| Creator runway | Session | Open canonical Classic `/create` by outcome—make an image, animation, 3D work, or game; continue a project; preserve/export; or mint/publish—then inspect all sixteen specialist tools with a plain-language description of what each makes and at least one truthful owned-Media, device-download, project-handoff, portable-site, or on-chain-publish destination before launch. Keep challenge-specific Mint Portal available without making challenge language the only route to minting. | `creation.runway.viewed`, `creation_tool.opened`, `creation_tool.exported`, `media.mint_manager.opened`, `media.mint_manager.receipt_verified` |
| Tezamp | Session | Open music player; play local/library audio and jukebox item audio; open Winamp Bootloader; when opened through Gamma, keep the legacy Tezamp deck, visualizer, queue, selected audio player, and My Music handoff inside Gamma-owned presentation chrome while preserving the shared `/api/media/mine?category=audio` library behavior. | `tezamp.opened`, `tezamp.track_played`, `tezamp.bootloader.opened` |
| colleKT duplicate art scanner and bridge | Session | Enter any Tezos wallet address; scan live mainnet FA2 balances for tokens held at least twice; fail closed unless decimals are zero and total supply is known at 5,000 or fewer; inspect editions owned versus supply, acquisition cost/date/source, last sale, floor, delta, sales, and pricing provenance; filter and sort results; connect a personal wallet; preview the exact token, target owner, quantity, signer, and WTF unit offer; approve WTF escrow and place the offer through the canonical wtfOS marketplace; retain the linked-wallet gallery and standalone colleKT bridge; keep all surfaces inside Gamma-owned chrome when opened through Gamma. | `collekt.session.viewed`, `collekt.tokens.viewed`, `collekt.launch_requested`, `collekt.duplicates.scanned`, `collekt.offer.terms_previewed`, `collekt.offer.placed` |
| Creation tools | Session for Macaroni; trusted creator for hosted wtfOS resources | Open Studio, Game Studio, Mint Portal, Broot (`/tools/broot`) with top-level Open support for Broot `.broot` project files that are JSON documents under a vanity extension, backward-compatible `.json` project imports, image/GIF/video media imports as canvas layers, compiled static app runtime without browser Babel, local FFmpeg.wasm-assisted neutral-by-default MP4 export with explicit animation modes when the wasm runtime can load, glfx-powered distortions for the active layer or whole canvas, default-window-visible tool/layer panels, keyboard focus and layer actions, undo-backed grouping/ungrouping plus raster merge/flatten layer operations with confirmation for destructive full-canvas changes, persistent Tezos wallet restore after refresh, connected-state wallet controls that do not request permissions again, and Mainnet HEN FA2 prepare/review/sign minting of the current canvas through the user's wallet with user-paid gas/storage, PArticle Painter, INDUSTR1ALIZER, Paul's Particles V1.0, Nikshumika Paint, Kandinsky Composer, PixelPatterns, PenRose Backgrounds, CH-EASE (`/tools/ch-ease`, legacy `/tools/macaroni-packager`), Colander (`/tools/colander`), Pasta Protocol studios (`/tools/spaghetti`, `/tools/gnocchi`, `/tools/ravioli`, `/tools/rotini`, `/tools/penne`, `/tools/lasagna`), and Macaroni through the Start Menu `CREATE!` category and vendored iframe/static bundles where applicable; Gamma hosts Broot, PArticle Painter, INDUSTR1ALIZER, Paul's Particles V1.0, Nikshumika Paint, Kandinsky Composer, Macaroni, PixelPatterns, and PenRose Backgrounds through the shared creation-tool iframe shell with The Count admin-puppet coverage, route-local Gamma chrome, and no React95 desktop fallback; Macaroni lets any signed-in wtfOS user originate blind-mint contracts, choose Macaroni V1 or V2 contract templates, define per-token edition quantities, configure optional V2 minter royalty pool/split/updater policy, and attach multiple unrevealed placeholder images for delayed reveal, shows hosted wtfOS IPFS pinning only when the signed-in account has effective `trusted_market_creator` access, keeps the `Publish to wtfOS` control visible but greyed and disabled for a confirmed signed-in user without that access, explains on mouse hover and keyboard focus that the Trusted Market Creator role is required and directs the user to the Contact Admin app, enables wtfOS subdomain publishing for approved creator accounts, keeps own-Pinata/own-node pinning and standalone website export as the normal non-trusted path, mints short-lived same-origin upload tickets before hosted wtfOS pinning and can POST the actual artifact bytes to a DNS-only direct upload origin at `/api/macaroni/ipfs/upload` to bypass Cloudflare request body caps while keeping the direct host limited to that bearer-ticket endpoint, exposes configured no-prereq native installer packages for Mac/Windows/Raspberry Pi when available, builds those packages through the Macaroni Desktop Installers workflow, and the native app serves the bundled Studio locally while blocking wtfOS hosted pin/publish APIs and exporting drop sites to the user's Documents/Macaroni/site folder, defaults generated IPFS gateway config to Fileship, enforces a 1 GB per-artifact hard max, a 250 MB average artifact limit, and an OBJKT-compatible <=1 MB square JPG/PNG collection logo/cover, generates OBJKT-sized per-token previews for GIF/video artifacts and uses the collection cover only for other non-image token display/thumbnail metadata, verifies the Shadownet rehearsal RPC and wallet chain before deploy/sync/mint signing, shows sandbox-safe inline validation/deploy feedback instead of native browser modals while keeping Shadownet and mainnet deploy/sync signing on the same chain-guarded path, treats optional royalty/treasury fields as connected-wallet defaults unless the creator enters a valid Tezos address, shows the Beacon wallet picker with Kukai and Temple options on explicit connect from the embedded `/tools/macaroni` iframe, allows the selected wallet popup to escape the creation-tool sandbox, routes selected Kukai pairing to `shadownet.kukai.app`, can start a new drop by clearing the current browser draft without disconnecting the wallet, restore Studio projects from backup JSON, visibly save sale-stage edits, styles generated pages only through bounded theme, hex accent, known font controls, and creator-editable social-share copy/handle controls, exports the standalone mint site as a draft-capable package for self-hosting, or publishes the mint page to `username.wtfos.me/drop-title` for trusted creators only after the drop has a valid deployed or resumed `KT1...` contract, immediately flushes the matching PDS outbox records, and distinguishes a live URL from a PDS-delivered-but-pending `.me` serving state; generated Macaroni mint pages restore a previously connected Beacon wallet only after live active-account/network validation, align the Beacon active-account RPC to the configured Macaroni RPC before signing, derive explicit wallet operation fees from the padded gas limit actually sent through Taquito/Beacon, provide a clean disconnect, prevent duplicate connect/request-permission flows from repeated clicks or stale passive restore, expose minter royalty metadata sync/lock status when V2 minter royalties are enabled, allow WalletConnect relay websocket and verify-frame CSP traffic on published user-site hosts, serve stored published Macaroni snapshots through the current shared mint-page runtime when possible, expose accessible landmarks/status regions/progress and quantity controls, label a one-stage live sale as `Mint is Live` and multi-stage live sales as `Currently on Sale Stage X of N`, title the connected-wallet gallery as drop tokens, show minted-this-stage versus max-per-wallet and currently-owned counts separately, order wallet cards with tokens minted by the connected wallet first and tokens owned from other minters second, add prefilled X and Bluesky compose share buttons on wallet token cards that mention the creator's matching platform handle when available and include the token media URL, add blind-mint-level X and Bluesky compose buttons that share the current sale stage, mint cost, per-wallet/access terms, supply, drop URL, and collection cover image URL, clamp the requested mint quantity to live collection remaining supply plus the connected wallet's remaining per-wallet/allowlist allowance, distinguish wallet-returned operation hashes from node-visible mempool/indexer acceptance, classify mint confirmation polling timeouts by operation hash as applied, failed/backtracked, or not confirmed before showing safe retry guidance, show stage max-per-wallet and wallet balance/cost status near mint controls, reload the connected wallet's currently held tokens and wallet-minted token history from TzKT after reconnect/refresh, and show a public Recent mints section that reads TzKT mint transfers, refreshes token metadata from contract storage/IPFS after reveal, resolves minter identity through Objkt and TzKT where available, serves token previews through the Fileship gateway fallback, and renders video artifacts when older metadata only supplies the collection cover as preview; published snapshot compatibility also repairs custom recent-mint grids by replacing mint-count-derived token IDs with actual TzKT mint transfer token IDs. | `creation_tool.opened`, `creation_tool.asset_loaded`, `creation_tool.exported`, `macaroni.installer_manifest.viewed`, `macaroni.desktop.exported`, `ipfs_pinning.storage.staged`, `ipfs_pinning.pin.completed`, `wallet.provider.preflight.succeeded`, `wallet.account_mismatch.blocked`, `blockchain.tezos.token_mint`, `macaroni.recent_mints.viewed`, `macaroni.drop_published` |
| PixAlerce alpha editor | Session; wallet + user-owned Pinata account for media mint | Open `/tools/pixalerce` from the desktop or Start Menu; create and edit a blank 3D pixel canvas with every top-level tool; use stamps, motion presets, palette, inspector, preview, and device camera input; save/reopen named projects through localForage/IndexedDB; export and validate PNG, GIF, MP4, WebM, and a network-independent OBJKT ZIP through unobstructed controls; choose wtfOS Media, device download, or Media + Mint Manager as the explicit export destination; persist owned exports through `/api/media/upload`; find image/video exports later in My Photos/My Videos and other formats in File Manager; and reopen the same resumable Mint Manager from persisted owned media. Choose HEN/Teia, an Objkt-ready Pasta collection, a compatible wallet/Colander-associated contract, or a new Pasta standard/open-edition contract before entering destination-specific token metadata. A personal Pinata JWT remains only in the open dialog and is cleared after pinning. HEN identifies the creator from the shared wallet session, prepares CIDv0 artifact and TZIP-21 metadata without a signature, reviews the exact Mainnet wallet/contract/CIDs/editions/royalties/fees, and fails closed unless the shared wallet and RPC prove Tezos Mainnet before one `mint_OBJKT` send. Pasta destinations transfer the pinned media through the existing expiring CH-EASE handoff, keep specialized origination/supply/sale forms and signatures in their owning publisher, then require `/api/mint-manager/receipt` to match an applied operation from a linked wallet with its indexed mint transfer. The vendored build is isolated under `/creation-tools/pixalerce/`, does not register or update a service worker, and stays alpha-test-only until Niko's redistribution license and complete stamp provenance are recorded. | `creation_tool.opened`, `creation_tool.asset_loaded`, `creation_tool.exported`, `media.mint_manager.opened`, `media.mint_manager.destination_selected`, `media.mint_manager.media_pinned`, `media.mint_manager.publisher_handoff_opened`, `media.mint_manager.receipt_verified`, `blockchain.tezos.token_mint` |
| PArticle Painter Teia mint | Session/wallet + user-owned Pinata account | Open `/tools/particle-painter`; render a GIF or WebM from the local frame buffer; enter a personal Pinata JWT in the mint dialog as a password field; keep that credential only in the open dialog, send it directly from the browser to Pinata for the artwork and TZIP-21 metadata, never persist it or compile it into the public bundle, then review/sign the Mainnet HEN/Teia mint with the connected wallet and inspect the returned TzKT operation. | `creation_tool.opened`, `blockchain.tezos.token_mint` |
| Macaroni V3 finalized sealed allocation and commitment reveal | Wallet for setup; authorized reveal operator or contract administrator recovery | Select instant or delayed V3 reveal in hosted or native Studio. Studio generates a private 32-byte nonce per final metadata URI, syncs only its SHA-256 commitment, and permanently finalizes token rows, quantities, commitments, and deterministic creator-defined allocation order before any sale stage opens. Studio and collector pages explicitly state that this is sealed creator ordering, not provably random selection. Registration recomputes every preimage and matches the complete finalized on-chain inventory after the administrator wallet signs a one-time network/contract-bound challenge. The operator reveals a 1/1 after mint or an edition only after every copy is allocated; delayed mode additionally waits for the contract window. The contract rejects pre-finalization sale/mint, post-finalization inventory edits, partial-edition reveal, unauthorized, early delayed, mismatched, and repeated reveals. | `blockchain.tezos.token_mint`, `creation_tool.exported`, `pasta_protocol.contract_recorded`, `colander.contract_action_submitted` |
| Macaroni drop wallet connect | Public/session/wallet | Generated Macaroni mint pages and stored published drops such as Airporters load the same audited Octez Connect v5 browser bundle before the shared wallet runtime, prefer the Octez Connect dApp session path for wallet pairing, keep Beacon as backup on the patched WalletConnect transport, enable mobile/QR pairing only when the deployment supplies its own `VITE_WALLETCONNECT_PROJECT_ID`, stop blocking the wallet controls as soon as a stored session is validated, use bounded primary/fallback reads for chain and balance refresh, keep Taquito/Octez RPC and chain-id preflight separate from wallet permission identity, and send plain named networks for Mainnet/Shadownet instead of attaching the dApp RPC URL to wallet network identity. | `wallet.provider.preflight.succeeded`, `wallet.provider.preflight.failed`, `wallet.account_mismatch.blocked`, `macaroni.drop_published` |
| Macaroni CH-EASE packages | Session/trusted creator/staff | Create wtfOS-stored Macaroni handoff packages through `/api/macaroni-packages`; upload numbered artifact files while preserving original basenames as token titles; edit per-token metadata and attributes; configure drop-page layout modules; finalize packages; pin CSV/manifest through shared IPFS pinning; download platform CSV exports; load finalized packages as Macaroni Studio sources; open from `/tools/ch-ease` or legacy `/tools/macaroni-packager`; when opened through Gamma, keep the native CH-EASE packager chrome, package queue, media grid, inspector, drop preview, and internal Studio/Domains/IPFS/Macaroni/Pasta handoffs inside the Gamma presentation shell while leaving export downloads, package APIs, sessionStorage package payloads, and feature behavior unchanged. | `macaroni.package_created`, `macaroni.package_item_uploaded`, `macaroni.package_metadata_updated`, `macaroni.package_drop_config_updated`, `macaroni.package_finalized`, `macaroni.package_csv_downloaded`, `macaroni.package_export_downloaded`, `macaroni.package_source_loaded`, `macaroni.package_handoff_opened`, `ipfs_pinning.storage.staged`, `ipfs_pinning.pin.completed` |
| IPFS Pinning Manager | Session + WTF Pin Collector + active PDS/site for broad scopes | Open canonical `/ipfs-pinning` or legacy `/apps/porcupin-setup` and `/apps/porcupin-dashboard` aliases; buy/use WTF Pin Collector role `wtf_pin_collector` via market SKU `wtf-pin-collector-pass` with legacy `wtf-autopin-membership` still honored; inspect active PDS repo, `username.wtfos.me` host, `wtf.tez` aliases, hosted provider health, Hetzner Object Storage/S3 status, Storage Box manifest mirror status, quota, jobs, PDS publish state, and restore proof; expose non-secret pin-home readiness through `/api/wtf-sites/my` for live Pasta inventory so operators can distinguish page-publish prerequisites from pin-recovery prerequisites before writes; enable whole-wallet backup after wallet entry and public PDS record disclosure; publish Pasta Protocol project-bundle recovery through `POST /api/ipfs-pinning/pasta-protocol/publish` only after the site is published, PDS/wallet are ready, object storage is reachable, and an existing live manifest is not already in flight; pause future scans, exclude items/collections, retry failed jobs, export manifests, and open AT record/well-known pointers. When opened through Gamma, keep the canonical manager and Porcupin setup/dashboard aliases, status grid, wallet backup controls, PDS/restore panel, jobs table, footer, and internal Domains/WTFIAM/Porcupin handoffs inside Gamma-owned presentation chrome while leaving `/api/ipfs-pinning/*`, policy payloads, job retries, PDS records, and storage behavior unchanged. Core stores policies/jobs/quota/provider state and AT URIs only; Hetzner S3 stores durable bytes and manifests under `ipfs-pinning/users/{userId}/...`; hosted Porcupin uses `/mnt/wtf-data/workers/porcupin`; Storage Box mirrors critical manifests/proofs only; user WTFOS PDS stores public `app.wtfos.media.pinPolicy`, `pinManifest`, and `pinItem` records; master/domain spine stores only `app.wtfos.index.ref` pointers. | `ipfs_pinning.policy.saved`, `ipfs_pinning.wallet_backup.enabled`, `ipfs_pinning.storage.staged`, `ipfs_pinning.pin.completed`, `ipfs_pinning.pds_record.queued`, `ipfs_pinning.pds_record.published`, `ipfs_pinning.subdomain_registry.linked`, `ipfs_pinning.restore_proof.created`, `porcupin.connect`, `porcupin.premium.check` |
| Game Studio templates/assets | Public/session | Fetch templates, targets, stock assets, vetted CC0 imported asset packs, snippets, raw stock asset files, and scaffold source files; when opened through Gamma, keep the template rail, snippet recipes, asset filters, asset cards, upload drop zone, selected-asset inspector, and preview/source handoffs inside Gamma-owned presentation chrome without changing asset URLs or scaffold APIs. | `game_studio.template.viewed`, `game_studio.asset.viewed`, `game_studio.asset_pack.checked`, `game_studio.snippet.viewed`, `game_studio.scaffold.created` |
| Game Studio projects | Session/creator/agent | Create/update/load/list projects; save source files, selected stock assets, local assets; enforce local-asset MIME/size/base64 limits; list builds; build server ZIP; submit/update an attributed Arcade game; confirm trusted-creator publication in the public catalog/detail and derived Arcade creator statistics; when opened through Gamma, keep project cards, toolbar, preview iframe, source editor, build history, and publishing controls inside Gamma-owned presentation chrome while preserving `/api/game-studio/*`, `/api/media/upload`, `/api/arcade/*`, SDK preview sandboxing, and explicit asset external links. | `game_studio.project.created`, `game_studio.project.updated`, `game_studio.build.started`, `game_studio.build.succeeded`, `game_studio.build.failed`, `game_studio.submitted_to_arcade` |
| Studio workspace | Session/permission | Create/list/open projects by Tezos use case and explicit Shadownet/mainnet target; follow a shared Concept → Collaborate → Create → Refine → Release → Activate runway; persist phase, checklist, CID, KT1, WTF LIVE room, and release-URL evidence; carry project context into the existing WIM conversation, Broot, IPFS Pinning, Pasta Protocol, Mint Portal, and WTF LIVE owner surfaces; connect/disconnect Google Drive; refresh usage; upload files/folders; preview/open original/rename/delete review files; accept Skywire post context queued through the WTFOS SystemEvent pipeline; create/resolve/reopen/delete annotations; mark up images with Paint 95 brush/highlighter strokes; comment; chat; pin/unpin; invite members; presence; when opened through Gamma, keep the runway, project list, Drive panel, project cards, file tree, preview/annotation stage, chat, members, invite picker, and project handoffs inside Gamma-owned presentation chrome without changing wallet, mint, pinning, storage, or permission authority. | `studio.project.created`, `studio.project.opened`, `studio.workflow_updated`, `studio.file.uploaded`, `studio.file.renamed`, `studio.file.deleted`, `skywire.pipeline.studio_queued`, `studio.annotation.created`, `studio.annotation.resolved`, `studio.chat.sent`, `studio.member.invited` |
| Studio storage/admin | Staff/session | Configure platform/user Drive; connect/disconnect admin drive; refresh quota/usage; set root folder; inspect backend/dependent projects. | `studio.storage.config_viewed`, `studio.drive.connected`, `studio.drive.disconnected`, `studio.drive.quota_refreshed`, `studio.root_folder.set` |

## Concern: Pasta Protocol

Ravioli mode-state safety (domain: Pasta Protocol; subdomain: atomic pack publisher; access: session, with `trusted_market_creator` required only for the hosted wtfOS pinner): when `/tools/ravioli` switches from a blind Limited Edition mode back to deterministic-vault mode, it clears and disables the blind-only reveal and open/refund deadline fields so hidden values from a prior product cannot contaminate the next pack. This behavior remains owned by the canonical `ravioli.pack_published` workflow and the `pasta-protocol.ravioli-limited-edition-expiry-deconfliction` behavior assertion.

Ravioli recovery-command safety (domain: Pasta Protocol; subdomain: atomic pack publisher proof/recovery; access: local proof operator): the superseded July 22 mode-0 writer command is retired and fails with `LEGACY_RECOVERY_RETIRED` before filesystem, IPFS, wallet, or chain work; the crossed 31-event current-v2 command is permanently retired with `CURRENT_V2_RECOVERY_RETIRED`; both crossed current-v3 commands are permanently retired with `CURRENT_V3_RECOVERY_RETIRED`; and both crossed current-v4 commands are permanently retired with `CURRENT_V4_RECOVERY_RETIRED`. Their sources remain quarantine evidence only and no retired lane has a runnable package command. Current-v4 authentically advanced boundary 40 with fresh canonical pins 13–15, an external mode-0700 private recovery capture, applied `create_pack`, three recipe commits, `finalize_blind_pack`, and collector-1 `buy`, reaching exactly 61 hash-linked events, 15 durable pins, and 15 applied operations. A transient collector-2 buyer-page read then timed out before wallet connection or submission; both RPCs proved exact terminal actor counters and empty matching mempools, and the immutable sale later expired. That boundary is partial audit evidence and may never be replayed. Any replacement must use a new run root and fresh Gnocchi, Rotini, controller, and router contracts. Fresh live execution now requires a disjoint existing mode-0700 private recovery directory, externalizes every blind precommit before its first signer PREPARED boundary, captures again on handled browser failure, and excludes every private byte/path from public proof. This internal recovery surface emits no new public interaction handle; it remains owned by `ravioli.pack_published` and the `pasta-protocol.ravioli-limited-edition-expiry-deconfliction` assertion.

Ravioli deadline-first continuation safety (domain: Pasta Protocol; subdomain: atomic pack publisher proof/recovery; access: local proof operator): replacement journal `28fd890cb0a8a136cebad8cb8280184da8f65d70a4f3777e15daa65320dbbb10` continued without replay from its 59-event stop, applied the collector-one → collector-two claim-preserving wrapper transfer, and reached a 79-event boundary with 15 pins, 21 applied global operations, and an authorized Gnocchi allocation adapter. Equivalent `Z` and `.000Z` timestamps must be compared as instants; their former textual mismatch stopped mode-2 publication before `create_pack`. For an already-issued product approaching an immutable deadline, the proof operator may execute the exact Studio transition as an authenticated deadline-first sidecar: mode-1 `set_pack_contents` applied before its reveal cutoff and screenshot 012 reloaded the permanent reveal from chain. Operation-hash reconciliation selects the unique root by hash, signer, top-level target, and entrypoint while retaining returned internal transactions as evidence. The subsequent holder-settlement guard observed the expired open cutoff and stopped before intent, signer, or chain activity, so this product proves publication, purchases, wrapper transfer, and reveal but does not claim atomic opening/delivery. A fresh long-window product must prove that interaction. These operator-only safeguards add no public handle; they govern `ravioli.wrapper_transferred`, `ravioli.contents_revealed`, `ravioli.pack_opened`, and the existing `pasta-protocol.ravioli-limited-edition-expiry-deconfliction` assertion.

Ravioli green/red deadline separation (domain: Pasta Protocol; subdomain: atomic pack publisher proof; access: local proof operator): fresh positive-path products use configurable long windows—24 hours of sale runway, reveal 48 hours after sale, and open/refund seven days after sale by default. The mode-1 funded-pool proof issues and sells exactly two of two wrappers so its authenticated reveal can proceed immediately instead of sleeping until a long sale expires. Expired-permission behavior belongs to a separate token-5 fixture that issues two wrappers, sells one, uses an explicit five-minute sale and seven-minute reveal cutoff, and must first prove a late creator reveal is rejected through read-only Shadownet simulation before holder refund and permissionless closure. Limited Edition children retain the stronger child-before-pack rule and must also leave at least the configured green sale runway. This proof-only timing policy adds no public handle; it governs the existing `ravioli.pack_published`, `ravioli.contents_revealed`, `ravioli.refund_credited`, and `ravioli.unrevealed_pack_cancelled` workflows.

Gnocchi recovery-command safety (domain: Pasta Protocol; subdomain: open-edition publisher proof/recovery; access: local proof operator): the exact `pasta-alpha-proof-20260724t015728z` / `KT19dHuzHkqzvC3CgobLoTLbars792TFm87j` boundary recovered three already-applied prefix operations and continued with nine new operations under a 46-event checkpoint, two durable pins, and zero replay. Its signer-free preflight performed no chain, IPFS, signer, or local writes; its GET-only finalizer classified the complete 19-screenshot evidence as `UI-LIVE-RECOVERED-CHECKPOINTED`. The crossed one-off writer is retired and cannot be treated as a reusable product command. This operator-only proof surface emits no new public interaction handle; ownership remains with `gnocchi.edition_published`, `gnocchi.edition_minted`, and `pasta-protocol.gnocchi-multi-edition-collection`.

Pasta Protocol is the Tezos publishing-app suite. Each specialized app compiles, configures, deploys, and exports one kind of contract-level or token-level product and shares the proven Macaroni wallet/deploy/export runtime. Every token-producing app compiles the canonical TZIP-12 comb layouts for balance, transfer, and operator entrypoints and must be independently classified and indexed as FA2 before its live proof can pass. Colander is the central local-first project workspace and contract-management console: it routes intent to the correct standalone app, preserves a portable project manifest, receives deployed contract addresses back from publishers, and retains its adapter-driven ownership graph and signed controls. Every shared self-hosted page renders only the controls owned by its configured publisher; Ravioli opening and Rotini artifact-pinning panels remain absent from the visible interaction surface for unrelated apps. When opened through Gamma, the publisher iframes keep their shared static iframe wrapper, provenance header, and sandbox inside Gamma-owned presentation chrome while preserving all wallet, contract, adapter, event, and static asset behavior. Macaroni itself remains independently usable and is tracked under Media, Creation, Gallery, and Preservation.

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| CH-EASE prep | Session/trusted creator/staff; local in Pasta Suite Desktop or CH-EASE Desktop | Format uploaded media and metadata into target-app collection or single-token packages and export them via `/tools/ch-ease` (legacy `/tools/macaroni-packager`); in the installer or direct static app, prepare the same versioned package entirely on-device, autosave metadata in a project-scoped draft without file bytes, recover it after reload with a file-reselection warning, pin loaded originals through the creator's own Pinata account or Kubo HTTP API without persisting credentials, write returned CIDs into the matching items, download JSON or a ZIP containing selected original media, and require durable artifact URIs before opening the chosen Pasta publisher with a same-origin handoff; install CH-EASE independently on macOS, Windows, or Raspberry Pi with all six publisher runtimes included locally as handoff dependencies and expose downloads only when a protected manifest has a safe URL plus matching SHA-256; preserve the originating Colander project id/title/network and `ch-ease` owner identity through preparation and the next publisher handoff; when opened through Gamma, preserve the Gamma route prefix for supported Pasta publisher handoffs; no origination or marketplace indexing. | `chease.target_selected`, `chease.media_pinned`, `chease.package_exported`, `chease.package_handoff_opened`, `chease.installer_manifest.viewed`, `pasta_protocol.draft_saved`, `pasta_protocol.draft_imported` |
| Spaghetti standard publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/spaghetti`; originate a standard FA2 collection or publish a token product, selecting a mint target (new collection, owned collection, HEN shared contract, or wtfOS open collection); import a CH-EASE package handoff through the shared `window.MD` runtime; use inline sandbox-safe feedback; export a standalone collection/gallery site; locally rehearse Shadownet publish choreography with an explicit chain guard before origination, create_token batching, mint batching, and canonical audit events. | `spaghetti.collection_deployed`, `spaghetti.token_published`, `spaghetti.exported`, `spaghetti.site_exported` |
| Gnocchi multi-edition publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/gnocchi`; configure an uncapped Timed OE, unbounded Forever OE, capped-and-timed Limited Edition, or advanced custom policy with flat or bonding-curve pricing; declare a creator reserve inside the lifetime issuance count; lock start/end/cap boundaries; create a new multi-token `PastaOpenEditionFA2` collection or verify administrator ownership of a current Gnocchi KT1 and append the next token id; list and select every edition in that collection with independent current supply, lifetime minted total, policy, lock, window, and vault state; import a CH-EASE package handoff; let independent collectors mint at the exact on-chain price; prevent burns from reopening a hard cap or rewinding price; let the collection administrator vault issuance without changing existing supply/ownership and later unvault the same token; and export the selected token's wallet-enabled mint page as a standalone site. | `gnocchi.collection_deployed`, `gnocchi.collection_verified`, `gnocchi.collection_editions_viewed`, `gnocchi.edition_published`, `gnocchi.edition_minted`, `gnocchi.edition_vaulted`, `gnocchi.edition_unvaulted`, `gnocchi.site_exported` |
| Ravioli atomic pack publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/ravioli`; compose deterministic vaulted bundles, blind funded-pool packs, reserved unminted Gnocchi allocations, Rotini tokens generated at open, or hybrid packs containing all three primitives; import a CH-EASE package handoff; and reuse one `PastaPackRouterFA2` plus its immutably bound `PastaBlindPackController` across many products. Before a fresh pair is pinned or originated, load the generated deployment certificate, verify the exact router, blind-controller, Gnocchi-adapter, and Rotini-adapter artifact/source identities, require its protocol to match the connected RPC head, validate protocol size arithmetic, certified metadata-URI envelope, and at least 1 KiB signed-operation headroom; reject an uncertified/stale build or oversized URI before the first pin, signature, or chain write; and explain the approximately 11.3 tez one-time storage/origination cost and normal lower-cost reuse path. For an existing router, require the exact supported router and controller code, complete entrypoint/view ABIs, connected administrator, binding, and next token id. Commit every ordered recipe and reserve all backing before wrapper issuance; require reserved Gnocchi allocations to expose an active locked issuance policy; support Timed OE, capped-untimed, Forever OE, and Limited Edition children without inventing deadlines for non-LE children; and let legitimately reserved capacity remain deliverable after a child's public mint window closes. Every blind product is itself a finite Limited Edition whose complete wrapper supply is atomically issued and listed through `finalize_blind_pack`; require `primary sale end < reveal deadline < open/refund cutoff`, and when an LE child exists also require sale end strictly before and reveal no later than its earliest public expiry. Before sale, generate every recipe nonce and an unbiased offset for the explicitly documented salted cyclic claim rotation, encrypt the complete public reveal/open kit with AES-256-GCM under a key derived from a 32-byte salt, pin a public definition containing aggregate policy/counts but no child identities, pin authenticated ciphertext, advertise only its URI/commitment in token metadata, commit its URI/salt/offset hash on chain, and save a bounded lossless private recovery journal before any signer boundary; funding and reservation operations remain public on Tezos. Fresh live proof execution requires a disjoint existing mode-0700 private directory and copies every blind precommit there before its first signer PREPARED boundary, without exposing any private byte or path in the public proof. Reveal publishes the committed ciphertext URI, salt, and rotation offset once; the standalone holder page reads authenticated status from the bound controller, rejects router/controller disagreement, unsupported envelopes, tampering, or a wrong salt, decrypts the exact v3 kit without wtfOS or a per-holder handoff, and opens only before the finite cutoff. During an active primary sale, an eligible existing holder sees Buy and Open as independent actions; page load and primary purchase never fetch reveal/open-kit content, opening resolves or accepts the kit only after the holder chooses Open, and one synchronous submission latch prevents rapid primary/secondary clicks from requesting or signing more than one operation. Live proof buyers may retry only bounded read-only page loads before wallet connection, pinning, or signing; every failed initialization closes its browser and resets its monitor. Before opening the buyer page and again immediately before submitting a timed purchase, the proof requires enough on-chain sale time for the complete retry budget plus a safety margin and derives chain-time waiting from the committed deadline. If an unrevealed pack misses its reveal deadline, or a revealed pack misses its open cutoff, the page becomes refund-only without fetching reveal ciphertext; any caller may burn an expired claim and credit its exact payment to the current wrapper holder, while only that holder can pull-withdraw credit to a chosen destination. After the reveal deadline, any connected caller may close a fully refunded unrevealed pack only when no claims or escrow remain; closure zeroes wrapper supply and unclaimed inventory while preserving existing holder withdrawal credits, and a rejecting destination rolls a withdrawal back without erasing credit. After cancellation, the connected pack administrator can open the cancellation-only Recover reserved child capacity panel, choose the exact official adapter, adapter kind, resource id, and capacity, and submit one `recover_adapter` write; Studio rejects active packs, unsupported adapter code, or a request exceeding router allowance/adapter reservation, then verifies the exact router allowance and adapter reservation decrement plus the Gnocchi target `total_reserved` decrement before reporting success. Blind-wrapper transfers move the exact top LIFO claim with the wrapper and freeze at the reveal deadline while unrevealed or at the open cutoff after reveal. For opening, resolve and recheck the holder's exact controller claim id and assigned serial; atomically burn the wrapper and complete all escrow transfers and allocated/generative mints or revert the full operation. For each Rotini action, obtain the authoritative target/project/seed from immutable pack/token/serial/action/project identity without recipient entropy, reproduce PNG, GIF, or a dependency-complete offline ZIP, independently recompute its cache hash, and describe the generator plus immutable seed as canonical while treating the rendered artifact as a reproducible self-hosted cache. After confirmation, show every delivered child, metadata/artwork links, and the TzKT operation; preserve recovery intent, hashes, addresses, and exact postconditions across interruption; expose only the selected Pinata, creator-owned Kubo, or permitted hosted pinner fields; and export the complete buyer/reveal/open/refund holder page with both Rotini runtimes for independent hosting. | `ravioli.collection_deployed`, `ravioli.bundle_published`, `ravioli.pack_published`, `ravioli.redeemed`, `ravioli.pack_opened`, `ravioli.contents_revealed`, `ravioli.wrapper_transferred`, `ravioli.refund_credited`, `ravioli.refund_withdrawn`, `ravioli.unrevealed_pack_cancelled`, `ravioli.adapter_capacity_recovered`, `ravioli.site_exported` |
| Rotini generative publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/rotini`; import CH-EASE collection context; upload weighted layer variants; choose PNG, animated GIF, or interactive ZIP output; preview deterministic composites locally; pin the reusable `pasta-rotini-generator@2` manifest and source layers; originate the dedicated `PastaGenerativeCollectionFA2`; publish a priced, supply-capped, optionally per-wallet-capped generator project without pre-minting iterations; let a collector reserve an immutable token id/seed/payment/supply slot, render and pin the finished artifact plus direct TZIP-21 metadata, and finalize the FA2 token with its exact artifact SHA-256; switch the standalone artifact pinner among Pinata, a creator-owned Kubo node, and permitted wtfOS storage while immediately exposing only the selected provider's required fields; package every interactive dependency inside a ZIP with top-level `index.html` and reject external URLs/network APIs; preserve paid finalization while generation is closed; refund expired reservations; and export the same wallet-enabled reserve/render/finalize page for marketplace-independent self-hosting. | `rotini.collection_deployed`, `rotini.generated`, `rotini.project_published`, `rotini.iteration_minted`, `rotini.package_exported`, `rotini.site_exported` |
| Penne distribution publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/penne`; configure split/claim/airdrop distribution modes; import a CH-EASE package handoff; accept Colander contract context; originate `PastaDistributionFA2`; and export the wallet-enabled collector claim flow as a standalone site. | `penne.collection_deployed`, `penne.distribution_configured`, `penne.site_exported` |
| Lasagna exhibition publisher | Session; trusted_market_creator only for hosted wtfOS pinner | Open `/tools/lasagna`; curate exhibition layouts and registry entries through `PastaExhibitionRegistry`; import CH-EASE collection context; accept Colander contract context; and export the current on-chain exhibition revision as a standalone site. | `lasagna.exhibition_published`, `lasagna.registry_updated`, `lasagna.site_exported` |
| Colander project workspace and contract management | Session | Open `/tools/colander`; create and locally persist a versioned Pasta Project manifest; rename it; duplicate its workflow/network as a clean independent project without sharing draft, contract, or site ledgers; reversibly archive it while preserving its prior stage; restore current or legacy archived work; permanently delete only from the archived state through explicit inline confirmation; choose an intended outcome and launch CH-EASE, Macaroni, Spaghetti, Gnocchi, Ravioli, Rotini, Penne, or Lasagna with project/network/contract context; import or export the active manifest; preserve that context through CH-EASE package preparation; autosave and reload unfinished work in every newer Pasta studio; export/import a portable versioned studio backup whose password and local-file fields are intentionally excluded; see recoverable studio work in Colander and resume the correct owner app; receive newly deployed or resumed KT1 addresses and self-hosted site artifacts back from Macaroni and all six newer Pasta publishers; rebuild a site artifact in its exact owner app/contract context or deliberately forget only the portable record while leaving installed bytes, ZIP downloads, and contracts untouched; automatically remember confirmed standalone deployments; verify a manually entered KT1 from chain before saving it; resume the address into the app-specific manager fields after restart; forget only the device-local reference without changing the contract; inspect structured app/network/last-verified contract records in Colander and reopen either its central manager or the owner app; recognize both Macaroni V1 and V2 blind-mint entrypoint families as Macaroni-owned contracts rather than generic FA2/Spaghetti records; advance exported projects to published; recover any KT1 directly; open adapter-specific control panels; configure and pause fixed-edition or blind-mint sales; route complex Macaroni sale/reveal editing to Macaroni, open-edition pricing to Gnocchi, and atomic pack opening/reveal management to Ravioli; submit transfers; preserve a visibly confirmed action result after the automatic post-write state refresh; update roles; and view the Wallet → Franchise → Collection → Token relationship graph. The aggregate UI-LIVE proof treats Colander as management software only: it discovers fresh same-run sibling contracts and signs an authorized management action without claiming its own contract or token. | `colander.project_created`, `colander.project_renamed`, `colander.project_duplicated`, `colander.project_archived`, `colander.project_restored`, `colander.project_deleted`, `colander.project_imported`, `colander.project_exported`, `colander.tool_launched`, `colander.site_record_forgotten`, `pasta_protocol.draft_saved`, `pasta_protocol.draft_exported`, `pasta_protocol.draft_imported`, `pasta_protocol.draft_cleared`, `pasta_protocol.contract_recorded`, `pasta_protocol.contract_verified`, `pasta_protocol.contract_resumed`, `pasta_protocol.contract_forgotten`, `colander.contract_opened`, `colander.handoff_opened`, `colander.contract_action_submitted`, `colander.transfer_submitted`, `colander.role_updated`, `colander.graph_viewed` |
| WTF.ME hosted Pasta pages | Public/session/trusted creator | Publish Pasta landing, mint, and collection pages to a claimed `username.wtfos.me` host only after read-only live inventory reports the host as claimed/claimable, expected-host matched, wallet/social/DID-ready, and pin-recovery prerequisites visible; preserve the Shadownet network and chain markers, current proof KT1s, relationship groups, wallet-connect marker, mint action marker, WTF.ME branding, user-site CSP/COOP wallet headers, and public pin discovery before treating hosted Pasta pages as production-ready. | `wtf_site.public.viewed` |
| Spaghetti desktop installers | Session | Fetch the authenticated `/api/spaghetti/installers` manifest for the standalone Spaghetti Studio Desktop app; expose configured macOS, Windows, and Raspberry Pi release downloads only when production-safe HTTPS or same-origin installer URLs plus SHA-256 checksums are configured; build those packages through the Spaghetti Desktop Installers workflow; the downloaded app serves Spaghetti locally while blocking hosted wtfOS pinning, publishing, preview-processing, and hosted-resource APIs. | `spaghetti.installer_manifest.viewed` |
| Pasta Suite desktop installers | Session | Fetch the authenticated `/api/pasta/installers` manifest for the bundled Pasta Suite Desktop app and `/api/pasta/installers/catalog` for one catalogue of the suite plus individual CH-EASE, Macaroni, Spaghetti, Gnocchi, Ravioli, Rotini, Penne, and Lasagna desktop installers, with every catalogue release tag derived from that product's configured version rather than stale static release metadata; expose configured macOS, Windows, and Raspberry Pi release downloads only when production-safe HTTPS or same-origin installer URLs plus SHA-256 checksums are configured; install through a branded universal macOS drag-to-Applications DMG or per-user Windows NSIS GUI wizard with install-location choice, Start menu/desktop shortcuts, launch-on-finish, license, checksums, and GUI-only unsigned-build fallback instructions—without Node.js, npm, Homebrew, terminal, or a separately started server; launch a local Colander start surface through cross-platform loopback-root resolution that maps `/` to the packaged `index.html` on POSIX and Windows, visibly chooses a project network, and defaults fresh project/contract work to Shadownet while keeping Mainnet an explicit opt-in, creates/imports/exports and normalizes the same versioned project manifests, renames/clean-duplicates/archives/restores/explicitly deletes projects with web-compatible lifecycle semantics, launches bundled CH-EASE, Macaroni, and every publisher with project context, preserves CH-EASE as the preparation owner, receives project-scoped preparation/studio drafts, deployed KT1s, structured app/network/verification contract records, and self-hosted site artifacts back through shared localStorage, resumes drafts and remembered contracts in their owner apps, reopens KT1s in the native wallet-safe manager, rejects malformed imported lifecycle records, opens each project-linked installed page, ingests the stored ZIP emitted by every newer publisher into `Documents/Pasta Suite/sites`, lists and serves each wallet-enabled drop/mint/gallery from a loopback `/sites/:slug/` URL, and explicitly uninstalls a slug-scoped managed page atomically while pruning every matching project artifact; build `1.0.1-alpha.1` suite and standalone packages only from a clean source revision and embed product/version/Git SHA/dirty-state/target provenance in every package; artifact-smoke every packaged macOS and Windows executable for required assets, stable-origin browser-storage persistence across quit/relaunch, and matching provenance; additionally create a Shadownet-default Colander project and launch all eight bundled tools in the suite, require current Ravioli controller/certificate and Rotini artifact/mint runtimes, verify Windows shortcuts, and uninstall the Windows package; build the suite package through the Pasta Suite Desktop Installers workflow and individual packages through their app-owned workflows; give the suite and all eight standalone shells unique stable loopback origins so localStorage workspaces, drafts, contract records, and recovery indexes survive quit/relaunch, focus the existing window on a second launch, and fail with explicit data-preserving guidance instead of silently selecting another origin when the assigned address is occupied; the downloaded apps serve Pasta tooling locally while blocking hosted wtfOS pinning, publishing, preview-processing, package-record, and installer APIs. | `pasta_suite.installer_manifest.viewed`, `pasta_protocol.installer_catalog.viewed`, `pasta_suite.site_installed`, `pasta_suite.site_uninstalled` |

## Concern: WTF TV, Playback, Channels, and Embeds

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| TV viewer | Public/session | List public channels; resolve by dial/slug/id; switch channel; watch deterministic stream; view current item/schedule/settings; accept Skywire post context queued through the WTFOS SystemEvent pipeline for future programming automation; adjust volume/menu; receive skip/error notice. | `tv.channel.listed`, `tv.channel.viewed`, `tv.channel.switched`, `tv.current_item.viewed`, `tv.stream.started`, `skywire.pipeline.tv_queued` |
| Roger Radio live TV channel | Public/session | Switch to the seeded `ROGERvision LIVE` public WTF TV channel; stream the Odysee iframe feed from `roger-radio-live`; resolve its channel/current/stream/embed state without routing the iframe through the media cache. | `tv.channel.viewed`, `tv.channel.switched`, `tv.current_item.viewed`, `tv.stream.started`, `tv.embed.resolved` |
| wtfOS Guide TV | Public/session | Switch to the seeded `wtfOS Guide TV` channel and watch only the official wtfOS promos plus TommyTezos FAQ how-tos. Boot reconciliation keeps one canonical playlist and removes unrelated videos, schedules, and bumpers from this channel. | `tv.channel.viewed`, `tv.channel.switched`, `tv.current_item.viewed`, `tv.stream.started` |
| TV playback/telemetry | Public/session/system | Fetch stream/current/media/cache; send playback events and item-end telemetry; track skipped/stalled/error/ended sessions; view aggregate telemetry if authorized. | `tv.playback.event`, `tv.item.started`, `tv.item.ended`, `tv.item.skipped`, `tv.item.error`, `tv.telemetry.aggregate_viewed` |
| TV embeds | Public | Resolve embed metadata; render iframe player; fetch oEmbed; tune every viewer to the server-authoritative current item and wall-clock offset; refetch that authority at natural/error boundaries; play at most one channel-scoped media item or bumper at a time. | `tv.embed.resolved`, `tv.embed.rendered`, `tv.oembed.requested`, `tv.embed.media_served` |
| Creator channels | Session/owner | List own channels; create/edit/delete; select channel; set title/description/logo/banner/slug/public flag/videos-per-bumper; refresh sources. | `tv.channel.created`, `tv.channel.updated`, `tv.channel.deleted`, `tv.channel.sources_refreshed` |
| TV playlists | Session/owner | Create/rename/set active playlist; open editor; add/remove/reorder videos; set duration; save items. | `tv.playlist.created`, `tv.playlist.updated`, `tv.playlist.activated`, `tv.playlist.items_updated` |
| TV token/media sources | Session/owner | Search playable tokens; add/remove token/media to/from channel; manage usage before delete; delete media item. | `tv.media.added`, `tv.media.removed`, `tv.playable_tokens.searched`, `media.usage_viewed` |
| TV bumpers | Public/session/owner | View personal/community bumper pools; upload bumper; set category; toggle media as bumper; patch/delete bumper; serve bumper media. | `tv.bumper.uploaded`, `tv.bumper.updated`, `tv.bumper.toggled`, `tv.bumper.deleted`, `tv.bumper.media_served` |
| TV schedule | Public/session/owner | View schedule/current item; create playlist schedule entry; delete schedule entry. | `tv.schedule.viewed`, `tv.schedule.created`, `tv.schedule.deleted` |
| TV cache | Public/session/staff | Fetch cache media; view cache stats; prefetch media; handle cache hit/miss/error and budget alerts. | `tv.cache.media_requested`, `tv.cache.hit`, `tv.cache.miss`, `tv.cache.error`, `tv.cache.prefetch_requested`, `tv.cache.stats_viewed` |
| WTF TV admin | Staff | Configure canonical source mode; select users/wallets; set tokens per wallet, durations, playlist size, refresh interval, bumper mode; initialize/refresh canonical channel. | `tv.admin.config_updated`, `tv.admin.channel_initialized`, `tv.admin.sources_refreshed`, `tv.admin.bumpers_selected` |

## Concern: Market, Exchange, Inventory, and Commerce

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| WTF In-App Marketplace | Session/trusted creator/staff | Browse categories including the ranked Apps section generated from the canonical wtfOS app catalog; view EXP balance, items, inventory, recent purchases, active sale pricing, role/prerequisite-locked app cards, and whole-WTF cart totals; create cart intents in WTF or EXP for normal items; create WTF-only app-unlock intents for eligible optional apps; fail closed before intent creation when a role-gated app is missing its required role, pass, or prerequisite; after purchase, owned app-unlock inventory personalizes `/api/apps/desktop` so the app appears in Start Menu and can be pinned as a desktop shortcut; checkout EXP; for WTF cart checkout use mainnet in-app market V2 `KT1FN2bwYAffC2VgmSNs76DiPkSwZurbBoHR` with explicit contract version, expected WTF token, expected treasury, amount, purchase reference, and deterministic cart hash before wallet signing; verify WTF op hash; sync purchases; use consumables; let trusted creators submit EXP-priced items privately and track submitted, approved, or rejected status plus operator notes; direct accounts without creator permission to Contact Admin; let operators review creator attribution and approve or reject before Store visibility; browse tangible WTF LIVE Tips items such as Roses, Pocket Change, Rubber Chickens, Jalapenos, Flaming Hearts, and Paul's Panties that can be bought with WTF or EXP; view received WTF LIVE tip transfers and redeem eligible tip items into earned WTF for the normal reward-ledger cashout flow. | `wtfiam.viewed`, `wtfiam.app_unlock.viewed`, `wtfiam.app_unlock.blocked`, `wtfiam.app_unlock.intent_created`, `wtfiam.cart_intent.created`, `wtfiam.exp_checkout.completed`, `wtfiam.purchase.verified`, `wtfiam.inventory.synced`, `wtfiam.item.used`, `desktop.shortcut.created`, `wtfiam.creator_item.created`, `wtfiam.creator_item.reviewed`, `wtfiam.sale_applied`, `inventory.item.transferred`, `wtf_live.tip.redeemed`, `reward.ledger.created`, `xp.spent` |
| In-app inventory use | Session/owner/API | Consume food/medicine through `/api/in-app-market/use`; transfer owned WTF LIVE tip items to another signed-in room user through `/api/in-app-market/tips`; redeem received tip transfers through `/api/in-app-market/tips/redeem`; load Arcade credits onto a Play Pass Card via mainnet WTF market verification; consume Arcade credits on paid Arcade sessions; enforce pet-ball cap and stock limits. | `inventory.item.consumed`, `inventory.item.granted`, `inventory.item.transferred`, `inventory.stock_reserved`, `inventory.cap_blocked`, `wtf_live.tip.sent`, `wtf_live.tip.redeemed`, `arcade.play_credit.consumed` |
| In-app market admin | Staff | View catalog; create catalog items; update stock, active state, visibility, pricing, metadata, rarity tier, price score, and category placement; lock item price or score anchors; rebalance suggested prices; create/update/delete category or SKU sales. | `wtfiam.admin.item_viewed`, `wtfiam.admin.item_created`, `wtfiam.admin.item_updated`, `wtfiam.admin.price_rebalanced`, `wtfiam.admin.sale_updated` |
| On-chain marketplace | Public/session/owner | Use mainnet Marketplace V2 `KT1C8jTazt2QyFLPKf27xRGssv99AtzagWHb` for all new wtfOS activity; view snapshot/listings/detail/mine/external mine/activity with contract version and legacy recovery context; create buy-now listing or auction with explicit quantity; buy/cancel listing with expected token, owner, quantity, and unit price; place/accept/reject/cancel offers with owner-scoped keys, explicit quantity, unit WTF, total WTF, target owner, offerer, and contract version; bind every approval, listing, auction, purchase, bid, settlement, cancellation, offer, and offer-accept send to the wallet address that prepared it, rejecting a switched signer before contract access; require the active signer to match a selected token's owning linked wallet before approval or listing; preview accept terms before wallet signing; block legacy accepts when canonical on-chain offer quantity is missing or greater than one; cancel external listing. Legacy V1 `KT1Jt6gU4fS5UYHdhsYyr2EfpBJtXZLrPPfj` remains live but is not the active application contract so its stranded 1.1 WTF offer can be recovered later by a human-authorized party. | `marketplace.viewed`, `marketplace.listing_created`, `marketplace.listing_bought`, `marketplace.listing_cancelled`, `marketplace.offer_created`, `marketplace.offer_accept_terms_previewed`, `marketplace.offer_accepted`, `marketplace.legacy_offer_accept_blocked`, `marketplace.offer_rejected`, `marketplace.offer_cancelled`, `wallet.account_mismatch.blocked` |
| Rat Race | Session/wallet | View urgency-ranked Tezos edition cards sourced canonically from the tz2at rolling market stream, with local market-index fallback only when tz2at is unavailable; choose preset dropdowns for card limit, recent-sales window, 1/3/7-day minted-age window, minimum sold-through percentage, and minimum recent-buy count; click Scan to run those filter settings instead of relying on automatic reloads; inspect sold-through, active supply, floor, estimated time-to-sellout, tz2at freshness, tz2at replay scan coverage, supplement-source diagnostics, and near-miss rejection reasons; accept Skywire post context queued through the WTFOS SystemEvent pipeline as market/social signal input; open the parent marketplace listing; create a direct wallet purchase intent for allowlisted external marketplace contracts only when supplemental Objkt hydration provides a public tez listing purchase key, price, and marketplace contract for the ranked token. | `rat_race.viewed`, `rat_race.scan_requested`, `rat_race.card.opened`, `rat_race.purchase_intent.created`, `skywire.pipeline.rat_race_queued`, `blockchain.tezos.token_sale` |
| Auctions | Public/session/owner | Browse active auctions; create auction; bid; cancel own auction; settle; inspect detail/bids. | `auction.created`, `auction.bid_submitted`, `auction.cancelled`, `auction.settled` |
| Trade boards/barter | Public/session/owner/agent | View public trade-board cache; search/switch modes; add/remove owned tokens; place/accept/reject/cancel offers using owner-scoped marketplace keys and explicit quantity/price terms; bind barter approvals, creates, accepts, and cancellations to the wallet that prepared the action and reject a switched signer before contract access; preview accept terms before wallet signing; view barter on-chain snapshot; on Gamma, keep `/trade-boards` inside the Gamma application shell with route-owned trade-board surface, mode switch, search, listing cards, offer controls, and barter handoff markers while preserving shared marketplace and barter APIs. | `trade_board.viewed`, `trade_board.token_added`, `trade_board.token_removed`, `trade_board.offer_created`, `trade_board.offer_accept_terms_previewed`, `trade_board.offer_accepted`, `trade_board.offer_rejected`, `trade_board.offer_cancelled`, `wallet.account_mismatch.blocked` |
| Swap/DEX | Public/session/wallet | View token list/pools/counterparts/health/metrics; choose from/to token; set amount/slippage; flip direction; execute token-to-token, tez-to-token, or token-to-tez swap; view op status/hash; when opened through Gamma, keep DEX health, token selectors, amount/quote fields, direction control, slippage controls, quote math, execution CTA, and 3Route exits inside Gamma-owned presentation chrome while preserving shared `/api/dex/*`, 3Route links, wallet session restoration, and `executeSwap` behavior. | `dex.tokens.viewed`, `dex.pool.viewed`, `swap.quote_viewed`, `swap.slippage_updated`, `swap.submitted`, `swap.confirmed`, `swap.failed` |
| Buyback/recapture | Public/session/staff | View an active buyback window, its manual-payment boundary, and its versioned directional WtfBuybackV1 proof; submit a completed swap operation hash for exact TzKT verification rather than initiate a wallet send; record auction bids with visible notice that no WTF moves or is reserved; staff create/fund/open/close/pause/resume/sweep buyback windows, with uniform contract-cap and proof/root compatibility enforced before activation; when opened through Gamma, retain the same shared buyback and auction flows inside the WTF Recapture Gamma surface. | `buyback.window.viewed`, `buyback.intent_submitted`, `buyback.window_created`, `buyback.window_opened`, `buyback.window_closed`, `buyback.window_swept` |
| Contract factory | Staff | Choose template/network with Shadownet as the fresh-work default and Ghostnet retained only as explicit legacy test support; edit storage JSON; compile; deploy; view deployed contracts; retire live contract; when opened through Gamma, keep deploy/wizard tabs, Kiln endpoint, storage editor, simulation steps, deploy controls, and registry table inside Gamma-owned presentation chrome while preserving shared `/api/factory/*` behavior. | `contract_factory.compiled`, `contract_factory.deployed`, `contract_factory.contract_retired` |
| Operator wallet | Staff | View signer status/balances; refresh balances; view unpaid rewards; preview/run/reconcile disbursement; run buyback actions; view recent runs; when opened through Gamma, keep signer status, balance table, reward ledger, buyback controls, and run history inside Gamma-owned presentation chrome while preserving shared `/api/operator-wallet/*` signer intent behavior. | `operator.status_viewed`, `operator.balance_refreshed`, `operator.disbursement.previewed`, `operator.disbursement.run`, `operator.run.reconciled` |
| Payroll | Strict admin | Open `/payroll`; explicitly connect a funding wallet through Payroll's isolated Octez session without reading or replacing the profile wallet session; view that wallet's mainnet XTZ and WTF balances; enter a syntactically valid Tezos wallet or contract destination; review the exact asset, atomic-safe amount, source wallet, recipient, chain id, and WTF FA2 contract before signing; recheck the active signer and mainnet chain immediately before sending; wait for one confirmation and open the resulting operation on TzKT. Payroll never accepts or stores a private key or seed phrase. | `payroll.wallet_connected`, `payroll.transfer_reviewed`, `payroll.transfer_confirmed` |
| Objkt Operator | Private owner (`wtf-admin` by default) | Open `/objkt-operator`; persist the public operator wallet address, spend cap, price and quality floors, Kukai/Objkt readiness, creator review decisions, Objkt market scans, signing queue transitions, operation hashes, and event history in PostgreSQL; discover 25 recent creators and inspect the weighted sales, buyers, volume, recency, Objkt verification, affordable-inventory depth, and floor-fit contribution for each creator before approving or rejecting; scan only approved creators; queue only candidates inside the persisted spend and resale policy; open Objkt and Kukai in named signing tabs. Seed phrases and signing passwords never enter the wtfOS API or database. | `objkt_operator.creator_reviewed`, `objkt_operator.market_scanned`, `objkt_operator.candidate_queued`, `objkt_operator.purchase_signed` |

## Concern: Club Dues, Memberships, and Subscription Access

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Dues public surface | Public/session | Open `/dues`, `dues.wtfgameshow.app`, or the Start Menu On Chain listing; the WTF OS desktop item is registered but default-off through the admin app gate; browse live dues contracts; inspect network, treasury, membership symbol, monthly dues, utility units, and live contract address; on Gamma, use the club-dues shell with route-preserving handoffs to Inbox and Operator Wallet. | `club_dues.viewed`, `desktop.icon.opened`, `api.public.read` |
| Contract customization | Public/session/admin | Fill top-level template data for club name, slug, description, network, treasury, admin, monthly dues, month length, utility-unit rate, grace window, arrears warning window, metadata URI, and manager wallet id; compile the SmartPy template through Kiln before any origination. | `club_dues.contract.customized`, `club_dues.contract.compiled`, `contract_factory.compiled` |
| External wallet deployment | Public/session/wallet | Compile the club dues template and originate it from the connected user's wallet on the configured test network before production mainnet use. | `club_dues.external_wallet.deployed`, `wallet.provider.preflight.succeeded`, `contract_activity.submitted` |
| Manager wallet deployment and authority | Admin | Save a contract draft; deploy the compiled V2 template through the selected platform wallet; record deployment run, op hash, originated KT1 address, signer status, and compile artifact. Contract control uses a two-step propose/accept transfer, privileged calls reject attached tez, and the dedicated contract-admin wallet is the normal final authority while the dues manager performs only explicitly delegated operations. | `club_dues.contract.deployed`, `club_dues.admin.proposed`, `club_dues.admin.accepted`, `operator.run.reconciled`, `contract_activity.submitted` |
| Member payment and renewal | Session/wallet | Create a payment intent; send exact XTZ dues through legacy `pay_dues` or tiered `pay_membership`; verify TzKT operation against linked wallet, live contract, amount, periods/months, tier, action, and payment ref; refresh paid-through timestamp and utility units in the WTF ledger. | `club_dues.payment.intent_created`, `club_dues.payment.verified`, `club_dues.member.renew_existing`, `wallet.operation.verify_failed` |
| On-chain membership record | System/contract | SmartPy contract issues tiered non-transferable membership receipt tokens, lets members renew old art, replace with current drop art, or pay the preserve fee to keep the old token as a collectible while minting the current drop, extends paid-through, accumulates utility units, forwards dues to treasury, and emits dues/member/drop events. | `club_dues.member.token_issued`, `club_dues.member.token_refreshed`, `club_dues.member.token_replaced`, `club_dues.member.token_preserved`, `club_dues.drop.updated`, `club_dues.tier.updated`, `club_dues.utility_units.issued` |
| Arrears and warnings | Admin/system/session | Sweep expired ledgers past grace period; mark members in arrears, optionally mark the contract naughty list through the manager wallet, and send WTFIG inbox warnings. | `club_dues.member.arrears_warned`, `club_dues.member.naughty_listed`, `notification.viewed` |

## Concern: WTF Casino, Membership, and Wagered Games

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Casino shell and entry gate | Session | View WTF Casino under Start Menu Gaming; show it visible but inactive when the current user lacks an active membership card; open `/casino` only when the app launcher is active; view app-pass status, membership status, configured contract, and fail-closed entry state; enter only when both the marketplace app pass and active membership card are present. | `casino.access.viewed`, `casino.entry.granted`, `casino.entry.rejected` |
| Casino app pass | Session/owner | Browse the Casino in-app-market category, purchase or sync the `casino-app-pass` item, and use it only as an app-access entitlement rather than a wager ticket. | `wtfiam.viewed`, `wtfiam.cart_intent.created`, `wtfiam.purchase.verified`, `wtfiam.inventory.synced` |
| Casino membership card | Session/wallet | Create a server intent, send exactly 1 XTZ through `purchase_membership` on the Casino membership contract, verify the operation hash against linked wallets, treasury forwarding, fee, contract address, and intent ref, then grant a 30-day membership. | `casino.membership.intent_created`, `casino.membership.verified`, `wallet.provider.preflight.succeeded`, `wallet.operation.verify_failed` |
| Casino table registry and wagers | Session/system | List installed table stubs; keep wagering disabled until each game has a domain-owned wager-session contract, house-take policy, settlement verifier, replay guard, and durable audit log; expose hashed audit summaries for mocked table actions without leaking raw wallet identifiers. | `casino.games.viewed`, `casino.wager_session.rejected`, `casino.entry.rejected`, `casino.audit.event_recorded` |
| Casino community practice tables | Session/creator/member/operator | Define a table name, description, instructions, and equal-chance result labels; submit it hidden from the floor; inspect personal submitted/approved/rejected status and operator note; prevent creator self-approval; let an operator approve or reject with attribution and a required note; publish approved tables with creator attribution; let a member with Casino entry play a persisted practice result; keep currency, wager, house take, payout, and rewards absent. | `casino.practice_game.submitted`, `casino.practice_game.reviewed`, `casino.practice_game.played` |
| WTF Does This Button Do?!!? | Session/wallet/system | Open `/casino/wtf-button`; view Red, Green, and Blue jackpot table states, mocked XTZ balance, wallet-specific press quote, WTF fee, pot contribution, expected time extension, leader panel, presser leaderboard, Rug Clash entrants, timeline, and strict/flexible price protection; submit mocked quote/press actions only through deterministic service interfaces until Tezos escrow/randomness contracts replace the mock adapter. | `wtf_button.lobby.viewed`, `wtf_button.table.viewed`, `wtf_button.quote.created`, `wtf_button.press.succeeded`, `wtf_button.press.rejected`, `wtf_button.price_protection.rejected`, `wtf_button.danger_zone.entered`, `wtf_button.rug_clash.started`, `wtf_button.rug_clash.entered`, `wtf_button.rug_clash.resolved`, `wtf_button.round.settled`, `wtf_button.round.refunded`, `wtf_button.audit.event_recorded`, `wtf_button.simulation.run` |
| Rug Pull: The Game | Session/wallet/system | Open `/casino/rug-pull`; view the active mocked round, current pot, next-round seed, pressure multiplier, button lock, Panic Mode countdown, witness vote state, player share table, and settlement feed; submit mocked join, delay, press, witness, and witness-vote actions through deterministic service interfaces until the dedicated Tezos contract and verifier replace the mock adapter. | `rug_pull.rules.viewed`, `rug_pull.round.join_intent_created`, `rug_pull.button.delay_intent_created`, `rug_pull.button.press_intent_created`, `rug_pull.witness.join_intent_created`, `rug_pull.witness.vote_cast`, `rug_pull.round.settled`, `rug_pull.audit.event_recorded`, `rug_pull.wager.rejected` |
| Guinea Pig Raceway | Session/wallet/system | Open `/casino/guinea-pig-raceway`; view the mocked live race card, 3D GLB racer scene, racer stats, five-track catalog data, conditions, global variables, lockout schedule, live race phase, paid effect caps, result replay metadata, tote board, ticket ledger, official-result audit hash, and replay camera angles; submit mocked bet/effect actions only through deterministic service interfaces until the raceway contract, randomness beacon, replay archive, and settlement verifier replace the mock adapter. | `guinea_pig_raceway.rules.viewed`, `guinea_pig_raceway.race_card.viewed`, `guinea_pig_raceway.tote.viewed`, `guinea_pig_raceway.ticket.accepted`, `guinea_pig_raceway.ticket.refunded`, `guinea_pig_raceway.official_result.recorded`, `guinea_pig_raceway.bet_intent_created`, `guinea_pig_raceway.bet_locked`, `guinea_pig_raceway.bet_rejected`, `guinea_pig_raceway.intro.started`, `guinea_pig_raceway.race.started`, `guinea_pig_raceway.effect_intent_created`, `guinea_pig_raceway.effect_rejected`, `guinea_pig_raceway.race.settled`, `guinea_pig_raceway.replay.viewed`, `guinea_pig_raceway.audit.event_recorded`, `guinea_pig_raceway.wager.rejected` |

## Concern: WTF Arcade, WTF Console, and Game Studio SDK

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| WTF Arcade catalog | Public | Browse public paid-play games, compatible-source imports, approved creator submissions, discovery shelves, stats, play fee, game detail, leaderboards, recent scores, champions, top players, player profile, source proxy assets; when opened through Gamma, keep catalog stats, search/filter controls, game cards, discovery/champion/top-player/recent-score rails, and player lookup inside Gamma-owned presentation chrome while preserving the shared Arcade catalog and public-data APIs. | `arcade.catalog.viewed`, `arcade.discovery.viewed`, `arcade.stats.viewed`, `arcade.play_fee.viewed`, `arcade.game.viewed`, `arcade.leaderboard.viewed`, `arcade.source_asset.served` |
| Púca’s Fortune Arcade cartridge | Public catalog/session play | Discover Púca’s Fortune in the public Arcade catalog; load its same-origin HTML5 package at `/games/installed/pucas-fortune/index.html`; keep local run saves, audio, fullscreen, Poker/Rummy/Cribbage mode selection, and touch/pointer controls working inside the Arcade iframe. | `arcade.catalog.viewed`, `arcade.game.viewed`, `arcade.session.created` |
| WTF Arcade play | Session | View Play Pass Card and loaded credit status/bypass; create WTF payment intent; create signed play session after card+credit/bypass; after a paid play of a mapped creator game, durably record a pending creator-share audit intent for later settlement; fail closed with a Windows-style card/credit error for paid games; boot sandboxed compatible-source games with SDK storage fallbacks; submit one-use signed score; receive XP for valid score events; when opened through Gamma, keep payment recovery links, runtime HUD, controls, and sandboxed game iframe inside the Gamma presentation shell without changing play-pass, session, score, or SDK bridge behavior. | `arcade.play_status.viewed`, `arcade.play_intent.created`, `arcade.play_credit.consumed`, `arcade.creator_payout.pending`, `arcade.session.rejected`, `arcade.session.created`, `arcade.score.submitted`, `arcade.score.accepted`, `arcade.score.rejected`, `xp.awarded` |
| WTF Arcade reports | Session/staff | Report active Arcade game; block duplicate open report per user/game/category; staff list reports; review/resolve/dismiss/reopen; priority includes report counts and invalid score signals; when opened through Gamma, keep report trigger and report dialog inside Gamma-owned presentation chrome while preserving moderation API behavior. | `arcade.report.created`, `arcade.report.reviewed`, `arcade.report.resolved`, `arcade.report.dismissed`, `arcade.report.reopened` |
| WTF Arcade creator submissions | Session/creator/trusted creator/staff | Submit ZIP media/game bundle; update own game; trusted creators auto-publish; staff list moderation queue; approve/reject/remove/restore; preserve version/provenance/score caps. | `arcade.game.submitted`, `arcade.game.update_submitted`, `arcade.game.auto_published`, `arcade.game.approved`, `arcade.game.rejected`, `arcade.game.removed`, `arcade.game.restored`, `xp.awarded` |
| WTF Arcade source import/admin | Staff/system | Run compatible-source ingest/check; scheduled worker records imported/skipped/updated rows and audit health even when no games changed; set non-user-submitted Arcade game credit price/free-play rule. | `arcade.source_import.started`, `arcade.source_import.completed`, `arcade.source_import.failed`, `arcade.credit_rule.updated`, `arcade.audit.created` |
| WTF Console catalog | Public/session | Browse stock Console titles, demo cartridges, installed games, owned wallet/media cartridges, SDK, bundles, dependencies, game detail, stock leaderboards, recent scores, champions, players, player profile; when opened through Gamma, keep the shared Console catalog, filters, stat strip, cards, controls, and owned-wallet scan messaging inside Gamma-owned presentation chrome. | `console.catalog.viewed`, `console.game.viewed`, `console.sdk.served`, `console.bundle.served`, `console.dependency.audit` |
| WTF Console play | Session | Create signed stock-game play session; submit one-use score; reject expired/used/missing/tampered tickets and impossible scores; award XP for first play, score submit, personal best, champion; when opened through Gamma, keep runtime HUD, controls, direct-iframe cartridges, ZIP fallback cartridges, and SDK bridge messaging inside the Gamma presentation shell without changing session or score APIs. | `console.session.created`, `console.score.submitted`, `console.score.accepted`, `console.score.rejected`, `xp.awarded` |
| WTF Console reports/admin | Session/staff | Report stock Console game only; Console public creator submission/moderation routes now return compatibility/gone responses pointing to Arcade; staff view Console audit events and cache dependencies. | `console.report.created`, `console.audit.viewed`, `console.compatibility_route.hit`, `console.dependency.cache_requested` |
| Game Studio SDK | Public/session/agent | Generate scaffold from templates; use SDK hooks `ready`, `startSession`, `getPlayer`, `getAvatarAsset`, `getAvatarSpriteSheet`, `updateScore`, `gameOver`, `pause`; build SDK-compatible ZIP; submit project to Arcade. | `game_sdk.ready`, `game_sdk.session_started`, `game_sdk.player_loaded`, `game_sdk.avatar_loaded`, `game_sdk.score_updated`, `game_sdk.game_over`, `game_sdk.paused` |

## Concern: Administration, Governance, and Operations

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Admin overview | Strict `admin` role | Open `/admin?section=overview` as the task-first start surface; reject direct, desktop, Start Menu, command-palette, browser-helper, and CLI access for every non-admin role even when that role has a related non-admin surface grant; review live inventory, route common complaint/recovery/access/surface workflows, search the complete admin topic index in plain language, filter the URL-backed section navigation, and press `/` outside an editor to focus the navigation search. | `admin.dashboard.viewed`, `admin.overview.viewed` |
| Commission moderation queue summary | Strict `admin` role | From Admin Start Here, review pending Store creator items, Arcade games, Casino practice tables, and Calendar tickets in one read-only summary; open WTFIAM Market, Arcade moderation, Casino, or Control Board to make the decision in the domain that owns the submission. `GET /api/admin/stats` supplies canonical pending counts and rejects non-admin callers. | `admin.overview.viewed`, `wtfiam.creator_item.created`, `arcade.game.submitted`, `casino.practice_game.submitted`, `calendar.ticket_submitted` |
| WTF OS admin surfaces | Admin | View every registered sub-app/tool/desktop item by domain and subdomain; open the app, open related central admin routes/tabs, adjust app gate visibility, and inspect native settings and automation handles. | `admin.os_surface.viewed`, `admin.os_surface.app_opened`, `admin.os_surface.admin_route_opened`, `admin.app_gate.updated` |
| User role review and WTF Passport | Staff with `manage_users`; permanent deletion separately requires `delete_users` | Open `/admin?section=users`; search/filter/sort all users by identity, highest assigned role, access level, EXP tier, curse count, or account-health signals; click a user name to deep-link `user=:id` and open their acute WTF Passport; inspect identity/account health, all assigned roles and active curses, effective permissions and wtfOS surfaces/routes/admin tabs/agent handles, every durable desktop setting, linked wallets, WTF domains, and recent EXP; adjust authorized roles, curses, identity, EXP, desktop style/colors/chat/cursor/wallpaper/physics/gravity/pet/icon layout/locale, social connections, or temporary passwords from named passport sections; keep permanent account deletion behind the separate `delete_users` permission so cohosts retain ordinary support access without destructive authority unless an admin explicitly grants it. | `admin.user.viewed`, `admin.user.passport.viewed`, `admin.user.role_assigned`, `admin.user.role_removed`, `admin.user.curse_assigned`, `admin.user.curse_lifted`, `admin.user.xp_awarded`, `admin.user.identity_updated`, `admin.user.desktop_settings.updated`, `admin.user.deleted`, `admin.user.resynced` |
| Curse assignment scope | Admin | Open `/admin?section=curses`; search/sort the canonical curse catalog by label, key, user-visible effect, affected user, or assignment reason; compare active assignment and affected-user counts; select a curse for its acute definition, assigned-user table, reasons/timestamps, direct WTF Passport handoff, and deliberate apply/lift controls. | `admin.user.curse_assigned`, `admin.user.curse_lifted` |
| Admin Help index | Admin/agent | Open `/admin?section=help`; search every central admin section, registered WTF surface, route, native setting, permission, curse, API path, and automation handle; filter by topic kind or risk; open the ranked destination and human resolution flow; switch to the agent contract to copy stable JSON. Agents with admin-panel permission can query the versioned, no-store `GET /api/admin/help-index` endpoint with optional `q`, `id`, and `kind` filters. | `admin.help.searched`, `admin.help.topic.opened` |
| Gameshow admin | Admin | Manage seasons, rounds, challenges, submissions, reward flags, side quests, completions, calendar, eliminations, drafting, control-board actions, and programmable gameshow automation rules; when `/control-board` opens through Gamma, keep season selection, tabs, cohort tables, rule panels, ticket queue, audit feed, and scaffold tabs inside Gamma-owned presentation chrome while preserving shared gameshow admin APIs. | `admin.season.updated`, `admin.round.updated`, `admin.challenge.updated`, `admin.side_quest.updated`, `control_board.action_applied`, `admin.challenge_automation.updated` |
| Challenge automation admin | Admin | Create/edit challenge automation definitions; choose triggers/predicates/actions; preview readable rule; seed example challenges; activate/pause/archive; inspect progress/events/audit. | `admin.challenge_automation.created`, `admin.challenge_automation.updated`, `admin.challenge_automation.status_updated`, `challenge_automation.audit.viewed` |
| Board/content admin | Admin | Moderate board; manage categories/channels/pins/permission overwrites/webhooks; manage links and FAQ. | `admin.board.updated`, `admin.content.link_updated`, `admin.content.faq_updated` |
| Roles/permissions/app gates | Admin | Open `/admin` as a suite-style control surface; select a role and inspect its catalog metadata, access level, assignability, default WTF OS posture, permission totals, registered surface totals, desktop-app visibility, route exposure, native app settings, and automation handles; create/update role catalog rows with category, purpose, description, color, icon, sort order, default OS access, and assignability; view/toggle additive role permissions by category; reset one role or all permission sets; search/filter/toggle registered WTF OS app/domain/process/UI surface access by role; grant Test Subject experimental access; view/update desktop, Start Menu, command-palette, and direct-route app gates plus gate-aligned MCP feature access; refresh every desktop-app docs/install-key registration in one action; mark an app license, docs, and install key as non-expiring while preserving manual revoke and launcher controls; distinguish loading from a failed registration query and expose an actionable retry state with database-migration guidance instead of an endless loader. | `admin.role_catalog.viewed`, `admin.role_catalog.updated`, `admin.permissions.updated`, `admin.permissions.reset`, `admin.role_access.updated`, `admin.role_access.reset`, `admin.app_gate.updated`, `desktop.app.disabled_by_admin` |
| Rewards/admin XP | Admin | Filter XP events; award XP manually; filter reward ledger; mark single/batch paid. | `admin.xp.awarded`, `admin.xp_log.viewed`, `admin.reward.updated` |
| Media/storage/admin | Admin | Inspect media storage usage; protect/delete storage keys; configure Studio Drive/root; refresh storage status. | `admin.media_storage.viewed`, `admin.media_storage.updated`, `admin.studio_storage.updated` |
| Backup Manager | Admin | Open `/backup-manager`; inspect latest backup run, backup artifact, targets, checksums, restore drill status, row-count/media-manifest requirements, and whether the system may truthfully claim backup safety; when opened through Gamma, keep status cells, restore proof panels, targets, and Recovery/Admin handoffs inside Gamma-owned presentation chrome while preserving shared restore-proof APIs and system events. | `backup_manager.viewed`, `backup_manager.opened` |
| System logs/health | Public/session/admin/system | Fetch minimal public liveness from `GET /api/health`; fetch dependency and scheduler readiness from `GET /api/health/ready` with `503` on degradation; fetch full runtime/job diagnostics from authenticated `GET /api/health/diagnostics`, which likewise returns structured diagnostics with 503 while degraded; fetch disk diagnostics from authenticated `GET /api/health/disk`; ingest bounded client logs; admin query logs, summaries, and events by source/type/severity/status/user. Routine scheduler starts and successes remain available from canonical `sync_runs` state and the rotating file trace; scheduler failures, skips, missing jobs, and other actionful events remain database-queryable. | `system.health.checked`, `system.readiness.checked`, `system.diagnostics.checked`, `system.disk_health.checked`, `system.client_log.ingested`, `system.log.viewed`, `system.error.recorded` |
| UX Lab | Admin | Open `/dev/ux-lab` portfolio/collection workspace and quick links for design/UX testing; when opened through Gamma, keep the collection workspace and quick-link handoffs inside the Gamma shell while preserving shared wallet, portfolio, collection, and token queries. | `ux_lab.opened`, `ux_lab.collection_workspace.used` |

## Concern: Public Data, Embeds, APIs, Agents, and Automation

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Public JSON APIs | Public | Read minimal liveness and readiness, app gates, links, FAQ text, FAQ tutorial and wtfOS promo catalogs, proxied FAQ/promo video/captions/posters, Arcade/Console/Game Studio public data, leaderboards, profiles, trade boards, marketplace snapshots, DEX data, calendar, TV, embeds, gallery/token data, crawler previews. Full runtime/job and disk diagnostics require a signed-in session. | `api.public.read`, `public_data.viewed`, `system.health.checked`, `system.readiness.checked` |
| Versioned Platform API | Public discovery / paired access token | Discover `/api/v1`, download the OpenAPI 3.1 contract, browse `/api/v1/docs`, and call the complete existing API through bearer-authenticated `/api/v1/*` aliases. Reads require `api:read`, mutations require `api:write`, admin paths additionally require an admin account plus `api:admin`, and disabled app gates fail closed without changing legacy `/api/*` browser/internal behavior. Friendly aliases include `/api/v1/me` and `/api/v1/tokens`. | `api.public.read`, `mcp.api.called`, `mcp.tool.called`, `mcp.tool.succeeded`, `mcp.tool.failed` |
| Static discovery metadata | Public/crawler/install surface | Request `/robots.txt`, `/sitemap.xml`, and `/manifest.json`; receive deliberate robots, sitemap XML, and web app manifest metadata rather than the SPA HTML shell. | `public.static_discovery.viewed` |
| Public bounded writes | Public/bot | Submit client logs; TV playback/item-end telemetry; configured board webhook; bot attendance/Discord routes with HMAC. | `system.client_log.ingested`, `tv.telemetry.ingested`, `board.webhook_received`, `bot.webhook.received` |
| MCP transport | Agent | Connect to `/mcp`; call tools with bearer token; receive rate-limit, standard access manifest, and feature-gate data; fail closed on revoked token/gate/role-mismatched scope; ignore any browser session cookie without emitting `Set-Cookie`. | `mcp.connected`, `mcp.tool.called`, `mcp.tool.succeeded`, `mcp.tool.failed`, `mcp.rate_limit.hit`, `mcp.browser_session_ignored` |
| MCP API bridge | Agent | Use `wtf_api_request` to call any `/api/v1` operation through the paired token while retaining API read/write/admin scopes, route ownership, app gates, and normal handler authorization. | `mcp.api.called`, `mcp.tool.called`, `mcp.tool.succeeded`, `mcp.tool.failed` |
| MCP desktop/pet | Agent | Read/update appearance; read pet; apply safe pet care auto/specific actions for paired user. | `mcp.desktop.read`, `mcp.desktop.updated`, `mcp.pet.read`, `mcp.pet.action_applied` |
| MCP public data | Agent | Search public tokens; list unlisted trade-board tokens; list TV channels; list Arcade games/stats/fee/status/audit; list Console stats/discovery/players/recent/audit; list Game Studio templates/assets/snippets/targets. | `mcp.public_data.read`, `mcp.arcade.read`, `mcp.console.read`, `mcp.game_studio.read` |
| MCP registered inventory | Agent | Read the standardized WTFOS registered inventory, current pathways, provenance, witness metadata, and deployment state so paired agents can handshake against the live creator registry. | `mcp.inventory.read`, `mcp.public_data.read`, `mcp.tool.called` |
| MCP paired mutations | Agent | Set paired user's trade-board tokens; create Arcade play intent; create/update/build/submit Game Studio projects; create trusted creator market item where permitted; create sanitized WTF Map Lab document payloads without reading, using, or exposing ingested AT repo/firehose data paths. | `mcp.trade_board.updated`, `mcp.arcade.intent_created`, `mcp.game_studio.project_mutated`, `mcp.market.creator_item_created`, `map_lab.node.created`, `map_lab.wire.created` |
| External automation | Bot/system | Discord proof/activity/role sync/profile; attendance mirror; Arcade source import; wallet/indexing sync; health monitors. | `bot.discord.proof_received`, `bot.discord.activity_received`, `bot.attendance.received`, `worker.arcade_source_import.run`, `worker.wallet_sync.run` |

## Existing EXP and Reward Triggers

| Trigger | Current reason/source | Amount behavior | Dedupe/guard |
| --- | --- | --- | --- |
| Challenge submission | — | No XP on submit; grading awards XP separately. | One submission per user/challenge blocks duplicates. |
| Challenge grade pass/bonus | `challenge_grade_reward` | Challenge `rewardXp` on pass/bonus. | Only when `xpAwarded === 0`; reward flag upserted. |
| Side quest approval/auto-approval | `side_quest_reward` | Side quest `rewardXp`. | Completion uniqueness; reward ledger guarded by user + side quest. |
| Canonical daily side quest claim | `daily_loop:*`, `Side quest: *` | Daily automation awards configured XP and WTF after the user claims a verified current-UTC-day completion; automatic daily loops such as Mint Art Monday execute the same reward actions immediately after verification. | Completion key is per user and UTC day; reward action logs and reward ledger source ids make claim retries or repeated same-day events idempotent. |
| DM message | `dm_message_sent` | +1 XP. | No explicit daily cap in route. |
| Legacy thread created | `thread_created` | +2 XP. | Legacy route behavior. |
| Legacy thread reply | `thread_reply` | +1 XP. | Legacy route behavior. |
| Board message | `board_message_sent` | +1 XP. | Slow-mode/permission checks; no XP daily cap in route. |
| Channel message legacy route | `channel_message_sent` | +1 XP. | Legacy route behavior. |
| Desktop pet care | `desktop_pet_care` | +4 XP per care action when eligible. | Once per action per day; stored in `desktop_pet_events` with `xpEventId`. |
| Discord activity | `discord_${kind}_${action}` | Bot-supplied `xpAmount`, 0-1000. | Requires linked user, positive amount, and unique `externalRef`. |
| Console/Arcade creator submission | `console_game_submission` | +15 XP. | Duplicate guarded by user/reason/source/event/game/version; daily cap 3. |
| Console/Arcade creator update | `console_game_update` | +5 XP. | Duplicate guarded by version; daily cap 5. |
| Console/Arcade publish | `console_game_publish` | +25 XP. | Duplicate guarded by version; daily cap 3. |
| Console/Arcade publish update | `console_game_update_publish` | +10 XP. | Duplicate guarded by version; daily cap 5. |
| Console/Arcade first play | `console_game_first_play` | +5 XP. | Dedupe per game/user; daily cap 20. |
| Console/Arcade score submit | `console_score_submit` | +1 XP. | Dedupe per run/score key; daily cap 25. |
| Console/Arcade personal best | `console_personal_best` | +3 XP. | Dedupe per run/score key; daily cap 15. |
| Console/Arcade champion | `console_game_champion` | +8 XP. | Dedupe per run/score/rank; daily cap 5. |
| In-app EXP checkout | `in_app_market_purchase` | Negative XP equal to EXP subtotal. | Requires pending EXP intent, stock reserve, cap checks, sufficient balance. |
| WTF LIVE tip redemption | `wtf_live_tip` | Earned WTF equal to the tip item's configured redeem value times transferred quantity. | Requires an owned, unredeemed in-app inventory transfer for the signed-in receiver; duplicate redemptions are blocked by transfer status and `redeemedAt`. |
| Staff manual award | Admin XP route | Staff-specified amount/reason. | Staff permission; XP event records awardedBy. |
| CRP nomination watcher | `side_quest_reward` | Side quest `rewardXp` per watcher logic. | External reference/nominator reward counts. |

## Canonical Monitoring Event Envelope

All domain handles above should emit or be mappable into a common event envelope.
When a table already exists, use it. When no table exists, instrument the route,
client interaction, SDK bridge, or worker boundary.

| Field | Meaning |
| --- | --- |
| `eventId` | Stable unique id for the monitoring event. |
| `handle` | Canonical handle such as `arcade.score.rejected`. |
| `source` | Browser, server route, MCP tool, SDK, bot, worker, system monitor. |
| `surface` | WTF product surface: desktop, gameshow, W, TV, Arcade, Console, Game Studio, market, wallet, admin, MCP. |
| `actorUserId` / `actorRole` | Signed-in or paired user and role when known. |
| `sessionId` / `mcpTokenPrefix` | Browser session or paired-agent token prefix when applicable. |
| `method` / `path` / `statusCode` / `durationMs` | HTTP or tool execution context. |
| `targetType` / `targetId` | Game, score, challenge, media, channel, listing, wallet, grant, project, etc. |
| `walletAddress` / `opHash` / `chainId` | On-chain context when applicable. |
| `score` / `runId` / `ticketDigest` | Game score and anti-cheat context. |
| `amountExp` / `amountWtfUnits` / `sku` | Reward, purchase, inventory, and commerce context. |
| `result` / `reason` | Success, failure, rejection, moderation status, validation reason. |
| `ipHash` / `userAgentHash` | Privacy-preserving abuse correlation. |
| `metadata` | Bounded JSON for domain-specific context. |
| `createdAt` | Server-side timestamp. |

Existing storage anchors include `system_event_logs`, `xp_events`,
`reward_ledger`, `challenge_system_events`,
`challenge_automation_definitions`, `challenge_automation_progress`,
`challenge_automation_completions`, `challenge_automation_action_logs`,
`challenge_automation_audit_logs`, `desktop_pet_events`, `wallet_events`, `sync_runs`,
`indexing_queue`, `contract_activity_logs`, `attendance_events`,
`discord_activity_events`, `console_play_tickets`, `console_scores`,
`console_player_stats`, `console_audit_events`, `console_game_reports`,
`game_studio_project_builds`, `in_app_market_payment_intents`,
`in_app_market_purchases`, `user_notifications`, TV telemetry/cache stores, and
Studio/board/DM/media domain tables.

## Cheat Detection, Warnings, Alarms, and Punishment Anchors

| Concern | Signals to monitor | Suggested handles |
| --- | --- | --- |
| Auth abuse | Login/register failure bursts, wallet proof failures, OAuth callback errors, session reuse anomalies. | `auth.login.failed`, `auth.register.failed`, `auth.wallet.verify.failed`, `system.rate_limit.hit` |
| Wallet mismatch | Cached wallet address without provider, wrong active wallet, chain mismatch, invalid op hash, duplicate op hash, manual attestation mismatch. | `wallet.provider.preflight.failed`, `wallet.account_mismatch.blocked`, `wallet.operation.verify_failed`, `contract_activity.failure` |
| Arcade/Console score abuse | Missing/invalid/tampered/expired ticket, used ticket replay, score too large, score over speed cap, invalid score rows, report category `score-abuse`. | `arcade.score.rejected`, `console.score.rejected`, `console.audit.score_rejected`, `arcade.report.created` |
| Arcade ticket abuse | Play session without ticket, repeated payment-intent creation, ticket inventory mismatch, trusted bypass overuse. | `arcade.play_intent.created`, `arcade.play_ticket.consumed`, `arcade.session.rejected`, `inventory.cap_blocked` |
| Creator-game abuse | Unsafe/stolen/spam reports, repeated rejected bundles, unsupported files, source import failures, trusted auto-publish anomalies. | `arcade.report.created`, `arcade.game.rejected`, `game_studio.build.failed`, `arcade.source_import.failed` |
| EXP/reward abuse | Duplicate XP attempts, daily cap hits, reward ledger duplicate guard, negative XP spend anomalies, manual staff award spikes. | `xp.awarded`, `xp.award_skipped`, `reward.ledger.created`, `admin.xp.awarded`, `xp.spent` |
| Challenge automation abuse | Duplicate normalized events, repeated completion keys, failed reward action retries, suspicious event bursts across multiple challenge definitions, ownership predicate mismatch. | `challenge_automation.event_deduped`, `challenge_automation.completion_deduped`, `challenge_automation.action.failed`, `app.interaction.tracked`, `nft.ownership.verified` |
| Side quest abuse | Duplicate completions, auto-verify failure, schema/route auto-verify drift, manual proof spam, and paid-quest submission/approval/reward attempts with a missing, pending, malformed, underpaid, or cross-quest fee record. | `side_quest.completion_submitted`, `side_quest.auto_verify.failed`, `side_quest.auto_verify.schema_gap`, `side_quest.approved` |
| Attendance abuse | Duplicate `externalRef`, suspicious heartbeat cadence, join/leave spam, unmatched Discord user, impossible overlapping presence. | `attendance.voice_state_ingested`, `attendance.heartbeat`, `discord.activity.ingested` |
| Social spam | Board message bursts, W timeline action bursts, slow-mode hits, webhook abuse, mail send bursts, delivery complaint spikes, browser blocked-navigation probes, media attachment spam, repeated deleted/moderated posts. | `board.message.created`, `messageboard.reaction.added`, `messageboard.reaction.removed`, `w.reply.created`, `w.like.created`, `w.repost.created`, `w.quote.created`, `mail.delivery.failed`, `browser.navigation.blocked`, `board.webhook_received`, `system.rate_limit.hit` |
| Media abuse | Unsupported MIME, extension mismatch, upload cap failure, object-storage limit, repeated private file access failure, destructive delete spikes. | `media.uploaded`, `media.upload_failed`, `media.file_served`, `media.deleted`, `system.validation.failed` |
| IPFS pinning abuse | Broad wallet backup without active PDS/site, public-record disclosure bypass, private vault/session/admin path attempts, signed URL or credential leakage in `storageRef`, quota exhaustion, native-disk fallback, provider failure loops, Storage Box mirror drift, suspended host publishing attempts, and suspicious retry bursts. | `ipfs_pinning.policy.saved`, `ipfs_pinning.wallet_backup.enabled`, `ipfs_pinning.storage.staged`, `ipfs_pinning.pin.completed`, `ipfs_pinning.pds_record.queued`, `ipfs_pinning.pds_record.published`, `ipfs_pinning.subdomain_registry.linked`, `ipfs_pinning.restore_proof.created`, `system.validation.failed`, `system.rate_limit.hit` |
| TV telemetry abuse | Item-end spam, cache prefetch spam, cache budget warnings, playback error spikes by channel/media/session. | `tv.telemetry.ingested`, `tv.cache.prefetch_requested`, `tv.cache.error`, `system.disk_health.checked` |
| Marketplace abuse | Wash-like self-dealing, wrong-wallet signer, stale listing op hash, duplicate purchase ref, insufficient EXP, stock race, pet-ball cap attempts, self-tipping attempts, and duplicate tip-redemption attempts. | `marketplace.listing_created`, `marketplace.offer_created`, `wallet.account_mismatch.blocked`, `wtfiam.exp_checkout.failed`, `inventory.cap_blocked`, `wtf_live.tip.sent`, `wtf_live.tip.redeemed` |
| Club dues abuse | Duplicate payment refs, wrong-wallet dues sends, wrong contract, bad amount/month count, expired intent, fake op hash, manager-wallet origination outside deployment window, and ignored arrears warnings. | `club_dues.payment.intent_created`, `club_dues.payment.verified`, `wallet.operation.verify_failed`, `club_dues.contract.deployed`, `club_dues.member.arrears_warned` |
| Casino abuse | Missing Casino app pass, expired/replayed membership intents, wrong-wallet membership sends, bad fee amounts, treasury-forward mismatch, entry attempts while wager sessions are disabled, WTF Button stale quotes, price-protection bypass attempts, duplicate Rug Clash entries, leader-exclusivity bypass attempts, mocked-balance drift, Rug Pull button-lock abuse, Rug Pull witness-vote manipulation, Raceway late bets, invalid exotic tickets, Raceway effect spam, replay tampering, and future house-take settlement drift. | `casino.entry.rejected`, `casino.membership.intent_created`, `casino.membership.verified`, `wallet.account_mismatch.blocked`, `wallet.operation.verify_failed`, `casino.wager_session.rejected`, `casino.audit.event_recorded`, `wtf_button.press.rejected`, `wtf_button.price_protection.rejected`, `wtf_button.rug_clash.entered`, `wtf_button.audit.event_recorded`, `rug_pull.audit.event_recorded`, `rug_pull.wager.rejected`, `guinea_pig_raceway.bet_rejected`, `guinea_pig_raceway.ticket.refunded`, `guinea_pig_raceway.effect_rejected`, `guinea_pig_raceway.audit.event_recorded`, `guinea_pig_raceway.wager.rejected` |
| MCP abuse | Tool-call bursts, scope denied, feature gate denied, revoked-token use, staff-only tool attempts by non-staff token, forged wildcard/admin scopes, browser-session cookie presented alongside a different paired token. | `mcp.tool.called`, `mcp.tool.failed`, `mcp.rate_limit.hit`, `mcp.authz.denied`, `mcp.browser_session_ignored` |
| Admin misuse | Role/permission changes, curse assignment, user deletion, temp password creation, reward paid marks, app gate changes, operator runs, source imports. | `admin.user.deleted`, `admin.user.curse_assigned`, `admin.permissions.updated`, `admin.reward.updated`, `operator.disbursement.run`, `arcade.source_import.started` |

## Concern: Skullzarmy / FAFOlab Integrations (skllzrmy)

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| TezosBeats Music | Session + wallet | Open `/music`; play NFT/library audio; manage playlists; taskbar mini-player; `/tezamp` legacy alias; when opened through Gamma, keep the TezosBeats deck, now-playing visualizer, NFT/library/playlists queue, playlist panel, and legacy Tezamp queue inside Gamma-owned presentation chrome while leaving TzKT NFT reads, `/api/media/mine?category=audio`, and `/api/music/*` playlist/now-playing APIs unchanged. | `music.opened`, `music.first_play`, `music.track_played`, `music.playlist_create` |
| Mastodon (Tusk) | Session | Link instance token; view home timeline in W Feed; set feed preferences. | `mastodon.link`, `mastodon.timeline`, `mastodon.preferences.updated` |
| IPFS Pinning Manager / Porcupin | Session + WTF Pin Collector + wallet/PDS for broad backup | Open `/ipfs-pinning`; use legacy Porcupin setup/dashboard aliases for continuity; connect or inspect own-node status; route hosted pins through wtfOS Porcupin on Hetzner S3/volume storage; publish portable pin ledgers into the user's wtfos.me PDS and serve public pointers through the subdomain registry; publish Pasta Protocol project-bundle recovery through `POST /api/ipfs-pinning/pasta-protocol/publish` only after the site/PDS/wallet/object-storage prerequisites pass; when opened through Gamma, keep the shared manager chrome and Porcupin aliases inside Gamma while the pinning APIs, PDS records, and storage paths remain shared. | `ipfs_pinning.policy.saved`, `ipfs_pinning.wallet_backup.enabled`, `ipfs_pinning.storage.staged`, `ipfs_pinning.pin.completed`, `ipfs_pinning.pds_record.queued`, `ipfs_pinning.pds_record.published`, `ipfs_pinning.subdomain_registry.linked`, `ipfs_pinning.restore_proof.created`, `porcupin.connect`, `porcupin.premium.check` |
| MindWalk | Session + credits | Launch arcade game; BYOK AI; credit deduction; record a pending skllzrmy creator-share audit intent for a later settlement job. No immediate token settlement is claimed. | `arcade.mindwalk.launch`, `arcade.mindwalk.journey_complete`, `arcade.creator_payout.pending` |
| PixelPatterns / PenRose | Session | Open creation tools; export PNG backgrounds. | `creation.pixelpatterns.opened`, `creation.penrose.opened` |
| Discovery engine | Public/session | Random artist/NFT API; Dashboard spotlight card; Discord `/wtf-stats`. | `discovery.random_artist`, `discovery.random_nft`, `discovery.spotlight` |
| Social auto-promote | Admin + opt-in | Review marketplace sale promo tweets; weekly thread drafts. | `social.auto_promote`, `social.weekly_thread` |
| FA2 templates | Admin | Contract Factory FA2 wizard; deploy fixed/mintable/pausable templates. | `contract.fa2.wizard`, `contract.fa2.deploy` |
| Generative art mint | Session | Mint Portal generative editor; p5 template; ZIP export; when opened through Gamma, keep editor, preview, export action, and guide inside the Mint Portal Gamma surface while preserving the local ZIP builder and p5 preview behavior. | `mint.generative.edit`, `mint.generative.export` |

## Pasta Protocol Contract-Management Additions

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Colander public Penne claim management | Session + connected wallet with an allocation | Open a Penne `PastaDistributionFA2` KT1 in `/tools/colander`; select the public `Claim allocation` action; enter the token id; pass the configured network guard; sign and confirm the contract's `claim` entrypoint; refresh the contract facts without routing through Penne, Objkt, Teia, or wtfOS hosting. | `colander.contract_opened`, `colander.contract_action_submitted` |

## Shipping-Parity Refinements

| Domain | Access | Possible interactions | Primary handles |
| --- | --- | --- | --- |
| Beta Dashboard catalog ownership | Public Beta hub; session on the linked route | Inspect Dashboard as the one session-gated collector-context owner in Beta's App Visibility Atlas, questlines, passports, relationship navigator, route groups, and Count recipes after Hoard retirement; open the existing `/dashboard` route through its unchanged session gate without restoring `/hoard` or creating a new route/event. | `public.route.viewed`, `app.interaction.tracked` |
| Pasta portable-site branding | Public/self-hosted collector page | Open any exported Spaghetti, Gnocchi, Ravioli, Rotini, Penne, or Lasagna collector site and receive the app-specific Sugo favicon as a self-contained SVG data URI derived from the site's existing app config; keep HTML/runtime bytes canonical across all six publishers and avoid repo-relative icon dependencies that break after ZIP extraction. | `spaghetti.site_exported`, `gnocchi.site_exported`, `ravioli.site_exported`, `rotini.site_exported`, `penne.site_exported`, `lasagna.site_exported` |

## Current Coverage Gaps To Track

| Gap | Impact | Tracking |
| --- | --- | --- |
| Side-quest schema declares `x_space_attendance`, `x_hashtag_post`, `console_hiscore`, `mint_with_tag`, `mint_in_curation`, and `discord_voice_presence`, but the route whitelist and `runAutoVerify` implementation currently only cover the smaller subset listed above. The new challenge automation engine gives admins registry-backed event/predicate/reward rules, but these specific side-quest enum values still need side-quest adapters or archival before they count as live auto-verifiers. | E2E and reward-trigger suites must either mark these as latent side-quest handles or add route support before treating them as live side-quest auto-verification. | `WTF-BB-127` |
| Some routes perform manual op-hash attestations instead of app-initiated wallet sends. | Monitoring must distinguish verified wallet-backed sends from manual claims until those flows are upgraded. | `WTF-BB-124`, `WTF-BB-126` |
| Public client logs and TV telemetry are intentionally bounded public writes. | Abuse monitoring should watch rates, payload shape, and source distribution even though these are not session-authenticated. | `WTF-BB-056`, TV telemetry rows |
| WTF Casino now has app access, membership-card scaffolding, mocked WTF Button, mocked Rug Pull, and mocked Guinea Pig Raceway modules, but real wagered games remain intentionally fail-closed. | Do not mark Casino wagering live until compliance gates, wallet-backed wager settlement, house accounting, anti-replay controls, WTF Button escrow/randomness contract tests, Rug Pull contract/panic settlement tests, Raceway randomness/replay/effect tests, and game-specific behavior tests exist. | `WTF-BB-138` |
| This inventory is Markdown, not yet a machine-readable manifest. | E2E and rewards integration will be easier if converted into JSON/YAML with domain, route, handle, access, rewardable, and cheat-signal fields. | Follow-up implementation artifact |
