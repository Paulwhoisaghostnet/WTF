# Gnocchi Individual User Stories

Each feature is expressed for Creator, Collector, Curator, Marketplace User, and Administrator, even when a role mostly observes or audits the feature.

## Single-token package import

### Creator

As a Creator
I want to perform single-token package import with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for single-token package import are satisfied
When the creator attempts single-token package import
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of single-token package import before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for single-token package import are satisfied
When the collector attempts single-token package import
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how single-token package import affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for single-token package import are satisfied
When the curator attempts single-token package import
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from single-token package import without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for single-token package import are satisfied
When the marketplace user attempts single-token package import
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover single-token package import without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for single-token package import are satisfied
When the administrator attempts single-token package import
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Open-edition metadata setup

### Creator

As a Creator
I want to perform open-edition metadata setup with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for open-edition metadata setup are satisfied
When the creator attempts open-edition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of open-edition metadata setup before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for open-edition metadata setup are satisfied
When the collector attempts open-edition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how open-edition metadata setup affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for open-edition metadata setup are satisfied
When the curator attempts open-edition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from open-edition metadata setup without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for open-edition metadata setup are satisfied
When the marketplace user attempts open-edition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover open-edition metadata setup without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for open-edition metadata setup are satisfied
When the administrator attempts open-edition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Timed/forever/capped sale mode

### Creator

As a Creator
I want to perform timed/forever/capped sale mode with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for timed/forever/capped sale mode are satisfied
When the creator attempts timed/forever/capped sale mode
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of timed/forever/capped sale mode before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for timed/forever/capped sale mode are satisfied
When the collector attempts timed/forever/capped sale mode
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how timed/forever/capped sale mode affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for timed/forever/capped sale mode are satisfied
When the curator attempts timed/forever/capped sale mode
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from timed/forever/capped sale mode without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for timed/forever/capped sale mode are satisfied
When the marketplace user attempts timed/forever/capped sale mode
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover timed/forever/capped sale mode without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for timed/forever/capped sale mode are satisfied
When the administrator attempts timed/forever/capped sale mode
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Bonding-curve pricing validation

### Creator

As a Creator
I want to perform bonding-curve pricing validation with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for bonding-curve pricing validation are satisfied
When the creator attempts bonding-curve pricing validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of bonding-curve pricing validation before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for bonding-curve pricing validation are satisfied
When the collector attempts bonding-curve pricing validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how bonding-curve pricing validation affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for bonding-curve pricing validation are satisfied
When the curator attempts bonding-curve pricing validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from bonding-curve pricing validation without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for bonding-curve pricing validation are satisfied
When the marketplace user attempts bonding-curve pricing validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover bonding-curve pricing validation without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for bonding-curve pricing validation are satisfied
When the administrator attempts bonding-curve pricing validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Collection/artifact pinning

### Creator

As a Creator
I want to perform collection/artifact pinning with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for collection/artifact pinning are satisfied
When the creator attempts collection/artifact pinning
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of collection/artifact pinning before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for collection/artifact pinning are satisfied
When the collector attempts collection/artifact pinning
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how collection/artifact pinning affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for collection/artifact pinning are satisfied
When the curator attempts collection/artifact pinning
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from collection/artifact pinning without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for collection/artifact pinning are satisfied
When the marketplace user attempts collection/artifact pinning
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover collection/artifact pinning without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for collection/artifact pinning are satisfied
When the administrator attempts collection/artifact pinning
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Open-edition contract deployment

### Creator

As a Creator
I want to perform open-edition contract deployment with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for open-edition contract deployment are satisfied
When the creator attempts open-edition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of open-edition contract deployment before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for open-edition contract deployment are satisfied
When the collector attempts open-edition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how open-edition contract deployment affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for open-edition contract deployment are satisfied
When the curator attempts open-edition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from open-edition contract deployment without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for open-edition contract deployment are satisfied
When the marketplace user attempts open-edition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover open-edition contract deployment without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for open-edition contract deployment are satisfied
When the administrator attempts open-edition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Collector open mint

### Creator

As a Creator
I want to perform collector open mint with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for collector open mint are satisfied
When the creator attempts collector open mint
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of collector open mint before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for collector open mint are satisfied
When the collector attempts collector open mint
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how collector open mint affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for collector open mint are satisfied
When the curator attempts collector open mint
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from collector open mint without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for collector open mint are satisfied
When the marketplace user attempts collector open mint
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover collector open mint without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for collector open mint are satisfied
When the administrator attempts collector open mint
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Live price and remaining supply preflight

### Creator

As a Creator
I want to perform live price and remaining supply preflight with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for live price and remaining supply preflight are satisfied
When the creator attempts live price and remaining supply preflight
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of live price and remaining supply preflight before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for live price and remaining supply preflight are satisfied
When the collector attempts live price and remaining supply preflight
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how live price and remaining supply preflight affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for live price and remaining supply preflight are satisfied
When the curator attempts live price and remaining supply preflight
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from live price and remaining supply preflight without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for live price and remaining supply preflight are satisfied
When the marketplace user attempts live price and remaining supply preflight
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover live price and remaining supply preflight without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for live price and remaining supply preflight are satisfied
When the administrator attempts live price and remaining supply preflight
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Pause or resume sale

### Creator

As a Creator
I want to perform pause or resume sale with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for pause or resume sale are satisfied
When the creator attempts pause or resume sale
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of pause or resume sale before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for pause or resume sale are satisfied
When the collector attempts pause or resume sale
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how pause or resume sale affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for pause or resume sale are satisfied
When the curator attempts pause or resume sale
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from pause or resume sale without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for pause or resume sale are satisfied
When the marketplace user attempts pause or resume sale
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover pause or resume sale without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for pause or resume sale are satisfied
When the administrator attempts pause or resume sale
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Export/recovery manifest

### Creator

As a Creator
I want to perform export/recovery manifest with clear progress, validation, and resumable output
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for export/recovery manifest are satisfied
When the creator attempts export/recovery manifest
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of export/recovery manifest before signing or purchasing
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for export/recovery manifest are satisfied
When the collector attempts export/recovery manifest
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how export/recovery manifest affects provenance, curation, and relationship metadata
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for export/recovery manifest are satisfied
When the curator attempts export/recovery manifest
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from export/recovery manifest without hidden quantity, price, or contract ambiguity
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for export/recovery manifest are satisfied
When the marketplace user attempts export/recovery manifest
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover export/recovery manifest without bypassing app roles or chain safety
So that Gnocchi can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for export/recovery manifest are satisfied
When the administrator attempts export/recovery manifest
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.
