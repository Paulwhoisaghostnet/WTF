# Lasagna Individual User Stories

Each feature is expressed for Creator, Collector, Curator, Marketplace User, and Administrator, even when a role mostly observes or audits the feature.

## Exhibition metadata setup

### Creator

As a Creator
I want to perform exhibition metadata setup with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for exhibition metadata setup are satisfied
When the creator attempts exhibition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of exhibition metadata setup before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for exhibition metadata setup are satisfied
When the collector attempts exhibition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how exhibition metadata setup affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for exhibition metadata setup are satisfied
When the curator attempts exhibition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from exhibition metadata setup without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for exhibition metadata setup are satisfied
When the marketplace user attempts exhibition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover exhibition metadata setup without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for exhibition metadata setup are satisfied
When the administrator attempts exhibition metadata setup
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Ordered token reference parsing

### Creator

As a Creator
I want to perform ordered token reference parsing with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for ordered token reference parsing are satisfied
When the creator attempts ordered token reference parsing
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of ordered token reference parsing before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for ordered token reference parsing are satisfied
When the collector attempts ordered token reference parsing
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how ordered token reference parsing affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for ordered token reference parsing are satisfied
When the curator attempts ordered token reference parsing
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from ordered token reference parsing without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for ordered token reference parsing are satisfied
When the marketplace user attempts ordered token reference parsing
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover ordered token reference parsing without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for ordered token reference parsing are satisfied
When the administrator attempts ordered token reference parsing
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Revision manifest validation

### Creator

As a Creator
I want to perform revision manifest validation with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for revision manifest validation are satisfied
When the creator attempts revision manifest validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of revision manifest validation before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for revision manifest validation are satisfied
When the collector attempts revision manifest validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how revision manifest validation affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for revision manifest validation are satisfied
When the curator attempts revision manifest validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from revision manifest validation without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for revision manifest validation are satisfied
When the marketplace user attempts revision manifest validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover revision manifest validation without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for revision manifest validation are satisfied
When the administrator attempts revision manifest validation
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Exhibition contract deployment

### Creator

As a Creator
I want to perform exhibition contract deployment with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for exhibition contract deployment are satisfied
When the creator attempts exhibition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of exhibition contract deployment before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for exhibition contract deployment are satisfied
When the collector attempts exhibition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how exhibition contract deployment affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for exhibition contract deployment are satisfied
When the curator attempts exhibition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from exhibition contract deployment without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for exhibition contract deployment are satisfied
When the marketplace user attempts exhibition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover exhibition contract deployment without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for exhibition contract deployment are satisfied
When the administrator attempts exhibition contract deployment
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Curator role management

### Creator

As a Creator
I want to perform curator role management with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for curator role management are satisfied
When the creator attempts curator role management
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of curator role management before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for curator role management are satisfied
When the collector attempts curator role management
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how curator role management affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for curator role management are satisfied
When the curator attempts curator role management
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from curator role management without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for curator role management are satisfied
When the marketplace user attempts curator role management
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover curator role management without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for curator role management are satisfied
When the administrator attempts curator role management
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Publish revision

### Creator

As a Creator
I want to perform publish revision with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for publish revision are satisfied
When the creator attempts publish revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of publish revision before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for publish revision are satisfied
When the collector attempts publish revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how publish revision affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for publish revision are satisfied
When the curator attempts publish revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from publish revision without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for publish revision are satisfied
When the marketplace user attempts publish revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover publish revision without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for publish revision are satisfied
When the administrator attempts publish revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Set current revision

### Creator

As a Creator
I want to perform set current revision with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for set current revision are satisfied
When the creator attempts set current revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of set current revision before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for set current revision are satisfied
When the collector attempts set current revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how set current revision affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for set current revision are satisfied
When the curator attempts set current revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from set current revision without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for set current revision are satisfied
When the marketplace user attempts set current revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover set current revision without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for set current revision are satisfied
When the administrator attempts set current revision
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Version history inspection

### Creator

As a Creator
I want to perform version history inspection with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for version history inspection are satisfied
When the creator attempts version history inspection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of version history inspection before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for version history inspection are satisfied
When the collector attempts version history inspection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how version history inspection affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for version history inspection are satisfied
When the curator attempts version history inspection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from version history inspection without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for version history inspection are satisfied
When the marketplace user attempts version history inspection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover version history inspection without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for version history inspection are satisfied
When the administrator attempts version history inspection
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Hosted exhibition export

### Creator

As a Creator
I want to perform hosted exhibition export with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for hosted exhibition export are satisfied
When the creator attempts hosted exhibition export
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of hosted exhibition export before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for hosted exhibition export are satisfied
When the collector attempts hosted exhibition export
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how hosted exhibition export affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for hosted exhibition export are satisfied
When the curator attempts hosted exhibition export
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from hosted exhibition export without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for hosted exhibition export are satisfied
When the marketplace user attempts hosted exhibition export
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover hosted exhibition export without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for hosted exhibition export are satisfied
When the administrator attempts hosted exhibition export
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

## Colander curation handoff

### Creator

As a Creator
I want to perform colander curation handoff with clear progress, validation, and resumable output
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given creator prerequisites for colander curation handoff are satisfied
When the creator attempts colander curation handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Collector

As a Collector
I want to trust the collector-visible result of colander curation handoff before signing or purchasing
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given collector prerequisites for colander curation handoff are satisfied
When the collector attempts colander curation handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Curator

As a Curator
I want to review how colander curation handoff affects provenance, curation, and relationship metadata
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given curator prerequisites for colander curation handoff are satisfied
When the curator attempts colander curation handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Marketplace User

As a Marketplace User
I want to inspect marketplace-safe facts from colander curation handoff without hidden quantity, price, or contract ambiguity
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given marketplace user prerequisites for colander curation handoff are satisfied
When the marketplace user attempts colander curation handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.

### Administrator

As a Administrator
I want to audit, gate, and recover colander curation handoff without bypassing app roles or chain safety
So that Lasagna can be used safely inside the Pasta ecosystem.

Acceptance Criteria:

Given administrator prerequisites for colander curation handoff are satisfied
When the administrator attempts colander curation handoff
Then the system validates inputs, shows status, persists only confirmed durable outputs, and exposes recovery or next-step evidence.

Failure Criteria:

- If required prerequisites are missing, the UI explains the boundary and does not mark the feature complete.
- If network, wallet, pinning, hosting, or indexer state is unavailable, the flow stays recoverable and names the durable artifact already created.
