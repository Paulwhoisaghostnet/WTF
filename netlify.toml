# =============================================================================
# DEPRECATED — retained for historical reference only.
# WTF Gameshow now deploys via Docker on a dedicated Hetzner server.
# See docker-compose.yml, Dockerfile, and Caddyfile for the current setup.
# This file has NO effect on the production deployment.
# =============================================================================

[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
