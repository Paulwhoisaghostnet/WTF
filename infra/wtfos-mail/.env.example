# Outbound smarthost (Resend). Inbound receive works without these; outbound stays disabled until set.
# Sign up at https://resend.com → add domain wtfos.me → verify DNS → create API key.
RELAY_HOST=smtp.resend.com
RELAY_PORT=465
RELAY_USER=resend
RESEND_API_KEY=re_your_api_key_here
